[
  {
    "id": "RUST-STREAM-01",
    "title": "build_history_summary 不过滤 stream 字段，thought/tool/stderr 文本污染伪多轮历史摘要",
    "severity": "medium",
    "category": "data-consistency",
    "target": "C2-stream-json",
    "location": "apps/desktop/src-tauri/src/code_assistant.rs:1595-1599",
    "description": "build_history_summary 拼接伪多轮历史摘要时，读取 transcript 的 output 事件只取 payload.text，未按 payload.stream 过滤。Rust 端 spawn_reader 写入 transcript 时，stdout/stderr/thought/tool 四类都写成 event:\"output\" 且 payload 含 stream 字段（code_assistant.rs:1150-1154）。该函数把所有 output 的 text 一律当作「【AI】回复」拼进摘要。两种污染源：(1) codex/opencode 永远走伪多轮（send_input 第 446-463 行 _ 分支），其 stderr 的诊断/warning 会被当成 AI 回复；(2) claude 因未捕获 cli_session_id 降级为伪多轮（claude_missing_id）时，思考内容（thought）和工具调用 JSON 片段（tool，含未完成的 input_json_delta 如 `{\"path\":\"b`）会被当成 AI 回复拼进摘要。前端 transcriptTextSinceLastInput（plugin-draft.ts:353-366）正确按 stream 过滤，仅 Rust 端此处漏过滤。",
    "evidence": "code_assistant.rs:1595-1599 (Some(\"output\"), Some(p)) => { let text = p.get(\"text\").and_then(|x| x.as_str()).unwrap_or(\"\"); if !text.trim().is_empty() { lines.push(format!(\"【AI】{text}\")); } } —— 没有 p.get(\"stream\") 判断。对比写入侧 code_assistant.rs:1151-1155 json!({\"stream\": item_stream, \"text\": item_text})，stream 字段存在但读取侧忽略。",
    "trigger": "codex/opencode 任意多轮追问（stderr 有输出即触发），或 claude 首轮未捕获到 session_id 后追问（thought/tool 流被拼入历史）。",
    "impact": "伪多轮场景下，喂给 codex/opencode/降级 claude 的历史 prompt 被诊断信息、思考原文、工具调用 JSON 片段污染，导致 CLI 收到混乱上下文，可能生成错误回复或误解历史。codex/opencode 的 stderr 诊断污染在常规使用中就会触发（每次追问都拼历史）。",
    "fix": "建议修复方向（只读建议，未实施）：在 build_history_summary 的 output 分支增加 stream 白名单过滤，例如 `let s = p.get(\"stream\").and_then(|x| x.as_str()).unwrap_or(\"stdout\"); if s != \"stdout\" { continue; }`，与前端 transcriptTextSinceLastInput 取 stream==='stdout' 对齐。若希望伪多轮历史也保留少量 stderr 诊断，可改为按 stream 前缀不同标签（如【诊断】而非【AI】），但绝不应与 AI 回复混为同一类。另需注意：claude 降级路径虽较少触发，但一旦命中会注入不完整 JSON 片段，对 LLM 上下文干扰更大，优先级不低于 codex/opencode 的 stderr。",
    "reasoning": "独立核实通过。读取侧 apps/desktop/src-tauri/src/code_assistant.rs:1595-1599 `(Some(\"output\"), Some(p)) => { let text = p.get(\"text\").and_then(|x| x.as_str()).unwrap_or(\"\"); if !text.trim().is_empty() { lines.push(format!(\"【AI】{text}\")); } }` —— 确无任何 `p.get(\"stream\")` 过滤，所有 output 事件的非空 text 一律当 AI 回复。写入侧 1150-1155 行 `json!({\"stream\": item_stream, \"text\": item_text})` 确认 stream 字段存在但读取侧忽略，与发现描述一致。分类路由核实：OutputFormat::StreamJson（仅 claude，599 行）将 Text→stdout、Thinking→thought(1128)、ToolUse→tool(1142)；OutputFormat::Plain（codex/opencode stdout 及所有 stderr，1148/615 行）按管道名（\"stdout\"/\"stderr\"）写入。触发场景核实：send_input 446-463 行 `_` 分支为伪多轮，codex/opencode 永远命中、claude 在 cli_session_id.is_none()（444-447 行 claude_missing_id）时命中，均必经 build_history_summary（453 行）。两类污染坐实：(1) codex/opencode 常规多轮——stderr 诊断被当【AI】拼入（每次追问拼历史即触发）；(2) claude 降级——extract_stream_json_items 887/975 行的 input_json_delta partial_json 走 name:\"\" → 1134-1135 行 merged=input_json，即不完整 JSON 片段如 {\"path\":\"b 写为 stream:\"tool\" 的 output 事件，再加 thought 原文，均被 build_history_summary 当【AI】。前端对照 plugin-draft.ts:353-366 与 conversations.ts:87 均正确按 payload.stream 过滤，唯独 Rust 此处漏过滤，契约不一致成立。文档取舍清单核查：清单仅接受「伪多轮机制本身」（拼接历史、降级可感知、codex/opencode 非真续接），未声明可混入 thought/tool/stderr；且 code_assistant.rs:1111-1113 注释明确「thought/tool 走独立流……绝不污染 stdout」，本处行为与此设计意图相悖，故非取舍。影响限于伪多轮上下文质量退化（不崩溃、不损坏数据、不涉资金/鉴权），严重度 medium 合理。"
  },
  {
    "id": "RUST-STREAM-03",
    "title": "set_cli_session_id 与 update_session_exit 对 sessions.json 无锁 read-modify-write，reader 与 waiter 线程并发可能丢失更新",
    "severity": "low",
    "category": "concurrency",
    "target": "C2-stream-json",
    "location": "apps/desktop/src-tauri/src/code_assistant/store.rs:274-290 与 :253-270",
    "description": "set_cli_session_id（reader 线程在收到 system/result 行时调用）与 update_session_exit（waiter 线程在进程退出时调用）均对 sessions.json 做 list_sessions→改字段→write_json 的非原子 read-modify-write，无文件锁。两线程分属不同 std::thread::spawn，存在交叉窗口：reader 在进程生命周期的任意时刻可能写 cli_session_id，waiter 在退出时写 status/exit_code/ended_at。若两者交叉（reader 读 sessions → waiter 读 sessions → waiter 写 → reader 写），reader 的 cli_session_id 写入会覆盖 waiter 刚写的 exit 状态，反之亦然。",
    "evidence": "store.rs:274-290 set_cli_session_id：list_sessions()→iter_mut find→record.cli_session_id=...→write_json（无锁）。store.rs:253-270 update_session_exit 同模式。调用方分别在不同线程：code_assistant.rs:1100 reader 线程调 set_cli_session_id；code_assistant.rs:1257-1259 waiter 线程调 update_session_exit。",
    "trigger": "进程恰好退出（waiter 写 exit）的瞬间，stdout reader 收到 result 行（写 cli_session_id）。",
    "impact": "极端时序下 sessions.json 的 cli_session_id 或 exit 状态可能被对方覆盖丢失；概率低（cli_id 通常在进程早期、exit 在结束时），但理论存在且为 store 层通用竞态。",
    "fix": "建议修复方向（仅供参考，不修改）：给 AssistantStore 内部加 Arc<Mutex<()>> 序列化 sessions.json 的读-改-写，或将 sessions.json 的所有写入收敛到单一写线程（如统一 waiter）；set_cli_session_id 即使 cli_id 已捕获也无害重写，update_session_exit 是退出终态应拥有最后写入权。鉴于触发概率低且影响轻，low 是恰当严重度，不必升级。",
    "reasoning": "已逐行核对代码，发现成立。store.rs:274-290（set_cli_session_id）与 store.rs:253-270（update_session_exit）均为非原子 read-modify-write：list_sessions()→iter_mut().find()→改字段→write_json（store.rs:490-496 是整文件覆盖 fs::write）。AssistantStore（store.rs:115-118）仅含 root: PathBuf，无任何锁；CodeAssistantState（code_assistant.rs:35-39）持有裸 store（非 Mutex）。两调用分处不同线程：reader 线程 code_assistant.rs:1064 spawn，于 :1100 调 set_cli_session_id；waiter 线程 code_assistant.rs:1227 spawn，于 :1257-1259 调 update_session_exit。两条线程共享同一根目录的 sessions.json，无序列化。\\n\\n触发时序可构造：claude stream-json 的 system init 事件通常在早期写 cli_session_id（此时 waiter 尚未写 exit，无重叠）；但若 system 事件未被捕获（如行解析失败/被拆行），reader 仅在末尾 result 事件才拿到 session_id——此刻子进程正在关闭 stdout，waiter 即将或同时检测到退出并写 exit 状态。经典丢失更新：reader 读旧文件（status=running）→waiter 读旧文件→waiter 写 status=exited→reader 写 status=running（覆盖 exit），或反向导致 cli_session_id 丢失。\\n\\n影响良性：仅本地 sessions.json 的 cli_session_id 或 exit 状态可能被对方覆盖。exit 被回退会让 UI 永远显示 running；cli_session_id 丢失则 claude 降级伪多轮（design §3.3.4 已定义的优雅降级）。无资金/鉴权/崩溃/数据损坏风险。\\n\\n文档取舍清单无项覆盖此并发问题（最近相关项是 Node/Python 预览执行安全边界、--resume 伪多轮降级，均不相关）。原评 low 准确：概率性、良性、本地桌面端 UX 瑕疵，符合 low「健壮性/次要边界/可维护性影响正确性」的定义。"
  },
  {
    "id": "RUST-STREAM-04",
    "title": "tool_use 块 input 字段为 JSON null/标量时被序列化为字符串字面量进 tool 流",
    "severity": "info",
    "category": "boundary",
    "target": "C2-stream-json",
    "location": "apps/desktop/src-tauri/src/code_assistant.rs:932-939",
    "description": "extract_stream_json_items 对 tool_use 块取 input 时，match 分支 serde_json::Value::String(s) => s.clone() 透传字符串，但若 input 是裸字符串之外的其它标量（数字/布尔/null），other => other.to_string() 会序列化。null → \"null\"、true → \"true\"、数字 → 字符串。完整 assistant 行（932-939）与 stream_event 的 content_block_start（968-974）两处同一模式。前端收到如 `AskUserQuestion null` 之类片段。非 JSON 对象的 input 罕见但协议不禁止。",
    "evidence": "code_assistant.rs:932-939 block.get(\"input\").map(|v| match v { serde_json::Value::String(s) => s.clone(), other => other.to_string(), }).unwrap_or_default()；null 值 block.get(\"input\") 返回 Some(Value::Null)，进 other => \"null\".to_string()。",
    "trigger": "claude 输出 tool_use 块且 input 字段为 null 或非对象/非字符串标量（极少见的协议变体）。",
    "impact": "工具卡片渲染出现 \"null\"/\"true\" 等字面量文本，非崩溃；边界健壮性问题。",
    "fix": "这是真实但无害的健壮性瑕疵，非文档取舍（清单未覆盖此防御性容错代码）。判定 confirmed 保留，但触发概率极低（协议层 input 几乎必为对象）。若要修复，可把 null/标量统一规范为 JSON 对象序列化：null → \"{}\"，标量 → 该标量包成对象或保持 to_string。鉴于影响仅 UI 文本，info 严重度合适，无需高优先级处理。原发现的描述、位置、严重度评估均准确，未发现夸大或误报。",
    "reasoning": "独立核实代码：code_assistant.rs:932-939（完整 assistant 行 tool_use 分支）与 968-974（content_block_start）确实为 `block.get(\"input\").map(|v| match v { serde_json::Value::String(s) => s.clone(), other => other.to_string(), }).unwrap_or_default()`。当 input 为 null 时 `block.get(\"input\")` 返回 Some(Value::Null)，进 other 分支，serde_json 的 Value::Null.to_string() 产出字面量 \"null\"；true→\"true\"，数字→其字符串形态。下游 code_assistant.rs:1131-1143 把 input_json 拼进 format!(\"{name} {input_json}\") 发往 tool 流，前端工具卡片会显示如 \"AskUserQuestion null\"。\n\n触发场景真实性（关键）：在真实 Anthropic Messages stream-json 协议中，tool_use 块的 input 几乎必为 JSON 对象——完整 assistant 行（type:\"assistant\"）input 是完整对象（单测 1704 行 {\"path\":\"a.ts\"} 已覆盖）；content_block_start 时 input 初始化为 {} 空对象（单测 1769 行已覆盖）；真正的入参增量经 input_json_delta.partial_json 字符串通道（code_assistant.rs:999-1007）传入，根本不经过这两个 match 分支。因此 input:null/true/42 等裸标量是协议中实际不会出现的边缘形态，原发现自己也承认\"极少见的协议变体\"。\n\n文档取舍核对：清单无对应条目，不属任何 explicit-skip/known-limitation/future-work/accepted-risk。代码注释明确这是为兼容\"部分实现把 input 当字符串传\"的防御性容错（935行），非被取舍的能力。\n\n严重度：即便异常发生，影响仅是工具卡片显示 \"null\"/\"true\" 字面量文本，不崩溃、不污染 stdout、不损害数据一致性，无功能损害。原评 info 准确。\n\n结论：代码瑕疵客观存在（other.to_string() 对 null/标量的字符串化是事实），但触发概率极低（协议层 input 几乎必为对象）且影响纯 UI 文本无害。属真实的边缘健壮性瑕疵，confirmed，severity 维持 info。"
  },
  {
    "id": "CAP-01",
    "title": "fs.read 越权错误信息回显 canonicalize 后的绝对路径，造成文件存在性/真实路径信息泄漏 oracle",
    "severity": "low",
    "category": "security",
    "target": "C4-capability",
    "location": "apps/desktop/src-tauri/src/capability.rs:132 (Display 51-59, 调用链 main.rs:64)",
    "description": "OutOfScope 分支把 canonicalize 之后的真实绝对路径（含完整解析符号链接/规范化大小写后的路径）作为错误参数返回；Display 实现把它写进 \"路径超出授权范围: {p}\"，main.rs:64 再 .to_string() 整条作为 Tauri rejection 返回前端，最终 postMessage 到插件 iframe。canonicalize 要求路径必须存在才成功：存在 -> OutOfScope(暴露真实绝对路径，等于确认文件存在 + 揭示真实位置)；不存在 -> canonicalize 失败 -> CapError::Exec(暴露 OS 错误文本，等于确认不存在)。配合 system.info 拿到的 hostname/OS，恶意插件可在 manifest 已声明 fs.read 的前提下，对任意绝对路径做存在性 + 真实路径的盲探。capability-gateway.md 规范只要求 OutOfScope 这个显式分类，并未要求把被解析后的目标路径写进错误字符串。",
    "evidence": "Line 132: Err(CapError::OutOfScope(target.to_string_lossy().to_string())); Line 55: CapError::OutOfScope(p) => write!(f, \"路径超出授权范围: {p}\"); main.rs:64 capability::invoke(...).map_err(|e| e.to_string()); 前端 plugins-runtime.ts:148 frame.contentWindow?.postMessage({..., error: errorMessage(error)});",
    "trigger": "插件声明 fs.read，调用 invokeCapability('fs.read', { path: '<任意绝对路径>' })；路径存在 -> OutOfScope 含真实绝对路径；路径不存在 -> Exec 含 OS 错误。",
    "impact": "任意已声明 fs.read 的插件可枚举用户文件系统的存在性与绝对真实路径（含跨盘符/隐藏目录/用户目录外路径），形成信息泄漏 oracle；同时把宿主真实绝对路径暴露给不可信 iframe。",
    "fix": "修复方向（最小改动、不破坏 capability-gateway.md 的错误分类契约）：让 OutOfScope 不携带任何路径信息，只返回固定串（例如 \"路径超出授权范围\"），并把 canonicalize 失败分支（Line 122）的 OS 错误文本也替换为固定串（如 \"路径不存在或无法访问\"），避免通过 Exec 文本回显存在性。注意：spec 第44行「Requested path must exist and canonicalize successfully」决定了「不存在即失败」的行为本身是契约的一部分，不可改成「不存在当 OutOfScope」，否则会改变契约语义；只需统一把 OS 错误文本脱敏即可。另可考虑：canonicalize 失败时直接归到 OutOfScope 的固定串而非 Exec，这样三类错误的可观察差异被收敛（不存在 / 越权均回固定 OutOfScope 串），可同时关闭两个 oracle 通道。",
    "reasoning": "代码核对完全坐实发现描述的泄漏链：apps/desktop/src-tauri/src/capability.rs:120-122 `canonical_scoped_path` 对 `PathBuf::from(expand_path(raw)).canonicalize()` 失败（路径不存在）映射为 `CapError::Exec(e.to_string())`，把 OS 错误文本（确认「不存在」）抛回；:132 路径存在但越权时返回 `Err(CapError::OutOfScope(target.to_string_lossy().to_string()))`，target 是 canonicalize 之后的真实绝对路径（已解析符号链接、规范化大小写、展开 $HOME）。:55 Display 把该路径拼进 \"路径超出授权范围: {p}\"。main.rs:64 `capability::invoke(...).map_err(|e| e.to_string())` 把整条文本作为 Tauri rejection 返回。plugins-runtime.ts:121 内置插件经 `tauriInvoke('invoke_capability', {...})` 抛出该 rejection，:114-150 `handleRuntimeCall` 在 catch 分支 `frame.contentWindow?.postMessage({..., error: errorMessage(error)}, '*')` 把错误文本原样 postMessage 给不可信 iframe（api.ts errorMessage 直接取 .message）。触发场景可验证：插件 manifest 声明 fs.read，调用 invokeCapability('fs.read',{path:'C:\\\\Windows\\\\System32\\\\drivers\\\\etc\\\\hosts'}) 命中存在路径越权→OutOfScope 暴露 canonicalize 后的 \\\\?\\C:\\Windows\\... 真实路径；path 指向不存在文件→canonicalize 失败→Exec 暴露 \"系统找不到指定的路径\" 文本。构成对任意绝对路径的存在性 + 真实规范化路径盲探 oracle。这与原始发现证据一致。文档取舍核查：capability-gateway.md 第27/44-45/66-68 行确认「路径必须存在并成功 canonicalize 才放行」「不存在即失败」是明示设计，故「通过 Exec 区分存在/不存在」本身属文档接受的存在性语义；但 spec 仅要求 OutOfScope 这个分类（第32/51行），从未要求把 canonicalize 后的绝对路径写进错误串，故真实路径泄露不在文档取舍清单内（清单中的 accepted-risk 仅覆盖 run_plugin_script 逃逸与纯软隔离，未涉及此网关错误文本）。因此判真实缺陷。严重度纠正：原评 medium 偏高。利用前提是插件已被授予 fs.read（开发者声明 + 用户授权），门槛不低；泄漏内容仅为「存在性 + 规范化真实绝对路径」而非文件内容，未造成实际文件读取越权；当前为团队内、代码签名后置的模型下仍是真实但低危的信息泄漏。故下调为 low。"
  },
  {
    "id": "CAP-02",
    "title": "fs.read 读取文件/列目录无大小与数量上限，授权大目录或大文件可致 OOM/卡死",
    "severity": "medium",
    "category": "boundary",
    "target": "C4-capability",
    "location": "apps/desktop/src-tauri/src/capability.rs:114 (及目录分支 100-111)",
    "description": "fs_read 走到文件分支时直接 std::fs::read_to_string(&path)，无任何大小上限或截断；列目录分支也无分页，read_dir 全量收进 entries Vec。manifest 允许把 $HOME/Documents、$HOME/Downloads 这类用户全量子树授权给 fs.read（见 file-explorer/manifest.json），这些子树可能含数 GB 的日志/导出文件或上百万文件的目录。read_to_string 还隐含 UTF-8 校验，超大文件会先把全部字节读进内存再因非 UTF-8 失败，放大内存峰值。文档清单中的 256 KiB/2 MiB/80 文件限制仅约束插件包上传（collab-api 上传约束），与 fs.read 运行态无关——capability-gateway.md 也未给 fs.read 设任何大小边界。",
    "evidence": "Line 114: let content = std::fs::read_to_string(&path).map_err(|e| CapError::Exec(e.to_string()))?;  目录分支 Line 100-111 无任何 cap/分页，entries 无上限 push。",
    "trigger": "插件声明 fs.read 且 paths 含宽子树（默认内置 file-explorer 即 $HOME/Documents、$HOME/Downloads）；对其中一个大文件或大目录调用 fs.read。",
    "impact": "本地 DoS（OOM/卡死整个桌面壳进程），或拖慢到影响其它插件/会话。无数据损坏，但可用性受损。",
    "fix": "建议修复（非本审查范围，仅指方向）：在 capability.rs:98 已有 meta.len()，文件分支可直接 `if meta.len() > MAX_FS_READ_BYTES { return Err(...) }`（如 1 MiB 或与上传对齐 256 KiB），目录分支对 entries 计数加上限并停止/分页。规范 capability-gateway.md §3/§5 应补 fs.read 的 size/count 边界契约。该缺口与清单中的 run_plugin_script accepted-risk 无关：那是「执行任意代码可逃逸」，而此处是「声明式结构化 capability 的资源边界缺失」，属网关本应兜底的健壮性问题。",
    "reasoning": "亲自核实代码：apps/desktop/src-tauri/src/capability.rs:114 文件分支为 `let content = std::fs::read_to_string(&path)...`，无任何大小预检/截断；98 行已取 meta.len() 却未用于边界判断。100-111 行目录分支 `read_dir` 全量 `.flatten()` push 进无上限 entries Vec，无分页/计数上限。文件路径校验在 119-134 行的 canonical_scoped_path 仅做作用域判断，不涉及大小。\n\n触发场景成立：apps/desktop/builtin-plugins/file-explorer/manifest.json:9 声明 `paths:[\"$HOME/Documents\",\"$HOME/Downloads\"]`——宽用户子树。index.html:71 启动即 `loadDir('$HOME/Documents')` 调用 fs.read 列目录（line 36 invokeCapability('fs.read', {path})），main.rs:58 invoke_capability 直接转发到网关。这两个子树常含数 GB 文件（视频/ISO/dump/node_modules）或数十万条目（node_modules/缓存/构建产物），read_to_string 还隐含 UTF-8 校验会把整字节先读入内存再因非 UTF-8 失败，放大峰值。\n\n文档取舍核对（全部排除）：(1) docs/collab-api.md:119「单文件上限 256 KiB，总包上限 2 MiB，最多 80 个文件」明确归属「上传约束」（line 115），仅约束 collab-api 插件包上传，与桌面 fs.read 运行态无关；(2) .trellis/spec/lingfang-desktop/backend/capability-gateway.md 全文（§1-§7）只讲 path canonicalization/scope，未给 fs.read 任何 size/count 边界（grep size/limit/分页/上限 零命中）；(3) ADR-0004 不含 fs.read 资源边界表述。清单中唯一的 accepted-risk「sandbox 仅软隔离 / run_plugin_script 在用户权限下执行任意代码可逃逸」(source: 06-13-node-python-local-exec/design.md §1.3/§6.1) 针对的是任意代码执行通道，与 fs.read 这种结构化能力网关调用是两个独立通道，不覆盖本缺口。清单中无任何条目将 fs.read 的 OOM/DoS 列为取舍。\n\n严重度评估：属本地 DoS（OOM/卡死整个桌面壳进程），无数据损坏、无鉴权绕过。插件跑在 Tauri WebView 内、用户权限下、为用户自装/团队内插件，是自毁式可用性受损而非攻击者可远程利用，故不构成 high/critical。但缺口真实存在、触发场景现实（内置 file-explorer 即默认授权 Documents/Downloads 并启动加载）、且修复成本极低（98 行已有 meta.len()，加一行上限即可），故不降为 low。维持 medium 准确。"
  },
  {
    "id": "CAP-04",
    "title": "match other 分支对已声明但未支持的 capability 返回语义错误的 NotDeclared",
    "severity": "low",
    "category": "contract-mismatch",
    "target": "C4-capability",
    "location": "apps/desktop/src-tauri/src/capability.rs:87",
    "description": "第一步 find() 通过后，kind 只匹配 \"fs.read\" / \"system.info\"；其余任何 kind（如 fs.write、net.fetch、clipboard、storage.kv、system.screenshot 等契约合法但本地未实现的 kind）落入 other 分支，返回 CapError::NotDeclared(\"...未声明能力: <kind>\")。但此时插件 manifest 实际上已声明了该 kind（find 成功），错误文案与事实矛盾，误导排查；capability-gateway.md 要求 \"Unsupported capabilities must return an explicit CapError\"，NotDeclared 虽是显式错误但语义错位（应区分「未声明」与「已声明但本地不支持」）。安全性上仍正确（一律拒绝），仅契约/可维护性问题。",
    "evidence": "Line 79-81 declared = registry.find(...).ok_or_else(|| NotDeclared)?; Line 84-88 match kind { \"fs.read\"=>..., \"system.info\"=>..., other => Err(NotDeclared(other)) } —— other 分支与前面 find 成功语义冲突。",
    "trigger": "manifest 声明 fs.write/net.fetch/clipboard 等本地未实现的合法 kind 后调用。",
    "impact": "无安全后果（仍拒绝）；误导性错误文案，影响调试与契约一致性。",
    "fix": "证据链：capability.rs:79-81 find() 通过即说明 manifest 已声明该 kind；capability.rs:87 other 分支却返回 NotDeclared；capability.rs:54 NotDeclared 的 Display 为\"插件未声明能力: {k}\"。矛盾仅在\"已声明但本地未实现\"的合法 kind（fs.write/net.fetch/clipboard/storage.kv/screenshots/notifications，capability-gateway.md:23 列出）触发。修复方向：新增 CapError 变体如 NotSupported/NotImplemented 用于 other 分支，或直接复用 Exec(\"能力未实现: {kind}\")；保留 NotDeclared 仅用于 Line 81 的 find 失败场景，使其与 capability-gateway.md:31/50 的 Error Matrix 语义一致。无安全后果（仍拒绝），故 low 而非更高。",
    "reasoning": "亲自核实 capability.rs:79-88 与 Display(54)，确认原发现描述准确。Line 79-81 的 find() 通过（kind 已在 manifest 声明）后，Line 84-88 的 match 仅匹配 \"fs.read\"/\"system.info\"，其余任何 kind 落入 other 分支返回 CapError::NotDeclared(other)（capability.rs:87）。该变体 Display(capability.rs:54) 输出\"插件未声明能力: <kind>\"——但此时 find() 已成功，证明插件确实声明了该 kind，只是桌面壳本地未实现（如 capability-gateway.md:23 列出的 fs.write/net.fetch/clipboard/storage.kv/screenshots/notifications 等合法 kind）。触发场景成立：插件 manifest 声明 fs.write 后调用 invoke(registry, plugin_id, \"fs.write\", args)，find 成功 -> other 分支 -> NotDeclared(\"fs.write\") -> 消息\"插件未声明能力: fs.write\"。文档核实：capability-gateway.md:14 要求未实现 capability 返回\"显式 CapError\"（字面满足，NotDeclared 是显式错误），但同文档 Error Matrix(capability-gateway.md:31、50) 明确定义 NotDeclared = \"undeclared capability\"，用 NotDeclared 表达\"已声明但未实现\"违反该矩阵语义定义。文档取舍清单无此项豁免，非取舍。无安全后果（仍一律拒绝），仅契约/错误文案语义错位影响调试，severity=low 维持原评。"
  },
  {
    "id": "CAP-07",
    "title": "缺少规范 §6 明确要求的「授权子路径成功」正向测试",
    "severity": "info",
    "category": "test-coverage",
    "target": "C4-capability",
    "location": "apps/desktop/src-tauri/src/capability.rs:152-207",
    "description": "capability-gateway.md §6 Tests Required 明确列出三项必需测试：「父级穿越拒绝」「同名前缀兄弟目录拒绝」「授权子路径成功」。当前实现只有前两个（fs_read_rejects_parent_traversal_outside_allowed_prefix、fs_read_rejects_same_prefix_sibling_directory），缺失规范明确要求的「授权子路径成功」正向用例。即没有测试证明 fs_read 在 starts_with 成立时能正常返回文件内容/目录列表，回归时一旦 prefix 判定被改坏（如误用字符串前缀）仍可能两负样例通过而正样例失败。",
    "evidence": "Tests: Line 167-186（穿越拒绝）、188-206（兄弟目录拒绝），缺一条形如 \"声明 allowed=D，请求 D/sub.txt 成功返回内容\" 的正向用例。spec capability-gateway.md §6 第63行明确要求。",
    "trigger": "修改 canonical_scoped_path/starts_with 判定逻辑后回归。",
    "impact": "正向路径缺乏回归保护；不直接是运行时缺陷。",
    "fix": "建议补一条形如「allowed=root.join(\\\"Documents\\\")，请求 Documents/sub.txt 成功返回 content；请求 allowed 本身成功返回 entries 列表」的正向测试，使 §6 三项测试齐全。位置应在 capability.rs:206 后、mod tests 结束前追加。这是低成本、零运行时影响的回归保护补强。",
    "reasoning": "亲自核实 apps/desktop/src-tauri/src/capability.rs:152-207：`#[cfg(test)] mod tests` 仅含两个测试 —— fs_read_rejects_parent_traversal_outside_allowed_prefix（167-186 行）与 fs_read_rejects_same_prefix_sibling_directory（188-206 行），二者唯一的断言均为 `assert!(matches!(err, CapError::OutOfScope(_)))`（184、204 行），全模块无任何断言 Ok 成功的正向用例（grep 确认无 .unwrap() 形态的成功断言）。核对规范 .trellis/spec/lingfang-desktop/backend/capability-gateway.md §6 Tests Required（60-63 行）逐字列出三项必测，第三项「Authorized child path success when adding broader coverage」明确缺失；§5（56-57 行）还给出正向语义 Good/Base Cases。文档取舍清单无任何条目豁免此项；沙箱软隔离 / 签名后置 / run_plugin_script 等取舍与 capability.rs 路径授权回归测试无关，spec 反而强化要求。触发场景真实：当前 canonical_scoped_path（119-134 行）实现正确（双端 canonicalize + PathBuf::starts_with），但若未来回归（改回 spec §7 明令禁止的字符串前缀检查、或为「安全」改成无条件返回 OutOfScope），两个负样例仍全绿而正向路径静默失效。严重度维持 info：非运行时缺陷，仅规范明示的回归保护缺口。"
  },
  {
    "id": "SCRIPT-01",
    "title": "plugin_id 未清洗即作为路径分量，导致 remove_dir_all 递归删除任意目录 + sandbox 外落盘执行",
    "severity": "critical",
    "category": "security",
    "target": "C5-script-plugins-main",
    "location": "apps/desktop/src-tauri/src/plugin_script.rs:219",
    "description": "RunPluginScriptInput.plugin_id 直接来自前端 manifest.id（apps/desktop/src/lib/plugin-draft.ts:1243 `parsed.id` 无任何清洗），在 materialize_sandbox 中被原样拼入路径 base.join(\"plugin-sandbox\").join(plugin_id)。plugin_id 既未过 sanitize_rel_path 也未做段级校验，含 '/'、'\\\\'、'..'、绝对盘符或空串时即可越出 plugin-sandbox 目录。",
    "evidence": "let sandbox = base.join(\"plugin-sandbox\").join(plugin_id);  // :219 未清洗\nlet _ = std::fs::remove_dir_all(&sandbox);                  // :221 在未规范化路径上递归删\nstd::fs::create_dir_all(&sandbox)...;                       // :222\n... sanitize_rel_path(&file.path) ...;                      // :225 只防 files[].path\n// 随后 canonicalize(sandbox) 与 canonicalize(entry_abs) 做 starts_with 断言（:237-241），\n// 但此时 sandbox_canon 本身已被 plugin_id 推到 plugin-sandbox 之外，断言恒真。\n前端 pluginId = parseManifest(files).id = parsed.id（plugin-draft.ts:1243，无 sanitize）。\nmain.rs:256-257 已注册该命令，任何前端可经 tauriInvoke('run_plugin_script', {input:{plugin_id:'...', ...}}) 直达。",
    "trigger": "构造 plugin_id=\"../..\"（或 \"..\\\\..\\\\..\"、\"C:/evil\"）调用 run_plugin_script；或 AI 生成的 manifest.json 写 id 含 '/'。",
    "impact": "任意文件覆盖 + 任意目录递归删除（用户权限下，不可恢复）。delete 率先发生，即使后续步骤报错损失已造成。canonicalize 前缀断言这一「软隔离第二道防线」被绕过。",
    "fix": "建议修复：在 materialize_sandbox 入口对 plugin_id 复用 sanitize_rel_path 的段级校验（禁空/./../隐藏系统段/绝对路径/盘符），或新增 sanitize_plugin_id 函数（允许字母数字-_但禁 / 和 \\\\ 和 ..）。同样需要审视 code_assistant.rs 是否有类似未清洗的路径分量。注意 sanitize_rel_path 当前禁止以 '.' 开头的段（隐藏系统段），会拒绝 \"my-plugin\" 不影响但 \".config\" 这种合法目录会被拒——但 plugin_id 通常不含子目录，用更严格的 [A-Za-z0-9_-] 白名单更合适。",
    "reasoning": "亲自核实代码：plugin_script.rs:219 `let sandbox = base.join(\"plugin-sandbox\").join(plugin_id);` 中 plugin_id 直接来自 RunPluginScriptInput.plugin_id（:48），未过 sanitize_rel_path 也未做段级校验；紧接 :221 `let _ = std::fs::remove_dir_all(&sandbox);` 在未规范化的路径上递归删，:222 create_dir_all，:225 只对 files[].path 调 sanitize_rel_path（:233 对 entry 调），plugin_id 是并行的、完全未防御的路径分量。Path::join 不会规范化 .. 段，plugin_id=\"../..\" 时 sandbox=base/plugin-sandbox/../..=base 的父目录，remove_dir_all 越界递归删；plugin_id 含 '/'/'\\\\'/盘符时同样越界。:237-241 的 canonicalize 前缀断言恒真，因为 sandbox_canon 本身已被 plugin_id 推到 plugin-sandbox 之外，entry_canon 必然 starts_with(sandbox_canon)，第二道防线被绕过。前端数据流已核实：ScriptPreviewPanel.tsx:96 pluginId: manifest.id；plugin-draft.ts:1243 id: parsed.id || parsed.name || 'generated-plugin'，parseManifest 全程无 sanitize（Grep 确认无任何 id 清洗/正则）；main.rs:256-257 已注册命令；capabilities/default.json 只控 window 权限，run_plugin_script 作为 invoke_handler 注册命令可被任意前端 JS 经 tauriInvoke 直达。触发场景：AI 生成 manifest.json 写 id 含 '..' 或 '/' 或盘符（如 \"id\":\"../../\"），或前端 JS 直接 tauriInvoke('run_plugin_script', {input:{plugin_id:'../../',...}})。影响：remove_dir_all 率先执行且不可逆，造成 sandbox 外目录递归删除（数据损坏，不可恢复）+ files[].content 任意路径覆盖写（:230 std::fs::write(&abs,&file.content) 中 abs=sandbox.join(rel)，sandbox 已越界）。"
  },
  {
    "id": "SCRIPT-02",
    "title": "超时 kill 仅杀直接子进程，孙进程成孤儿 + wait_with_output 永久阻塞导致命令线程挂起",
    "severity": "high",
    "category": "concurrency",
    "target": "C5-script-plugins-main",
    "location": "apps/desktop/src-tauri/src/code_assistant.rs:1314-1325 (经 plugin_script.rs:280 run_capture_with_env 调用)",
    "description": "超时分支调用 child.kill()（Windows=TerminateProcess 仅终止直接子进程；Unix 默认不杀进程组），随后 wait_with_output() 阻塞读取 piped stdout/stderr 直到管道写端全部关闭。若脚本经 child_process/subprocess 派生孙进程，孙进程继承 stdout/stderr 管道写端，父进程被 kill 后写端仍被孙进程持有，wait_with_output 永远读不到 EOF，调用线程永久阻塞。run_captured_inner 用单一主线程轮询 try_wait + 最终 wait_with_output，不存在并发读管道清空缓冲的 reader 线程，故无法避免此死锁。对照 code_assistant 长会话用 stop_child_process（setsid + kill -PGID / Windows taskkill /T）真正杀进程组，预览执行未复用该逻辑。",
    "evidence": "// code_assistant.rs:1314-1325（run_captured_inner，被 plugin_script.rs:280 复用）\nif started.elapsed().as_millis() > timeout_ms as u128 {\n    let _ = child.kill();                       // 仅直接子进程\n    let output = child.wait_with_output()...;   // 阻塞至所有管道写端关闭\n    return Ok(CapturedOutput { ..., timed_out: true });\n}\n// build_spawn_command (code_assistant.rs:1417-1447) 未设 CREATE_NEW_PROCESS_GROUP / CREATE_NEW_JOB，\n// spawn 时不在独立进程组/作业对象内，kill 无法波及孙进程。\n// 对比 stop_child_process (code_assistant.rs:1195-1219) 用 kill -PGID / child.kill()+wait()。",
    "trigger": "Node 脚本 require('child_process').spawn(...).unref() 后 while(true){}；或 Python 脚本 subprocess.Popen([...).__enter__ 后死循环；达到 timeout_ms 触发 kill 分支。",
    "impact": "(1) 超时后孙进程继续运行，违反「超时 kill」软隔离，可继续 fs/网络/计算；(2) run_plugin_script 命令线程永久挂起，前端 ScriptPreviewPanel 卡在「运行中…」且 15s 超时兜底失效，进程退出才回收。",
    "fix": "修复方向（供后续）：在 build_spawn_command 中为预览执行路径设置独立进程组/作业对象（Unix pre_exec setsid；Windows CREATE_NEW_PROCESS_GROUP 或 Job Object），并把超时分支的回收改为杀整个进程组（复用 stop_child_process 的 PGID kill 语义，或 Windows taskkill /T /PID），最后再 wait_with_output。这样既复用长会话的成熟逻辑，又能让 wait_with_output 真正收到 EOF，避免线程挂起。同时建议为 wait_with_output 加超时上限（如 timeout_ms + 2s）作为防御。",
    "reasoning": "独立复核代码，描述全部属实。\n\n1. run_captured_inner (code_assistant.rs:1279-1328) 是 plugin_script.rs:280 run_capture_with_env 复用的同步轮询+超时函数。超时分支在 code_assistant.rs:1314-1325：\n   ```\n   if started.elapsed().as_millis() > timeout_ms as u128 {\n       let _ = child.kill();\n       let output = child.wait_with_output().map_err(...)?;\n       return Ok(CapturedOutput { ..., timed_out: true });\n   }\n   ```\n   child.kill() 是 SIGKILL(Unix)/TerminateProcess(Windows)，只终止直接子进程，不波及孙进程；wait_with_output() 阻塞读取 piped stdout/stderr 直到所有写端关闭。\n\n2. build_spawn_command (code_assistant.rs:1417-1447) Windows 仅设 CREATE_NO_WINDOW，无 CREATE_NEW_PROCESS_GROUP(0x200)/CREATE_NEW_JOB；非 Windows 分支 (1441-1446) 无 pre_exec(setsid)。故子进程与父同进程组/作业，孙进程继承 stdout/stderr 管道写端。spawn_and_attach (code_assistant.rs:567-573) 对比确凿：长会话补 pre_exec(libc_setsid)，stop_child_process (1195-1219) kill -PGID 杀整组；预览路径完全没复用这套进程组隔离。\n\n3. 触发链成立：用户生成的脚本就是任意 Node/Python。Node: `require('child_process').spawn(process.execPath,['-e','while(true){}']).unref(); while(true){}`；Python: `subprocess.Popen([sys.executable,'-c','while True: pass']); while True: pass`。脚本继承 piped stdout/stderr 句柄，孙进程同样继承写端。到达 timeout_ms 时 child.kill() 杀掉解释器，孙进程仍持有管道写端，wait_with_output() 永远读不到 EOF → run_plugin_script 命令线程永久阻塞。run_captured_inner 是单线程轮询 (1302 loop 内 try_wait + sleep 50ms)，无并发 reader 线程清空管道，无法规避该死锁。插件单元测试 timeout_kills_infinite_loop (plugin_script.rs:447) 仅覆盖 `while(true){}` 单进程，未覆盖派生孙进程场景，故不能反驳本缺陷。\n\n4. 文档取舍复核：design §6.1 accepted-risk 仅声明「child_process 可逃逸（与本地 node main.js 等价风险）」；该取舍覆盖的是「孙进程能做坏事」，但不能覆盖以下两点：(a) design §6.1 把「超时 kill」明确列为软隔离承诺之一——孙进程存活直接破坏了超时停止的承诺；(b) wait_with_output 线程永久挂起是清单之外的纯并发正确性 bug，与「可逃逸」取舍无关。§1.3/§4.3 的「无参数一次性 + 默认 15s 超时」与「交互式脚本在等待输入时超时」也未覆盖此孙进程致挂起的死锁。\n\n5. 影响：(1) 超时后孙进程继续运行（fs/网络/CPU），违反软隔离承诺；(2) run_plugin_script 命令线程永久挂起，前端 ScriptPreviewPanel 卡在「运行中…」且 15s 兜底失效，需重启应用才能回收。\n\n严重度维持 high：属线程永久挂起的不可恢复 DoS 且破坏文档安全承诺，触发条件清晰（开发者自生成脚本可派生子进程）。仅因需特定脚本形态而非随机攻击触发，未上调到 critical。"
  },
  {
    "id": "SCRIPT-03",
    "title": "probe_script_runtime 误判 Store stub python 为可用（仅查 timed_out，未查退出码/版本内容）",
    "severity": "medium",
    "category": "error-handling",
    "target": "C5-script-plugins-main",
    "location": "apps/desktop/src-tauri/src/plugin_script.rs:132-144",
    "description": "probe 命中候选后仅以 !captured.timed_out 判定 available，未校验 exit_code==0 也未校验版本行是否真含 'Python'/'v' 字样。Windows 上未装 Python 时 Microsoft Store 的 python.exe 别名（位于 %LOCALAPPDATA%\\Microsoft\\WindowsApps\\，0 字节 stub，is_file()==true）会被 find_binary 命中；其 --version 不挂起（timed_out=false），而是向 stderr 打印 'Python was not found; run without arguments to install from the Microsoft Store...' 并以 9009 退出。结果 available 被置为 true、version=Some(Store 提示语)，绕过了 py launcher 优先策略的设计意图（candidate 顺序 py>python>python3 仅在 py 存在时才生效；当 py 缺失而 Store stub 存在，即「完全未装 Python」的最常见场景，stub 被误判为可用）。",
    "evidence": "// plugin_script.rs:132-144\nOk(captured) if !captured.timed_out => {\n    let raw_version = format!(\"{}\\n{}\", captured.stdout.trim(), captured.stderr.trim());\n    let version = raw_version.lines().find(|l| !l.trim().is_empty()).map(|l| l.trim().to_string());\n    return Ok(ProbeResult { available: true, binary_path: Some(...), version, hint: None });\n}\n// captured.exit_code（=Some(9009)）与 stdout 内容均未参与判定。",
    "trigger": "Windows 上未安装 Python（py launcher 与真实 python 均不存在），但 Microsoft Store 应用执行别名 python.exe 处于激活态（默认开启）。probe_script_runtime('python')。",
    "impact": "解释器缺失的最常见 Windows 场景被误报可用，友好降级（hint/重试/源码预览）失效；点击运行后 stub 退出 9009，前端落到 nonzero_exit 而非 interpreter_missing，提示语混乱。",
    "fix": "修复方向（仅审查建议，不在本任务范围内执行）：在 plugin_script.rs:132 的 guard 上叠加 exit_code 与版本语义校验——例如 Ok(captured) if !captured.timed_out && captured.exit_code == Some(0) && raw_version.to_lowercase().contains(\"python\")（nodejs 候选同理校验 v 字样或 exit 0）。这样 Store stub 的 9009 会落入 _ 分支 continue 到下一个候选，最终落到 interpreter_missing + hint，恢复设计 §4.5 的真实意图。注意 Python 实现（如 PyPy）输出格式可能不含 'Python' 字样，需谨慎收紧校验条件——可改为仅以 exit_code==Some(0) + 排除 stderr 含 'was not found' 作为判定。",
    "reasoning": "亲自核实代码后确认发现属实。\n\n1) plugin_script.rs:132-144 的 guard 确为 `Ok(captured) if !captured.timed_out`，仅查 timed_out，未查 captured.exit_code 也未校验 stdout/stderr 内容是否真含 `Python x.y.z`。源码注释（:118, :124）声称 --version 实跑可「确认非 stub」，但实现未做内容/退出码判定，注释与代码不一致。\n\n2) CapturedOutput（code_assistant.rs:1268-1273）确实带 exit_code: Option<i32>，信息可用却被忽略。\n\n3) find_binary（code_assistant.rs:1382-1407）按 full.is_file() 命中候选。Windows Store 应用执行别名 python.exe 位于 %LOCALAPPDATA%\\Microsoft\\WindowsApps\\，是 0 字节 stub，is_file()==true，会被 find_binary 命中。\n\n4) 触发场景成立：Windows 上未装 Python（py 缺失）但 Store stub 处于激活态（默认开启）时，候选顺序 [py, python, python3]（plugin_script.rs:107）跳过 py 命中 python stub。Store stub 执行 `--version` 不挂起（无 TTY stdin），而是 stderr 打印 \"Python was not found; run without arguments to install from the Microsoft Store...\" 并以 9009 退出 → timed_out==false → guard 匹配 → available=true，version 取首行非空即 Store 提示语。\n\n5) 影响路径已核实：ScriptPreviewPanel.tsx:65-69 据 result.available 进入 ready 态并在 :186 把 stub 提示语当作 version 展示；点击运行后 stub 退出 9009，plugin-script.ts:100-107 归为 nonzero_exit 而非 interpreter_missing，丢失安装指引。这正是「完全未装 Python」最常见 Windows 场景被误报可用。\n\n非文档取舍：design.md §4.5（:460-462 已确认约束）与 §6.4（:517）明确要求 --version 实跑确认非 stub；实现未真正确认（未查 exit_code/版本语义），属实现未达成已确认约束，非 accepted-risk/known-limitation。design 注释提到「stub 会挂起由超时兜底」的假设对真正 Store stub 不成立（stub 立即 9009 退出）。\n\n严重度 medium 准确：UX/功能降级，非数据损失/崩溃/安全；用户重装 Python 后可自愈，但友好引导中断、提示语混乱。"
  },
  {
    "id": "SCRIPT-04",
    "title": "sandbox 目录无最终清理，按 plugin_id 累积残留（资源泄漏）",
    "severity": "low",
    "category": "resource-leak",
    "target": "C5-script-plugins-main",
    "location": "apps/desktop/src-tauri/src/plugin_script.rs:219-231",
    "description": "materialize_sandbox 仅在每次同名 plugin_id 运行时 remove_dir_all 自身目录，从不回收其它 plugin_id 的历史 sandbox。AI 生成的 plugin_id 随对话变化（safePluginId 仅在前端 buildLocalDraft 用，run_plugin_script 路径用的是 manifest.id 未经 safePluginId），app_data_dir/plugin-sandbox/ 下会持续堆积孤立目录（每个含上次写入的脚本），无任何清理或上限。",
    "evidence": "// plugin_script.rs:219-222\nlet sandbox = base.join(\"plugin-sandbox\").join(plugin_id);\nlet _ = std::fs::remove_dir_all(&sandbox);   // 仅清当前 plugin_id\nstd::fs::create_dir_all(&sandbox)...;\n// 无 LRU/容量上限/退出清理；前端传 pluginId = manifest.id（非 safePluginId）。",
    "trigger": "反复运行不同 manifest.id 的 nodejs/python 插件（对话式创建多次迭代）。",
    "impact": "长期使用后 app_data 膨胀；孤立目录中残留旧脚本（可能含敏感信息）无法回收。功能不致失效，故归 low。",
    "fix": "补充修正原 finding 描述的一处不准确：原 finding 称 run_plugin_script 用「manifest.id 未经 safePluginId」与「safePluginId 仅在前端 buildLocalDraft 用」——这点属实，但需澄清 manifest.id 并非任意随机串，而是模型从 prompt 语义派生的 slug（如番茄钟插件通常为类似 'pomodoro-timer' 这类稳定 slug），同一类插件多次迭代往往复用同一 id（此时被自清理覆盖）；只有切换到「完全不同用途的插件」时才新增目录。因此累积速率比「每次对话都新增」慢，但不改变无清理的事实，仍是真实（虽小的）缺陷。修复方向：在 run_plugin_script 落盘前/后或 app 退出时（on_run_event）做一次 plugin-sandbox 全目录扫描，保留最近 N 个（按 mtime LRU）或仅保留当前 plugin_id 目录，删除其余；亦可设置总容量上限。属改进建议级别。",
    "reasoning": "独立核实三处代码与文档，结论与原 finding 一致（仅对影响描述做了精简修正）。\n\n证据链：\n1) apps/desktop/src-tauri/src/plugin_script.rs:219-221 — materialize_sandbox 中 `let sandbox = base.join(\"plugin-sandbox\").join(plugin_id);` 紧跟 `let _ = std::fs::remove_dir_all(&sandbox);`。此 remove 仅删当前 plugin_id 自身目录，逐文件重新写入。整个文件无任何跨 plugin_id 清理、无 LRU、无容量上限、无 mtime 退役。\n2) 全仓搜索 plugin-sandbox/cleanup/on_window_event/RunEvent 在 src-tauri/src 下：plugin-sandbox 字面仅在 plugin_script.rs:5/219 出现；main.rs setup(220) 之外无 on_window_event / on_run_event 处理；唯一相关 remove_dir_all 在 plugin_script.rs:221（自清理）以及 capability.rs / code_assistant.rs 的测试与独立 store 目录（与 plugin-sandbox 无关）。确认无启动/退出/周期清理。\n3) 触发链成立：ScriptPreviewPanel.tsx:96 `pluginId: manifest.id` → plugin-script.ts:70 转为 plugin_id 透传 Rust。manifest.id 来源 plugin-draft.ts:835 `parsedManifest?.id || pluginId`，即 AI 模型（claude/codex/opencode）每次生成可在 manifest 里写任意 id；相同 id 被自清理覆盖、不同 id 累积孤立目录。原 finding 对「safePluginId 仅前端兜底、实际用 manifest.id」的论断正确。\n\n文档取舍核对：通读设计 .trellis/.../node-python-local-exec/design.md §3.3(b)(244-245) 仅写「清空旧内容后重建，避免脏数据」（即同 plugin_id 自清理）；§1.3/§6 全部取舍条目（软隔离、run_plugin_script 绕网关、无交互、Web 端不可运行等）均不涉及 sandbox 目录清理/磁盘膨胀/LRU。所附文档取舍清单亦无此项。故非文档明确取舍。\n\n严重度评估：维持 low。每次运行单包上限 2 MiB / 80 文件（按上传契约保守估），累积为缓慢磁盘膨胀，不致功能失效、无句柄/FD/僵尸进程等活资源泄漏。原 finding「可能含敏感信息」略夸大——sandbox 仅落盘用户自己刚生成的脚本（无 token/密钥，env 经 minimal_env 白名单裁剪），敏感性角度弱；真实影响是 app_data 膨胀 + 旧脚本残留无法回收。属健壮性/可维护性缺陷，low 准确。"
  },
  {
    "id": "SCRIPT-05",
    "title": "read_plugin_file 与 invoke_capability 错误归一为 String，丢失 CapError 语义；内置插件加载失败静默吞没",
    "severity": "low",
    "category": "error-handling",
    "target": "C5-script-plugins-main",
    "location": "apps/desktop/src-tauri/src/main.rs:64; apps/desktop/src-tauri/src/plugins.rs:86-99",
    "description": "main.rs 注册命令时把各模块 Result<_,String> 直接 .map_err(|e| e.to_string())，cap 错误（NotDeclared/OutOfScope/Exec）被压成裸字符串，前端无法据错误类型区分「未声明」与「越权」做差异化提示。plugins.rs:86-89 对 read_dir 失败静默返回空 Vec，对单个插件 manifest 解析失败（缺 id/name/非法 JSON）也静默跳过（parse_manifest 返回 None），启动期 eprintln 仅打印总数，不报告哪些子目录加载失败。",
    "evidence": "// main.rs:64 invoke_capability\ncapability::invoke(...).map_err(|e| e.to_string())\n// plugins.rs:86-89 + 95-98\nlet read = match std::fs::read_dir(base_dir) { Ok(r)=>r, Err(_)=>return result };\nif let Some((plugin, caps)) = parse_manifest(&path) { ... } else { /* 无日志 */ }",
    "trigger": "内置插件目录 manifest.json 损坏/缺字段；或插件调用未声明 capability。",
    "impact": "内置插件加载失败无可见告警，开发者难定位；capability 错误语义丢失。不影响正确性，可维护性问题。",
    "fix": "建议归类为 info/low：1) 若要保留 CapError 语义，可在 capability.rs 给 CapError 实现 Serialize（带 code 字段如 not_declared/out_of_scope/exec），invoke_capability 返回 Result<Value, serde_json::Value> 或结构化错误对象，前端 plugins-runtime.ts:148 可据此做差异化提示（目前前端并未消费结构化类型，属未来增强）。2) plugins.rs load_builtin_plugins 在 parse_manifest 返回 None 时建议记录哪条目录失败（eprintln! 或 tracing::warn），read_dir 失败也建议打印目录路径，便于开发/打包态定位。这些都不影响首发正确性，可在后续健壮性迭代中处理。注意：finding 中「前端无法据错误类型区分」的表述不准确——当前前端并未尝试区分，丢失的只是「未来可程序化分支」的潜力，而非已实现功能。",
    "reasoning": "独立重读代码核实，发现描述的四点代码事实全部属实：\n\n1) apps/desktop/src-tauri/src/main.rs:64 — `capability::invoke(&state.registry, &plugin_id, &kind, &args).map_err(|e| e.to_string())`，确将 Result<_, CapError> 的结构化错误压成裸 String。同样 read_plugin_file 在 main.rs:47-53（canonicalize、防穿越、read_to_string）也全部 .map_err(|e| e.to_string())。\n2) apps/desktop/src-tauri/src/capability.rs:44-49 — CapError 确有三个语义变体 NotDeclared/OutOfScope/Exec，且 Display 实现（51-59）只是把中文前缀拼进字符串。归一化为 String 后前端拿不到结构化枚举只能拿到含中文前缀的字符串。\n3) apps/desktop/src-tauri/src/plugins.rs:86-89 — read_dir 失败 match Err(_) => return result（空 Vec），无任何日志。\n4) apps/desktop/src-tauri/src/plugins.rs:95-98 — parse_manifest 在 manifest.json 读失败 / JSON 非法 / 缺 id / 缺 name 时返回 None（见 23-29 行），if let Some(...) else 分支无日志直接跳过。Grep 确认 plugins.rs 全文无任何 println/eprintln/log/tracing/warn/error，加载失败完全静默。\n5) main.rs:224 — `eprintln!(\"已加载 {} 个内置插件（目录 {:?}）\", loaded.len(), dir)` 仅打总数，不报告哪些子目录失败。\n\n触发场景成立：builtin-plugins/todo-list/manifest.json 等存在（已确认 3 个内置插件）；若打包时某个 manifest.json 损坏或缺字段，启动时静默少加载，仅看到「已加载 N 个」数量异常。capability 错误归一化场景：插件调用未声明 capability 时，前端拿到字符串「插件未声明能力: X」。\n\n文档取舍核对：逐条对照清单，没有任何一项涉及 capability 错误语义归一化或内置插件加载失败日志，故不属已知取舍/接受风险。finding 原评 docTradeoffRef 为空，核实正确。\n\n但影响评估需纠正：finding 称「前端无法据错误类型区分做差异化提示」为已实现功能丢失，这不准确。读 plugins-runtime.ts:110-112 errorMessage 与 139-150 handleRuntimeCall，前端只把错误字符串原样透传到 iframe 的 error 字段，从未尝试据错误类型分支——所以当前并无功能损失，丢失的仅是「未来可程序化区分」的潜力。且字符串本身已带中文前缀（插件未声明能力/路径超出授权范围/能力执行失败），用户已能读到类别。综合：代码事实属实、非文档取舍、可在打包/开发场景触发，属真实但极轻微的可维护性问题；严重度维持 low（贴近 low/info 边界，因不影响正确性）。"
  },
  {
    "id": "RUSTSHIM-01",
    "title": "sessions.json/registry.json 多线程 read-modify-write 无锁，导致丢失更新与潜在 JSON 损坏",
    "severity": "high",
    "category": "concurrency",
    "target": "C3-shim-scan",
    "location": "O:\\lingfang-platform\\apps\\desktop\\src-tauri\\src\\code_assistant\\store.rs:115-118 (AssistantStore 无锁)、:239-290 (upsert/update_session_exit/set_cli_session_id)、:490-496 (write_json fs::write 非原子)；调用方 code_assistant.rs:1221-1265 (spawn_waiter)、:1055-1183 (spawn_reader)、:330-500 (start_session/send_input)",
    "description": "AssistantStore 仅是 root: PathBuf 的 Clone 包装，无任何锁/Mutex。upsert_session/update_session_exit/set_cli_session_id/rename_session/touch_draft_updated_at 全部是 list_sessions() 读 → mutate → write_json 写的非原子 RMW，中间无同步；write_json 用裸 fs::write(path, raw)（open+truncate+write，非 tmp+rename）。多线程并发：start_session/send_input 命令线程 + spawn_waiter 后台线程 + spawn_reader 后台线程同时写同一 sessions.json。设计者对 processes HashMap 用了 Arc<Mutex>（code_assistant.rs:38），却独独遗漏 store 文件写。registry.json（register_process/unregister_process/cleanup_registered_processes）同款非原子 RMW，存在同类丢失更新。",
    "impact": "典型丢失更新：新会话 record 整条丢失（子进程变孤儿且 UI 不可见）；或 update_session_exit(exited) 被 send_input 的 upsert(status=running) 覆盖，会话卡 running，前端 activeExited 守卫据此阻止追问，多轮功能锁死；极端时序下 JSON 整体损坏致全部历史在 UI 不可见。",
    "trigger": "用户开第二个会话时第一个会话恰好在退出（waiter 写 update_session_exit），或 claude 运行中捕获 cli_session_id 时刻正好与会话退出/其他命令重叠",
    "fix": "给 AssistantStore 加 Mutex（与 processes 既有 Arc<Mutex<HashMap>> 同款），或把 sessions.json 的 RMW 串行化到单 writer 线程；write_json 改为 tmp+rename 原子替换。",
    "reasoning": "全新独立发现，不在清单内。"
  },
  {
    "id": "RUSTSHIM-02",
    "title": "send_input 无运行态守卫，对仍在跑的 session 重入会覆盖 process map、孤儿前一轮子进程并交错输出",
    "severity": "high",
    "category": "state-machine",
    "target": "C3-shim-scan",
    "location": "O:\\lingfang-platform\\apps\\desktop\\src-tauri\\src\\code_assistant.rs:416-500 (send_input)、:574-619 (spawn_and_attach processes.insert 覆盖)、:1221-1265 (spawn_waiter 按 session_id 误删)",
    "description": "send_input 仅 `list_sessions().find(session 存在)`（422-427），无 status==='running' 守卫；upsert_session 把 status 重置 running（479）；spawn_and_attach 的 `processes.insert(session_id, child.clone())`（595）无条件覆盖旧 Arc 而不停止旧 child。child1 退出时 waiter 按 session_id 误删 child2 的 map/registry 条目并抢先 update_session_exit(exited)。前端 handleAskUserAnswer（PluginCreatorHome.tsx:597）仅守 `if (!streaming) return`，AskUserQuestion 卡片在 streaming 恒 true 时渲染，故 child1 仍 running 时用户点 option 即触发。",
    "impact": "并行子进程致 transcript 输出交错污染（两轮 AI 回复混在一起）；旧 child1 孤儿未被 stop/registry 追踪；child2 的退出状态/条目被 child1 的 waiter 抢先 update_session_exit/remove 覆盖或清空，child2 变孤儿且 stop_session 因 map 空返回「session 已结束」无法停止；多轮链断裂。",
    "trigger": "AskUserQuestion 卡片回答路径（handleAskUserAnswer 仅查 streaming，会话仍 running 时触发）；或 UI 状态与后端不同步",
    "fix": "send_input 在 find() 后追加 `if session.status == 'running' return Err(...)` 守卫；或 spawn_and_attach 入口若 processes.get 已存在则先 stop_child_process 旧 child 再 insert，并让旧 waiter 优雅退出、跳过其 update_session_exit/unregister 以免误清新轮。",
    "reasoning": "与 SPAWN-01 是同一根因（send_input 重入 + processes.insert 覆盖 + waiter 按 session_id 误清）的双角度报告（C1-spawn 与 C3-shim-scan）。触发可达性分歧：SPAWN-01 判 medium（常规流不可达，依赖 child1 卡死）；RUSTSHIM-02 升 high（AskUserQuestion 路径在 streaming 守卫下可达）。结论一致，触发条件存争议。"
  },
  {
    "id": "SPAWN-01",
    "title": "send_input 复用 session_id 重 spawn 时不停止前一轮仍在运行的子进程，导致孤儿进程 + 进程表/注册表条目被旧 waiter 误删 + 状态被覆盖",
    "severity": "medium",
    "category": "state-machine",
    "target": "C1-spawn",
    "location": "O:\\lingfang-platform\\apps\\desktop\\src-tauri\\src\\code_assistant.rs:498 (send_input→spawn_and_attach)、:594-596 (processes.insert 覆盖)、:1247-1264 (spawn_waiter 退出清理按 session_id)",
    "description": "send_input 仅校验 session 记录存在（422-427），不校验前一轮子进程是否已退出也不停止它。spawn_and_attach 在 594-596 行 processes.insert(session_id, child.clone()) 覆盖旧条目（同 key）。child1 退出时 spawn_waiter 按 session_id 执行 processes.remove + unregister_process + update_session_exit('exited')，会误删 child2 的进程条目/PID 注册表项并覆盖 child2 仍在 running 的状态。",
    "impact": "级联故障：child1 孤儿继续运行（烧 LLM token/CPU）；child2 失去进程条目，stop_session 报『session 不存在』无法停止；child2 PID 不在注册表，应用重启 cleanup_registered_processes 扫不到，永久孤儿；会话状态被 child1 退出码污染为 exited 而child2 还在跑；stdout/stderr 输出事件 child1/child2 交错污染。",
    "trigger": "前一轮 child1 仍存活时调用 send_input（handleAskUserAnswer 仅 streaming 守卫，AskUserQuestion 时 streaming 恒 true）；或 send() 的 activeExited 守卫存在 TOCTOU",
    "fix": "send_input/spawn_and_attach 在 processes.insert 前先 take 旧 Arc 调 stop_child_process 并按 PID（非 session_id）做 register/unregister；或 send_input 入口校验旧 child 仍在 map 则拒绝/先停止。",
    "reasoning": "与 RUSTSHIM-02 是同一根因的双角度报告。SPAWN-01 判 medium 因触发依赖常规流外的低概率边缘（claude headless 在 AskUserQuestion 时是否阻塞存活未测）；RUSTSHIM-02 升 high 因 AskUserQuestion 路径在 streaming 守卫下判定可达。两份结论一致。"
  },
  {
    "id": "SPAWN-02",
    "title": "stop_child_process 在 Windows 不杀进程树（仅 child.kill），长会话停止时孙子进程孤儿（与 SCRIPT-02 的 run_captured 路径不同）",
    "severity": "medium",
    "category": "resource-leak",
    "target": "C1-spawn",
    "location": "O:\\lingfang-platform\\apps\\desktop\\src-tauri\\src\\code_assistant.rs:1195-1219 (stop_child_process)",
    "description": "stop_child_process 的进程组杀逻辑在 #[cfg(unix)] 块内（kill -TERM/-KILL -<pgid>），块外是无条件的 child.kill() + child.wait()（1217-1218）。Windows 编译时跳过 unix 块，Rust Child::kill 在 Windows 是 TerminateProcess 只杀直接子进程不递归杀树。无 taskkill /T、无 CREATE_NEW_PROCESS_GROUP。对比 store.rs:471-482 kill_process Windows 分支用 taskkill /PID /T。",
    "impact": "Windows 上停止长会话时孙子进程（MCP server、工具进程、codex 的 node 子进程等）持续运行，泄漏 CPU/内存/网络连接/可能持有的 LLM 会话。核心停止动作本身成功，损害是渐进性资源累积。",
    "trigger": "Windows 上调用 stop_session 或进程自然退出时走 stop_child_process，且子进程当时有活着的孙子进程",
    "fix": "stop_child_process 的 #[cfg(windows)] 分支用 taskkill /PID <pid> /T [/F]，与 store.rs:kill_process Windows 分支对齐。",
    "reasoning": "与 RUSTSHIM-03 是同一 stop_child_process Windows 不杀进程树缺陷的双角度报告（C1-spawn vs C3-shim-scan）。与 SCRIPT-02（run_captured_inner 超时路径）不同代码路径。"
  },
  {
    "id": "SPAWN-03",
    "title": "append_transcript 无文件锁/Mutex，stdout+stderr 双 reader 并发追加导致 JSONL 行交错损坏",
    "severity": "medium",
    "category": "data-consistency",
    "target": "C1-spawn",
    "location": "O:\\lingfang-platform\\apps\\desktop\\src-tauri\\src\\code_assistant\\store.rs:328-347 (append_transcript)；调用方 code_assistant.rs:602-617 (stdout/stderr 双 reader)",
    "description": "append_transcript 用 OpenOptions append 打开 + writeln! 写一行，无 Mutex、无文件锁。spawn_and_attach 在 602-617 行对同一 session 同时启动两个 spawn_reader（stdout + stderr），并发写同一 {sessionId}.jsonl。writeln! 经 write_fmt → write_all 循环 write()，对大 payload 拆成多次 write() 系统调用，另一 fd 的 write 可插入其间造成 JSONL 行撕裂。",
    "impact": "JSONL transcript 文件出现交错/半行/非法 JSON。下游 read_transcript / build_history_summary 解析损坏行时 serde_json::from_str 失败被静默 continue/skip，丢失该行产出；伪多轮历史摘要残缺；本轮 assistant turn 文本不全。",
    "trigger": "任何单会话运行期间（stdout+stderr 两 reader 天然并发写同一文件）；SPAWN-01 旧 child reader 未退出时加剧（4 个并发写者）",
    "fix": "给 append_transcript 加 per-file 或 per-store 的 Mutex，或把 transcript 写入串行化到单 writer 线程。",
    "reasoning": "全新独立发现。原描述『Linux O_APPEND 对 write()<4096 原子，超 4096 被拆』论据有误（PIPE_BUF 仅适用管道/FIFO，普通文件无 4KB 上限），但底层 write_all 拆分缺陷仍真实可触发。"
  },
  {
    "id": "RUSTSHIM-04",
    "title": "collect_workspace_files 跟随目录符号链接但无环检测，sandbox 内 symlink 环导致无限递归栈溢出",
    "severity": "medium",
    "category": "logic-bug",
    "target": "C3-shim-scan",
    "location": "O:\\lingfang-platform\\apps\\desktop\\src-tauri\\src\\code_assistant.rs:714-776 (collect_workspace_files)",
    "description": "collect_workspace_files 用 entry.metadata()（跟随符号链接返回目标元数据）判定是否目录，对指向目录的符号链接 metadata.is_dir()==true，于是递归 collect_workspace_files(&path) 而 path 是符号链接路径。read_dir(&path) 同样跟随符号链接。函数内无 visited 集合、无深度上限、无 symlink 环检测。sandbox 内存在目录符号链接环（a/link -> a）时递归会沿符号链接无限深入同一物理目录，最终栈溢出 panic。注释『符号链接等非普通文件跳过』具误导性——对目录符号链接根本不跳过而是递归。",
    "impact": "scan_workspace_files 命令栈溢出 panic（栈溢出无法被 catch_unwind 捕获，是进程级 abort），整个 Tauri 桌面进程崩溃退出。属本地 DoS / 崩溃。Windows 上 mklink /J junction 无需特殊权限，触发门槛反而更低。",
    "trigger": "claude 在 sandbox 内（--permission-mode bypassPermissions + Write/Bash 工具）创建目录符号链接环，或指向祖先目录的符号链接；随后前端调 code_assistant_scan_workspace_files",
    "fix": "递归前用 symlink_metadata 判定（不跟随），或在递归前 canonicalize(current) 加入 visited HashSet 去重，或加 depth 上限参数。",
    "reasoning": "全新独立发现。"
  },
  {
    "id": "SPAWN-05",
    "title": "spawn_waiter 在 try_wait 返回 Err 时直接 break 不杀子进程，map/注册表已清导致孤儿且不可停止",
    "severity": "low",
    "category": "robustness",
    "target": "C1-spawn",
    "location": "O:\\lingfang-platform\\apps\\desktop\\src-tauri\\src\\code_assistant.rs:1228-1264 (spawn_waiter)",
    "description": "spawn_waiter 循环里 try_wait 返回 Err 时直接 break None（1244），跳过任何 kill，随后照常 remove 进程条目、unregister、update_session_exit('exited', None)、发 exit 事件。child 既没被杀也没被 wait 回收，且 map/注册表已被清空。",
    "impact": "罕见不变量违例：存活子进程被从进程表/注册表抹除却未 kill/wait，外部 stop_session 会得到『session 不存在或已结束』而无法停止；exit 事件 exitCode=null 误导前端。触发条件罕见。",
    "trigger": "try_wait 返回错误（系统瞬时错误、child 句柄异常等罕见情况）",
    "fix": "在 spawn_waiter 的 Err 分支也执行 child.lock().take() 并调用 stop_child_process 回收。",
    "reasoning": "全新独立发现。与 SPAWN-04 同属 spawn_waiter 健壮性问题。"
  },
  {
    "id": "SPAWN-04",
    "title": "stop_session 与 spawn_waiter 抢 child.take() 存在竞态，子进程自然退出先于用户 stop 时 stop 返回错误且状态写成 exited 而非 stopped",
    "severity": "low",
    "category": "concurrency",
    "target": "C1-spawn",
    "location": "O:\\lingfang-platform\\apps\\desktop\\src-tauri\\src\\code_assistant.rs:507-523 (stop_session) vs :1228-1264 (spawn_waiter)",
    "description": "若子进程在用户点停止之前/之中自然退出，spawn_waiter 在 200ms 轮询先 try_wait 拿到 Ok(Some)，在 1239-1241 行 take 掉 child 并 break 走退出路径。随后 stop_session 在 514 行 take 拿到 None → killed=false → 521-523 return Err('session 已结束')。窗口真实存在（spawn_waiter 每 200ms 轮询，CLI 单轮快速退出 + IPC 往返延迟即可命中）。",
    "impact": "用户点了停止却收到『session 已结束』错误 toast；会话状态落 exited 而非用户预期的 stopped，状态机语义与用户意图不一致。spawn_waiter 已写全量输出，无数据丢失。",
    "trigger": "子进程在用户点击停止与 stop_session 拿锁之间退出",
    "fix": "stop_session 521-523 处将『已结束』视为目标达成返回 Ok(()) 而非 Err（让 stop 对『进程已死』幂等）。",
    "reasoning": "全新独立发现，属 stop_session 非幂等契约瑕疵。原评 medium 降为 low（无数据丢失，终态 exited 语义正确）。"
  },
  {
    "id": "SPAWN-06",
    "title": "exit 事件可能早于 reader 的最后一批 output 事件发出，finalizeSession 读 transcript 可能丢末尾产出",
    "severity": "low",
    "category": "concurrency",
    "target": "C1-spawn",
    "location": "O:\\lingfang-platform\\apps\\desktop\\src-tauri\\src\\code_assistant.rs:1260-1263 (spawn_waiter exit emit) 与 spawn_reader:1150-1164 (output emit)",
    "description": "spawn_waiter 与 spawn_reader 是独立线程无同步。子进程退出后 spawn_waiter 在下一次 200ms 轮询的 try_wait 拿到 Ok(Some) 即发 exit 事件；但子进程退出时其 stdout/stderr pipe OS 缓冲区可能还有未读数据，reader 的下一次 read_line 才会读到并 emit output。两端 emit 时序无约束，exit 可能先于最后一批 output 到达前端。",
    "impact": "finalizeSession 读 transcript 文件可能漏掉 reader 尚未 flush 落盘的最后一行 output，导致本轮 assistant turn 末尾产出缺失（多为围栏闭合符或最后一行 stream-json，可致 hasStructuredBlocks 误判为纯对话态降级）。纯 UX/数据完整性轻微问题。",
    "trigger": "子进程退出时 pipe OS 缓冲区仍有数据，且 spawn_waiter 的 200ms 轮询恰好先命中 Ok(Some)",
    "fix": "spawn_waiter 在发 exit 事件前 join reader 线程或等待 reader flush；前端 finalizeSession 读 transcript 增加 retry/短延迟。",
    "reasoning": "全新独立发现。磁盘 transcript 最终不会丢（append_transcript 无缓冲同步落 OS），仅 finalizeSession 的 stale 快照可能漏末尾行。"
  }
]
