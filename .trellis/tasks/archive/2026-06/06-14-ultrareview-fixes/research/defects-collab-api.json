[
  {
    "id": "TEAM-01",
    "title": "redeemInvitation 邀请码计数存在 TOCTOU 竞态：并发兑换会越过 maxUses 超发",
    "severity": "high",
    "category": "concurrency",
    "target": "A2-team",
    "location": "apps/collab-api/src/modules/team.service.ts:36-49",
    "description": "redeemInvitation 先在事务外用 findUnique 读取邀请码，并在事务外完成三项校验：status!=='ACTIVE'（L37）、expiresAt 已过期（L38）、usedCount>=maxUses（L39）。随后的 $transaction 只做 membership.upsert + 无条件的 invitationCode.usedCount increment（L42-47），校验本身没有进入事务，也没有任何行锁或带条件的 updateMany 原子扣减。整个 collab-api 没有设置事务隔离级别（main.ts 仅有 app.setGlobalPrefix，全文 grep 无 isolationLevel），即 PostgreSQL 默认 READ COMMITTED。",
    "evidence": "L36 `findUnique({ where: { codeHash: hashInvite(code) } })` 在 $transaction 之外；L39 `if (invite.usedCount >= invite.maxUses) throw` 在事务之外；L41-49 事务体内 `await tx.invitationCode.update({ where: { id: invite.id }, data: { usedCount: { increment: 1 } } })` 是无条件自增，与 consume() 用 `balanceCents: { gte: amount }` 做条件 updateMany 的写法形成鲜明对比——邀请码扣减未做同样的原子条件更新。READ COMMITTED 下两条并发事务读到同一 usedCount，各自通过 L39 判断，各自 +1。",
    "trigger": "对同一邀请码（maxUses=1 或任何有限值）并发提交两次 POST /api/invitations/redeem；或用脚本同时发起 N 个 redeem 请求。",
    "impact": "邀请码 maxUses 配额形同虚设，攻击者/多用户并发 redeem 同一码即可越量加入团队，直接破坏团队规模边界与准入控制。这是本审查目标列出的核心问题（防超发是否真的有效）——答案是否定的。",
    "fix": "修复方向（与同文件 consume() 已用模式对齐）：把无条件 increment 改为带条件的原子更新并校验影响行数。例如将 L41-49 事务体内改为：先 tx.teamMembership.upsert；再用 tx.invitationCode.updateMany({ where: { id: invite.id, status: 'ACTIVE', usedCount: { lt: invite.maxUses }, OR:[{expiresAt:null},{expiresAt:{gt: new Date()}}] }, data: { usedCount:{increment:1} } }) 并断言 result.count===1 否则回滚（throw badRequest('邀请码无效/已满/已过期')）。同时将 findUnique 的读取挪入事务内（或事务内重新读取）以消除 TOCTOU。expiresAt 过期与 team.status 校验也建议一并放进 updateMany 的 where 条件做原子校验，避免读后写窗口。注意 createInvitation 的 maxUses 与 usedCount 同为 INTEGER，无溢出风险。",
    "reasoning": "亲自核实代码，发现描述全部属实。team.service.ts:36 的 findUnique 走 this.prisma（事务外自动提交读），L37-40 四项校验（status!=='ACTIVE'、expiresAt 过期、usedCount>=maxUses、team.status!=='ACTIVE'）均在 $transaction 之外完成；L41-49 事务体内 L47 是无条件 usedCount:{increment:1}，无任何条件 where 守护。对比同文件 consume() (L123-132) 正确用 tx.team.updateMany({where:{...,balanceCents:{gte:amount}},data:{decrement}})+count!==1 校验做原子条件更新，redeemInvitation 完全没有同款防护，属同类防超发缺口的真实不对称。隔离级别核实：main.ts:1-36 无 isolationLevel 设置；apps/collab-api 全目录 grep isolationLevel|SERIALIZABLE|REPEATABLE READ|FOR UPDATE 零命中，即 PostgreSQL 默认 READ COMMITTED。数据库侧防线核实：migrations/0001_init/migration.sql:59-71 InvitationCode 表只有 pkey/fkey/codeHash 唯一索引/两个普通索引，无 CHECK(usedCount<=maxUses) 约束。触发时序成立：READ COMMITTED 下并发两请求对 maxUses=1 同码均读到 usedCount=0、各自过 L39、各自 +1，结果 usedCount=2 且两个不同用户均加入团队（TeamMembership 唯一约束 teamId_userId 只能去重同一用户重复 redeem，挡不住不同用户）。文档取舍核实：清单内 collab-api 相关条目（单当前团队+邀请码模型、团队管理员只能访问所属团队、普通用户只能访问所属团队授权数据）反而强化了邀请码作为团队准入边界的地位，超发破坏该意图而非豁免；accepted-risk「两后端 JWT 不互通」与本并发问题无关。严重度 high 合理：非 critical（非直接资金损失/鉴权绕过），但属核心准入边界控制的并发竞态绕过，普通并发请求即可触发，无 workaround。"
  },
  {
    "id": "TEAM-02",
    "title": "submitApplication 并发可生成多条 PENDING 申请（无唯一约束）",
    "severity": "medium",
    "category": "concurrency",
    "target": "A2-team",
    "location": "apps/collab-api/src/modules/team.service.ts:20-28",
    "description": "submitApplication 先 `findFirst({ where: { userId, status: 'PENDING' } })`（L21）判断是否已有待审申请，未命中则在事务外 `create`（L23-25）。schema 的 TeamAdminApplication 没有任何 (userId, status) 唯一约束（仅 @@index([userId, status])，schema.prisma L144）。两请求在 READ COMMITTED 下都看不到对方的 in-flight 记录，于是各自 create。",
    "evidence": "L21 `const pending = await this.prisma.teamAdminApplication.findFirst({ where: { userId, status: 'PENDING' } }); if (pending) return ...` 与 L23 `create` 之间无事务、无唯一约束。schema.prisma L129-145 TeamAdminApplication 仅有 @@index([status, createdAt])、@@index([userId, status])，无 unique。",
    "trigger": "同一用户并发提交两次 POST /api/team-admin-applications（双击/重试/脚本）。",
    "impact": "同一用户出现两条 PENDING 申请；adminApplications 列出两条；approve 时 createTeamForApplication 需 findUnique(id) 按指定 id 处理，另一条长期滞留 PENDING 形成脏数据；myApplication 仅返回最新一条（orderBy createdAt desc），用户/管理员可能对另一条无感知。",
    "fix": "位置确认 apps/collab-api/src/modules/team.service.ts:20-28（submitApplication）；schema apps/collab-api/prisma/schema.prisma:129-145（TeamAdminApplication 无 unique）；审批链 apps/collab-api/src/modules/admin.service.ts:231-234 与 apps/collab-api/src/modules/auth.service.ts:135-151（createTeamForApplication 按 id 处理、不连带处理其他 PENDING）；入口 apps/collab-api/src/modules/applications.controller.ts:13-17（无节流）。修复方向：在 schema 上加部分唯一索引（Postgres：CREATE UNIQUE INDEX ON \"TeamAdminApplication\"(\"userId\") WHERE \"status\"='PENDING'），或用 Prisma upsert/原子 create+where 守卫。原 prd 0003 迁移（wallet/economy）与本表无关，不影响结论。",
    "reasoning": "独立核实全部成立。team.service.ts:21-25 中 submitApplication 先 findFirst({userId,status:'PENDING'}) 判断（L21-22），未命中则 create（L23）——check 与 act 之间无事务、无锁。schema.prisma:129-145 TeamAdminApplication 仅有 @@index([status,createdAt]) 与 @@index([userId,status])，无任何 @@unique；migration.sql（0001_init:108-109）确认 DB 端无 partial unique index；其余三个迁移对该表零改动。因此 READ COMMITTED 下同一认证用户并发 POST /api/team-admin-applications（applications.controller.ts:13-17，无节流）时，两请求 findFirst 都返回 null，各自 create，产生两条 PENDING。\\n\\n影响链亦核实：approve 走 admin.service.ts:231-234 → auth.service.ts:135-151 createTeamForApplication，按指定 id findUnique 处理，不连带作废同用户其他 PENDING，故另一条滞留为脏 PENDING；myApplication（team.service.ts:30-33）orderBy createdAt desc 仅返回最新一条，用户对第二条无感知；adminApplications（admin.service.ts:224-228）findMany 全列出，管理员会看到两条。触发场景真实（双击/重试/脚本并发）。\\n\\n严重度评估：维持 medium。申请为低频管理动作，重复 PENDING 不直接造成资金损失/鉴权绕过/崩溃；脏记录可通过现有 rejectApplication 清理。但极端时序下（双提交 + 双审批）createTeamForApplication 会为两个不同 id 各建一个团队，数据膨胀比原描述略重，仍非数据损坏级。综合判定为 medium 合理，不纠正。"
  },
  {
    "id": "TEAM-03",
    "title": "软删除/挂起团队未阻断其资源：SUSPENDED 团队的成员仍可消耗余额、创建/列出邀请码、移除成员",
    "severity": "medium",
    "category": "data-consistency",
    "target": "A2-team",
    "location": "apps/collab-api/src/modules/admin.service.ts:100-116",
    "description": "adminDeleteTeam 仅将 team.status 置为 SUSPENDED（L102）并写审计，不连带停用团队成员关系、邀请码或阻止余额操作。而 team.service.ts 中 balance()/ledger()/consume()/currentMembers()/removeMember()/createInvitation()/listInvitations()/disableInvitation() 全部经由 ensureCurrentTeam/ensureTeamAdmin，二者都只过滤 membership.status==='ACTIVE'（auth.service L114-121），从不校验 team.status。对照之下，plugin.service.ts:17 与 team.service.ts:40 都对 team.status==='ACTIVE' 做了校验，说明项目已认可‘挂起团队应阻断其业务’这一语义，但团队域自身接口未落实。",
    "evidence": "auth.service.ts L114 `where: { userId, status: 'ACTIVE' }, include: { team: true }`——include 仅取数据不过滤；L125 ensureTeamAdmin 同样无 team.status 检查。team.service.ts L102-108 disableInvitation、L79-94 createInvitation、L123-132 consume 均不再查 team.status。而 plugin.service.ts:17 `if (membership.team.status !== 'ACTIVE') throw forbidden('团队当前不可上传插件')` 证明团队挂起本应阻断业务。",
    "trigger": "平台管理员删除团队（PUT/DELETE /api/admin/teams/:id）后，原团队管理员凭旧 token 仍可调用 POST /api/teams/current/consume、POST /api/teams/current/invitations 等。",
    "impact": "平台管理员‘删除/停用团队’后，该团队管理员仍可消耗共享余额、生成新邀请码、踢出成员，状态机不一致；审计与实际可操作性脱钩。",
    "fix": "位置锚点：admin.service.ts:100-116（仅置 SUSPENDED）、auth.service.ts:113-127（ensureCurrentTeam/ensureTeamAdmin 无 team.status 检查）、team.service.ts:68-132（removeMember/createInvitation/listInvitations/disableInvitation/balance/ledger/consume 全无 team.status 检查）、security.ts:24-31（无状态 JWT 无 team.status、无吊销）、teams.controller.ts（暴露的 current 端点）。修复方向建议：在 ensureCurrentTeam（或单独守卫）中追加 team.status==='ACTIVE' 校验，让所有团队域业务统一阻断挂起团队；或在 adminDeleteTeam 中连带将 ACTIVE 成员关系置为 SUSPENDED 并把 ACTIVE 邀请码置为 DISABLED，使前端感知一致。",
    "reasoning": "独立重读代码逐条核实，发现描述与代码完全吻合：\n\n1) admin.service.ts:100-116 adminDeleteTeam 仅 `this.prisma.team.update({ where: { id }, data: { status: 'SUSPENDED' } })`（L102）+ audit，不触及 memberships/invitations，也不在事务层阻余额扣减。已确认。\n\n2) auth.service.ts:113-121 ensureCurrentTeam 的 `where: { userId, status: 'ACTIVE' }` 中 status 是 **membership.status** 而非 team.status；`include: { team: true, user: true }` 仅预加载不参与过滤。L123-127 ensureTeamAdmin 调 ensureCurrentTeam 后仅校验 role，无 team.status 检查。已确认。\n\n3) team.service.ts 全部团队域接口（L68 removeMember / L79 createInvitation / L96 listInvitations / L102 disableInvitation / L111 balance / L117 ledger / L123 consume）均经 ensureCurrentTeam/ensureTeamAdmin，无 team.status 校验；consume L126-130 在事务内用 `team.updateMany({ where: { id, balanceCents: { gte: amount } } })` 扣款，对挂起团队照样成功。\n\n4) 对照 plugin.service.ts:17 `if (membership.team.status !== 'ACTIVE') throw forbidden('团队当前不可上传插件')` 与 team.service.ts:40 `if (invite.team.status !== 'ACTIVE') throw forbidden('团队当前不可加入')` 证明项目已认可“挂起团队应阻断其业务”语义，仅团队域自身接口未落实。\n\n触发场景验证：security.ts JwtAuthGuard 为无状态（L24 仅 jwt.verify 取 {sub,email,platformRole}，无 team 状态、无 token 吊销、JWT 默认 7d）。平台管理员 DELETE /api/admin/teams/:id 后，原团队管理员凭旧有效 JWT 调 POST /api/teams/current/consume、POST /api/teams/current/invitations、DELETE /api/teams/current/members/:userId 均仍能成功——状态机不一致、可消耗余额/生成新邀请码/移除成员。\n\n文档取舍核对：清单中团队相关条目（团队数据隔离、团队管理员能力边界）只谈“成员归属/管理员权限范围”，未声明“挂起团队的资源仍可自由操作”为可接受取舍；admin.service.ts:99 注释仅说明软删除=保留数据（避免误删），不构成“操作面仍开放”的取舍声明。无任何 known-limitation/accepted-risk/explicit-skip 覆盖此操作漏洞，故非文档取舍。\n\n严重度核对：原评 medium 准确。无鉴权绕过/资金外泄/崩溃；余额是团队自有（消耗自己余额不构成资金损失），但状态机不一致 + 挂起团队仍可被新邀请扩张 + 余额流水持续消耗，是有真实业务影响的正确性/状态机缺陷，介于 low 与 high 之间，medium 合理。"
  },
  {
    "id": "TEAM-04",
    "title": "consume 负数/非法输入被 Math.max(1,...) 静默改写为 1，而非拒绝；账本金额与调用方意图不符",
    "severity": "low",
    "category": "logic-bug",
    "target": "A2-team",
    "location": "apps/collab-api/src/modules/team.service.ts:125",
    "description": "amount = Math.max(1, Number(input.amountCents || 0))。当调用方传 amountCents: -5、0、undefined、或 'abc'（Number 后 NaN，`||0` 归 0）时，都会被钳制为 1，而不是拒绝。负值防护的实际效果是‘把任何非法输入当成扣 1 分’，而非‘拒绝非法输入’。",
    "evidence": "L125 `const amount = Math.max(1, Number(input.amountCents || 0));`；后续 L127 条件 updateMany 使用该 amount，L129 落一条 reason 来自 input.reason 的 DEBIT 流水。没有任何对 input.amountCents<=0 或非数字的 badRequest 抛错路径（common.ts 有 badRequest 工具但此处未用）。",
    "trigger": "POST /api/teams/current/consume 传 { amountCents: 0 } 或 { amountCents: -5 } 或 { amountCents: 'abc' }。",
    "impact": "调用方误传 0/负值/非法字符串时不会收到 400，而是被静默记一条 1 分的扣款流水，余额与流水均偏离调用方真实意图；虽然不致负余额，但破坏调用契约与可审计性。",
    "fix": "建议修复：consume 入口对 amountCents 做显式校验——Number.isFinite 且 >=1 且为整数才接受，否则抛 badRequest('金额非法')。可用 Number(input.amountCents)。注意 admin.service.ts:138 同一写法存在相同缺陷（adminAdjustBalance），建议一并修。另外可考虑引入全局 ValidationPipe + DTO（class-validator 已在 deps，仅未启用），从框架层统一拦截非法输入，根治此类问题。",
    "reasoning": "独立读码核实，发现描述成立（一处细节需修正，见末段）。\n\n代码证据 apps/collab-api/src/modules/team.service.ts:123-132：\n- L123 consume(userId, input: { amountCents: number; reason?: string })\n- L125 const amount = Math.max(1, Number(input.amountCents || 0));\n- L127 tx.team.updateMany({ where: { id, balanceCents: { gte: amount } }, data: { balanceCents: { decrement: amount } } })；count !== 1 才抛 insufficientBalance\n- L129 落一条 DEBIT 流水，amountCents=amount, reason=input.reason || 'usage'\n- 全函数无对 amountCents<=0 或非数字的 badRequest 路径（common.ts:25 确有 badRequest 工具，但此处未用）\n\n触发场景核实成立：\n- 路由 teams.controller.ts:61-65 @Post('consume') 裸 @Body()，无 ValidationPipe（main.ts:9-34 未 useGlobalPipes；class-validator/package.json 仅在 deps 未启用），运行时无类型校验\n- 该接口为团队成员级公开接口（docs/collab-api.md:64），任何登录成员可调\n- 输入 { amountCents: 0 }：0 || 0 = 0 → Math.max(1,0) = 1 → 落 1 分 DEBIT，扣 1 分，HTTP 200，不报 400\n- 输入 { amountCents: -5 }：-5 || 0 = -5 → Math.max(1,-5) = 1 → 同上落 1 分扣款\n- 输入 { amountCents: undefined }：undefined || 0 = 0 → Math.max(1,0) = 1 → 同上\n- 输入 { amountCents: 'abc' }：注意此处发现描述有误——JS 实际语义是 'abc' truthy → Number('abc') = NaN → Math.max(1, NaN) = NaN → L127 decrement: NaN 与 L129 amountCents: NaN 写入 Prisma Int 列，行为更糟（NaN 写入，DB 层报错或脏写），而非描述中的「NaN 归 0 被钳为 1」\n\n文档取舍核实：清单 41 条均未涉及 consume 输入校验、负数防护策略、Math.max 钳制语义、余额扣款入参约束。非取舍、非 known-limitation、非 future-work、非 accepted-risk。\n\n影响：0/负值/undefined 非法输入被静默当作「扣 1 分」而非拒绝，破坏调用契约与可审计性，余额/流水偏离调用方真实意图；但因 updateMany 的 gte:amount 保护，不会致负余额。无资金损失/数据损坏/鉴权绕过，属输入校验缺失类缺陷，有 workaround。维持 low。"
  },
  {
    "id": "TEAM-05",
    "title": "createInvitation 不校验 expiresAt，过期时间或非法日期被静默接受",
    "severity": "low",
    "category": "logic-bug",
    "target": "A2-team",
    "location": "apps/collab-api/src/modules/team.service.ts:89",
    "description": "createInvitation 的 expiresAt 直接 new Date(input.expiresAt)，不校验时间是否已过去、是否为合法日期字符串。可生成 status=ACTIVE 但实际已过期的邀请码；schema 也有 EXPIRED 枚举值但创建/兑换路径从不使用它（到期判定完全依赖 redeem 时的 expiresAt<now 运行时判断，状态字段不随时间迁移）。",
    "evidence": "L89 `expiresAt: input.expiresAt ? new Date(input.expiresAt) : null`；new Date('2020-01-01') 合法但已过去；new Date('not-a-date') 为 Invalid Date，Prisma 会抛运行时错（500）。schema InvitationStatus 含 EXPIRED（L42-44），但代码从不写入 EXPIRED。",
    "trigger": "POST /api/teams/current/invitations 传 { expiresAt: '2000-01-01' } 或 { expiresAt: 'not-a-date' }。",
    "impact": "管理员可能误建‘一上线就过期’的邀请码而得到与预期不符的状态；listInvitations 仍显示 ACTIVE 误导前端；Invalid Date 输入会 500 而非 400。",
    "fix": "建议修复方向（不在本审查范围内实施）：在 createInvitation 内对 expiresAt 做 try/catch + isFinite(date.getTime()) 校验，过期或非法日期抛 badRequest(400)；可选地在 listInvitations 输出层推导 isExpired 标志或将过期码状态回写为 EXPIRED，以让 EXPIRED 枚举真正生效、避免管理面板误导。controller 也建议加 DTO+ValidationPipe 以统一输入校验（目前整个 collab-api 无全局 ValidationPipe）。",
    "reasoning": "独立逐行核实四个子主张均成立。\n\n(1) 代码确实如此：apps/collab-api/src/modules/team.service.ts:89 `expiresAt: input.expiresAt ? new Date(input.expiresAt) : null`，对输入无任何校验。控制器 teams.controller.ts:33 `@Body() body: { maxUses?: number; expiresAt?: string }` 直接透传，无 DTO/无 ValidationPipe（main.ts:9-13 仅注册 AppExceptionFilter，无 useGlobalPipes；全 collab-api src grep `ValidationPipe|class-validator` 无匹配）。\n\n(2) 创建即 ACTIVE：schema.prisma:147-162 InvitationCode status 默认 ACTIVE（L156 `@default(ACTIVE)`），createInvitation 写入时不显式设 status。故传 `expiresAt:'2000-01-01'` 会得到 status=ACTIVE 且 expiresAt<now 的「已过期仍 ACTIVE」邀请。listInvitations（L96-99）原样返回行（含 status 字段），前端会看到 ACTIVE 误导。\n\n(3) EXPIRED 枚举是死值：schema.prisma:40-44 含 EXPIRED，但全 src grep `EXPIRED` 无任何写入点；到期判定完全靠 redeemInvitation L38 `if (invite.expiresAt && invite.expiresAt.getTime() < Date.now())` 运行时短路，status 字段不随时间迁移。\n\n(4) 非法日期 → 500 而非 400：`new Date('not-a-date')` 返回 Invalid Date 对象（不抛异常），传给 Prisma DateTime? 触发 Prisma 校验异常；common.ts:55-90 AppExceptionFilter 仅捕获 AppError/HttpException，其余一律 L85 落到 500 且 L86-88 把原始 error.message 透出（轻微信息泄漏 + 错误 HTTP 状态码）。\n\n触发场景成立：任意 TEAM_ADMIN（auth.service.ts:123-127 ensureTeamAdmin 限制）POST /api/teams/current/invitations 传 `{\"expiresAt\":\"2000-01-01\"}` 或 `{\"expiresAt\":\"not-a-date\"}` 即可。\n\n文档取舍核对：清单中无任何条目覆盖邀请码 expiresAt 校验或 EXPIRED 状态迁移。唯一相关的「单当前团队 + 邀请码模型」条目仅说明移除 TenantSelect、采用邀请码模型，未把「接受过期/非法日期」列为有意取舍。故非文档取舍。\n\n严重度评估：原评 low 准确。影响仅限 (a) 管理面板展示状态与实际可用性不一致（redeem 路径 L38 正确拦截，无功能性破坏/无资金/无鉴权绕过），(b) 非法输入触发 500 而非 400 + 轻微错误信息泄漏。属典型输入校验缺失 + 状态机瑕疵，不达 high（无核心功能失效、无数据损坏）。"
  },
  {
    "id": "TEAM-06",
    "title": "重新加入已移除成员不更新 joinedAt，与 ensureCurrentTeam 的 joinedAt desc 选当前团队规则冲突",
    "severity": "low",
    "category": "state-machine",
    "target": "A2-team",
    "location": "apps/collab-api/src/modules/team.service.ts:42-46",
    "description": "redeemInvitation 对已存在（含 REMOVED）的 (teamId,userId) 关系走 update 分支，把 status 重置为 ACTIVE、role 置回 MEMBER，但不更新 joinedAt（schema 中 joinedAt 仅有 @default(now)，无 @updatedAt）。ensureCurrentTeam（auth.service L114-118）与 sessionFor（auth.service L66）都以 `orderBy: { joinedAt: 'desc' }` 取第一条作为‘当前团队’。于是被移除用户用旧邀请码/同一团队重新加入后，该关系 joinedAt 仍是历史旧值，可能小于用户在其它团队的关系，导致‘当前团队’指向错误团队。",
    "evidence": "team.service.ts L42-46 upsert `update: { status: 'ACTIVE', role: 'MEMBER' }`（无 joinedAt）；schema.prisma L121 `joinedAt DateTime @default(now())`；auth.service.ts L117 `orderBy: { joinedAt: 'desc' }`、L66 同样的排序。",
    "trigger": "用户先加入团队 A（被移除），再加入团队 B（当前团队=A？B 取决于 joinedAt），随后用团队 A 的邀请码重新 redeem A。",
    "impact": "重新加入旧团队的用户，其 currentTeam/consume/balance 可能落到另一团队，余额与上下文错位；UX 与权限边界混乱。属次要，因正常加入新团队时 joinedAt 是新值，仅‘重新激活旧关系’路径触发。",
    "fix": "建议修复：team.service.ts:45 与 admin.service.ts:123 的 update 分支加上 joinedAt: new Date()（重新加入视为新加入事件，使 ensureCurrentTeam/sessionFor 的 joinedAt desc 语义与设计意图一致）。无需 schema 改动或迁移（joinedAt 已有默认值，更新写当前时间即可）。注意 admin.service.ts 的 adminSetTeamAdmin 同样存在该缺陷（独立于本发现，但同根因），建议一并修。",
    "reasoning": "亲自核对代码，描述属实。team.service.ts:42-46 的 upsert 在 (teamId,userId) 已存在（含 status='REMOVED'）时走 update: { status: 'ACTIVE', role: 'MEMBER' }，未更新 joinedAt。schema.prisma:121 joinedAt DateTime @default(now()) 无 @updatedAt；migration.sql:41 同为 DEFAULT CURRENT_TIMESTAMP 无触发器更新——确认 joinedAt 在重新激活路径保持历史旧值。auth.service.ts:66（sessionFor，被 me/register/login/refresh 调用）与 auth.service.ts:117（ensureCurrentTeam，被 currentTeam/balance/ledger/consume/currentMembers/plugin/marketplace 全链路调用）均用 orderBy: { joinedAt: 'desc' } 取首条作为\"当前团队\"。\n\n触发时序已验证：schema 主键 @@id([teamId,userId]) 仅约束单团队内唯一，不阻止跨团队多 ACTIVE membership。用户先加入 A(T1) → 被移除(status=REMOVED, joinedAt 仍 T1) → 加入 B(T2>T1, ACTIVE) → 重新 redeem A 的邀请码（update 分支，status=ACTIVE 但 joinedAt 仍 T1）。此时 ensureCurrentTeam/sessionFor 在 [A(T1), B(T2)] 中按 desc 选 B，当前团队错指向 B 而非刚加入的 A。redeemInvitation 末尾返回 auth.me(userId)→sessionFor，用户立刻看到错误当前团队。\n\n影响比原发现描述更实质：team.service.ts:123-132 consume 通过 ensureCurrentTeam 从 membership.teamId 扣余额，会扣到 B 团队共享余额而非 A——跨团队数据一致性 bug，非纯 UX。plugin/marketplace 等同样落到错误团队上下文。\n\n文档取舍核对：清单中\"TenantSelect 多团队切换已移除\"(collab-platform.md:86) 仅指移除 UI 显式切换入口，与重新激活时 joinedAt 语义无关；\"单当前团队 + 邀请码\"模型(collab-platform.md:87)隐含依赖 joinedAt 反映最新加入时刻，重新激活时保留陈旧时间戳违背该意图。清单中无任何条目将\"重新激活不更新 joinedAt\"列为 known-limitation/accepted-risk/explicit-skip。故非文档取舍。\n\n严重度：维持 low。触发需多 ACTIVE membership（非\"单当前团队\"主路径）+ 重新激活两条件叠加，路径窄；影响为团队共享余额/上下文错位（用户自发起的 redeem），无权限提升、鉴权绕过、个人钱包资金风险。类别 state-machine 准确（REMOVED→ACTIVE 转移未刷新驱动\"当前团队\"选择的 joinedAt 字段）。"
  },
  {
    "id": "TEAM-08",
    "title": "createTeamForApplication 并发双审批会触发 slug 唯一冲突 500（错误码映射不当）",
    "severity": "low",
    "category": "concurrency",
    "target": "A2-team",
    "location": "apps/collab-api/src/modules/auth.service.ts:135-151",
    "description": "createTeamForApplication 在事务外 findUnique + `application.status !== 'PENDING'` 判定（auth.service L137-139），事务内再 update 为 APPROVED。双平台管理员并发审批同一申请：两请求都通过 L139 检查，各自进入事务尝试 team.create。slug 由 `${slugify(teamName)}-${application.id.slice(0,6)}` 计算（L140），同一 application 的 slug 完全相同，第二个 team.create 命中 Team.slug @unique（schema L104）而抛 PrismaClientKnownRequestError，事务回滚并以未捕获异常形式被 AppExceptionFilter 兜底成 500（common.ts L85-89）。数据完整性靠唯一约束保住，但错误码映射错误、未做幂等。",
    "evidence": "auth.service.ts L137 `findUnique`、L139 `if (application.status !== 'PENDING') throw conflict` 均在 L141 `$transaction` 之前；L140 slug 由 application.id 派生，对同一申请恒定；schema.prisma L104 `slug String @unique`。common.ts L85 非 AppError/HttpException 一律 500。",
    "trigger": "两个平台管理员同时对同一 PENDING 申请点 POST /api/admin/team-admin-applications/:id/approve。",
    "impact": "并发审批第二个请求返回 500 而非 409/幂等成功；slug 唯一性是唯一护栏，一旦 slug 由其它来源（adminCreateTeam 用 slugify(name)，无后缀）已占用同名字符串，正常单次审批也可能意外 500。",
    "fix": "修复方向：把状态机变更做成幂等 + 正确映射 P2002。两种可行做法：(1) 在 $transaction 内部对 application 做带 where 的条件更新——update where { id, status: 'PENDING' }，根据返回行数判定是否已被并发抢走（行数=0 时 return 已有团队或抛 409）；(2) 在 AppExceptionFilter 中针对 Prisma P2002（code 'P2002'，唯一约束冲突）映射为 409 conflict，避免裸 500。两者结合更稳。注意不要把 createTeamForApplication 的状态检查与 team.create 拆到事务外。teamName 中的 Unicode 由 slugify 保留（[一-龥] 未被替换），slug 可能含中文字符；如需 ASCII-only slug 应另议，但这与本并发 bug 无关。原 finding 中\"adminCreateTeam 同名预占导致单次审批 500\"子论断应剔除（两 slug 永不相等）。",
    "reasoning": "独立读码逐行核对，发现成立。\n\n证据链（全部亲自 Read 核实）：\n1. apps/collab-api/src/modules/auth.service.ts:135-151 createTeamForApplication：\n   - L136 ensurePlatformAdmin(reviewerId)（鉴权）\n   - L137 const application = await this.prisma.teamAdminApplication.findUnique(...) —— 在 $transaction 之外读取\n   - L139 if (application.status !== 'PENDING') throw conflict('该申请已处理') —— 状态机判定也在事务外\n   - L140 const slug = `${slugify(application.teamName)}-${application.id.slice(0, 6)}` —— 对同一 application，teamName 与 application.id 均为常量，故 slug 完全确定且恒定\n   - L141 return this.prisma.$transaction(async (tx) => { ... })\n   - L142 tx.team.create({ data: { name: application.teamName, slug } })\n2. apps/collab-api/prisma/schema.prisma:104 model Team 中 `slug String @unique` —— DB 层唯一约束确认；全 schema 无 @version/乐观锁（Grep 命中 0 处 @version），故状态检查与 team.create 非原子。\n3. apps/collab-api/src/common.ts:85-89 AppExceptionFilter：非 AppError、非 HttpException 一律落 500 { code:'internal_error' }。Prisma PrismaClientKnownRequestError（P2002 唯一约束冲突）二者皆不是 → 500。\n4. 路由链确认：admin.controller.ts:128-132 POST team-admin-applications/:id/approve → admin.service.ts:231-234 approveApplication → auth.createTeamForApplication。\n\n触发时序（可复现）：两个平台管理员对同一 PENDING 申请并发点 approve。两请求都 L137 读到 PENDING、都过 L139、都算出同一 slug、都进事务；先者 team.create + application.update→APPROVED 提交成功，后者 team.create 命中 slug @unique 抛 P2002，事务回滚，异常穿透到 AppExceptionFilter 返回 500。数据完整性因 @unique 保住（无误建第二个团队、申请状态不会被二次篡改），但 HTTP 语义错误（应为 409 或幂等成功）、非幂等。\n\n严重度维持 low：竞态真实存在，但唯一约束阻止持久数据不一致；唯一可观测影响是低频管理员操作的 500 与错误码，无资金/鉴权/数据损坏。\n\n对原 finding 的修正（finding 的\"影响\"第二句是误报）：原称 adminCreateTeam 用 slugify(name)（无后缀）\"已占用同名字符串\"会导致正常单次审批也 500。我读 admin.service.ts:86 `slug: input.slug || slugify(name)` 得到裸 slug（如 'foo'），而 application slug 为 `slugify(name)-XXXXXX`（恒带 '-'+6 位十六进制后缀，如 'foo-a1b2c3'），二者永不相等。故 adminCreateTeam 路径不会与 application slug 冲突，单次审批不会因此 500。该子论断应剔除；核心并发发现仍成立。\n\n文档取舍核对：遍历清单，无一条涉及并发审批、slug 冲突、错误码映射；最接近的 JWT 临时不互通/团队角色边界均无关。非文档取舍。"
  },
  {
    "id": "ECO-01",
    "title": "并发购买同一付费插件时，重复竞态靠 Purchase 唯一约束兜底，但 P2002 未捕获导致返回 500 而非 already_purchased",
    "severity": "medium",
    "category": "transaction",
    "target": "A3-economy",
    "location": "apps/collab-api/src/modules/economy.service.ts:67-73,77-108",
    "description": "purchase 的幂等检查在事务外（行 67-73 findUnique），仅有 advisory 性质。并发请求 A、B 同时通过 already==null 判断，都进入 $transaction。READ COMMITTED 下，行 79 updateMany 对买家钱包行加行锁并串行，B 的 updateMany 在 A 提交后重检：若扣款后余额仍 ≥ price，B 也成功扣款并给卖家加款；最终 B 在行 92 tx.purchase.create 命中 @@unique([pluginId, buyerUserId])（schema.prisma:297）抛 Prisma P2002。但 economy.service 全文无 try/catch（已 grep 确认仅 common.ts/security.ts 有 catch），P2002 一路冒泡到 AppExceptionFilter（common.ts:85），被当作 500 internal_error 返回，而非语义化的 already_purchased。",
    "evidence": "行 67 const already = await this.prisma.purchase.findUnique({...}) 在事务外；行 77 await this.prisma.$transaction(async (tx) => { ... }); 内无 catch；行 92 await tx.purchase.create 命中 schema.prisma:297 @@unique([pluginId, buyerUserId])；common.ts:85 默认 500 路径。",
    "trigger": "同一用户对同一付费插件并发提交两次购买（双击、前端无防抖重试、多标签页同时点购买）。",
    "impact": "数据最终一致（P2002 回滚了 B 的扣款/卖家加款/流水），但用户双击或前端重试购买时收到 500 internal_error，而非 409/already_purchased，无法区分『已购买』与『真故障』，前端重试逻辑可能误判。",
    "fix": "修复建议：在 $transaction 外用 try/catch 包裹，捕获 PrismaClientKnownRequestError 且 error.code === 'P2002'（meta.target 含 pluginId/buyerUserId），转换为 common.ts:29 已存在的 conflict('该插件已购买')（409）或自定义 already_purchased AppError，使语义与单次幂等检查一致。另需注意第二个子情况：当买方余额刚好等于 price 时，并发第二笔会得到 402 insufficient_balance（误导），可在事务内扣款前先 tx.purchase.count/findUnique 复检（事务内行锁下安全）或扣款后命中已购买时回滚并返回 already_purchased。",
    "reasoning": "亲自读 economy.service.ts:53-112 与链路核对，发现链路全部成立：\n\n(1) 幂等检查在事务外 — 第 67-69 行 `this.prisma.purchase.findUnique` 发生在第 77 行 `this.prisma.$transaction` 之前。并发 A、B 均可读到 already==null。\n\n(2) 行 79 `tx.wallet.updateMany({ where: { userId, balanceCents: { gte: price } }, data: { balanceCents: { decrement: price } } })` — Prisma + PrismaPg(PostgreSQL) 默认 READ COMMITTED（全仓 grep 无 isolationLevel 覆盖）。A 的 UPDATE 取得买家钱包行锁，B 阻塞，A 提交后 B 用提交后最新余额重评 WHERE。若买家初始余额 ≥ 2×price，B 的 count==1，扣款成功并继续给卖家加款（行 86-90）；随后第 92 行 `tx.purchase.create` 命中 schema.prisma:297 的 `@@unique([pluginId, buyerUserId])`（迁移 Purchase_pluginId_buyerUserId_key 唯一索引已确认），抛 Prisma P2002，Prisma 回滚整个交互式事务（B 的扣款、卖家加款、两条流水全部撤销 — 数据最终一致），并将 P2002 重新抛出。\n\n(3) 全文无 try/catch — grep 确认 apps/collab-api/src/modules 下仅 plugin-package.ts、plugin.service.ts、security.ts、common.ts 含 catch；economy.service.ts 不含。WalletController.purchase(wallet.controller.ts:21-22) 直接 return this.economy.purchase(...)，无拦截。\n\n(4) P2002 到达 AppExceptionFilter(common.ts:55-90，main.ts:13 全局注册)：既非 AppError 也非 HttpException，落入 common.ts:85 `response.status(500).json({ code:'internal_error', ... })`。已存在的 conflict() 助手(common.ts:29)未被使用。\n\n触发场景真实：同一用户对同一付费插件双击购买、多标签页同时购买、前端对网络抖动做无去重重试。\n\n影响：数据最终一致(无双重扣款、无卖家双重入账)，但并发重复购买返回 500 internal_error 而非代码已定义的 already_purchased 形态(行 70-72)，前端重试逻辑无法区分\"已购买\"与\"真故障\"。\n\n补充更强证据 — 第二子情况：当买家余额恰等于 price 时，B 的 updateMany 重评 WHERE(balance=0 >= price)失败，count==0，抛 insufficientBalance() 返回 402，而非 already_purchased —— 此即\"告知用户余额不足，但用户其实已拥有该插件\"的直接误导，进一步佐证契约不一致是真实缺陷而非纯理论。\n\n文档取舍核对：清单无任何条目覆盖\"并发去重/P2002 不映射为语义错误\"；\"单事务结算\"是已遵守的设计原则(购买确在单事务内)，不属于取舍豁免。故确为真实实现缺口。"
  },
  {
    "id": "MKT-02",
    "title": "并发改分会丢失更新导致 ratingSum 与真实评分脱钩",
    "severity": "high",
    "category": "concurrency",
    "target": "A4-marketplace",
    "location": "apps/collab-api/src/modules/marketplace.service.ts:130-144",
    "description": "rate() 先在事务外读取 existing = prisma.pluginRating.findUnique(...)（line 130），再在独立的数组事务（$transaction([...])）里以 `ratingSum: { increment: existing ? score - existing.score : score }` 增量。由于 existing 是事务前快照，两个并发更新会用各自陈旧的 existing.score 计算差量。Postgres 默认 Read Committed 下，两条 plugin UPDATE 都以 `SET ratingSum = ratingSum + delta` 串行提交，delta 基于过期值，导致丢失更新。",
    "evidence": "设初始 ratingSum=5、ratingCount=1、评分行 score=5。用户并发发两次 rate(score=3)：两调用都在事务前读到 existing.score=5，delta 均为 3-5=-2。事务C: ratingSum 5→3；事务D(等行锁后): ratingSum 3→1。最终 ratingSum=1、ratingCount=1、评分行 score=3，avg_score=1.0 而真实平均应=3.0。line 140 `ratingSum: { increment: existing ? score - existing.score : score }` 与 line 137 plugin.update 用同一事务，但 existing（line 130）游离于事务外。",
    "trigger": "同一用户对同一已评插件并发发起两次改分（如双击/多标签页/客户端重试），或对未评插件并发发起 create 路径下两次评分。",
    "impact": "评分聚合（ratingSum/ratingCount）与 pluginRating 明细长期脱钩，avg_score 与排序错误且不可自愈（无重算路径）。同一用户多次改分/重复提交或多个标签页并发即触发。",
    "fix": "修复方向（供后续参考，只读代理不改）：把 existing 读取移入交互式 $transaction（async 回调形式），并在事务内先 SELECT ... FOR UPDATE 锁定 PluginRating 唯一行再计算 delta；或对 Plugin 表的 ratingSum 改为触发器维护 / 改为读取时实时 SUM(score)（去聚合字段）。create 路径并发（未评插件并发两次 rate）同样受影响：两调用都走 create 分支 delta=score、ratingCount +1 各一次，唯一约束让 PluginRating 行收敛但聚合 ratingSum/ratingCount 仍双计；avg 因同比缩放可能巧合正确但 ratingCount 明细错误。",
    "reasoning": "亲自读 marketplace.service.ts:111-147 核对：line 130 `const existing = await this.prisma.pluginRating.findUnique({ where: { pluginId_userId: { pluginId, userId } } })` 确实在 line 131 的 `this.prisma.$transaction([...])` 之外；line 140 `ratingSum: { increment: existing ? score - existing.score : score }` 的 delta 基于这个事务前快照。schema.prisma:312 确认 `@@unique([pluginId, userId])`（upsert 命中同一行），schema.prisma:200-201 确认 Plugin.ratingSum/ratingCount 是非规范化聚合字段。\n\n时序验证（同一用户对同一已评插件并发两次 rate(score=3)，初始 ratingSum=5 / ratingCount=1 / PluginRating.score=5）：两个请求在事务前都读到 existing.score=5，因此两事务各自 delta = 3-5 = -2。plugin.update 是原子 `SET ratingSum = ratingSum + delta`，行锁使两次串行：5→3→1，最终 ratingSum=1、ratingCount=1、PluginRating.score=3 → avg_score=1.0，而真实平均=3.0。这是 delta 基于过期快照的丢失更新，而非单纯两 UPDATE 抢 ratingSum（后者会被原子 increment 串行保护），所以单纯 increment 并不能拯救——根因是 existing 游离于事务外。\n\n无自愈路径：grep 全代码库（apps/collab-api/src、prisma/migrations）无 recompute/recalc/SUM(score)/trigger，avg_score（line 41、67）直接用 ratingSum/ratingCount。controller（marketplace.controller.ts:31-35）直接调用 market.rate，无锁/无防抖。\n\n文档取舍清单核对：无任何条目涉及评分聚合并发去重或评分丢失更新可接受。最接近的条目（市场审核状态机 DRAFT/REJECTED→PENDING→APPROVED/PUBLIC、上传 256KiB/2MiB/80文件约束、平台抽成代价）均与本并发问题无关。故非文档取舍。\n\n严重度评估：原评 high 准确。这是正确性数据一致性 bug——聚合字段（ratingSum/ratingCount）与 pluginRating 明细长期脱钩，avg_score 与按 ratingSum 排序（line 24-25 orderBy）持续错误且不可自愈；触发场景现实存在（同用户双击/客户端网络重试/多标签页并发改分）。不涉及资金/崩溃/鉴权绕过，故不到 critical。"
  },
  {
    "id": "MKT-03",
    "title": "rate 校验过宽：可对 PENDING/DISABLED 状态的市场插件评分",
    "severity": "low",
    "category": "state-machine",
    "target": "A4-marketplace",
    "location": "apps/collab-api/src/modules/marketplace.service.ts:111-127",
    "description": "rate() 的插件可评性校验只做 `where: { id: pluginId, marketplace: true }`（line 113），不像 search/detail/install 那样要求 reviewStatus===APPROVED && status===ENABLED && visibility===PUBLIC。submitPluginToMarketplace 在提交时即把 marketplace 置 true 但 reviewStatus=PENDING（plugin.service.ts:85），此时插件尚未通过审核即可被评分；adminApprovePlugin 后若管理员再 adminUpdatePlugin 把 status 置 DISABLED（admin.service.ts:209-219 不动 marketplace/reviewStatus），已下架插件仍可继续被评分。",
    "evidence": "marketplace.service.ts:113 `const plugin = await this.prisma.plugin.findFirst({ where: { id: pluginId, marketplace: true } });`——对比 detail(line 49)/install(line 88)/search(line 15-22) 均含 reviewStatus:'APPROVED'、status:'ENABLED'。plugin.service.ts:85 提交即 `marketplace: true, reviewStatus: 'PENDING'`。",
    "trigger": "对处于 reviewStatus=PENDING（已提交未审核）或 status=DISABLED（管理员已下架但 marketplace/reviewStatus 未改）的插件调用 POST /api/marketplace/rate。",
    "impact": "未公开/已下架插件的评分进入聚合并影响排序与 avg_score，状态机存在非法前置态；PENDING 阶段被竞品恶意刷分也无从拦截。",
    "fix": "修复建议（最小改动）：在 rate() line 113 把 where 改为 { id: pluginId, marketplace: true, reviewStatus: 'APPROVED', status: 'ENABLED', visibility: 'PUBLIC' }，与 detail/install/purchase 保持一致，消除状态机一致性缺口。注意：原发现的核心论据\"PENDING 阶段恶意刷分\"是误判（不可达），真正的价值是消除与同文件其他端点的校验一致性缺口，而非堵截恶意刷分。验证时不要被\"PENDING 恶意刷分\"误导——应聚焦 DISABLED 后历史用户仍可评分这一真实但轻微的场景。",
    "reasoning": "代码事实确认无误：marketplace.service.ts:113 rate() 的可评性查询仅为 `where: { id: pluginId, marketplace: true }`，未像 search(line 17-18)/detail(line 49)/install(line 88)/purchase(economy.service.ts:55) 那样要求 reviewStatus=APPROVED + status=ENABLED + visibility=PUBLIC。submitPluginToMarketplace (plugin.service.ts:85) 提交即把 marketplace=true 且 reviewStatus=PENDING；adminUpdatePlugin (admin.service.ts:209-219) 置 status=DISABLED 时不动 marketplace/reviewStatus。两者描述属实。\n\n但原发现的主要影响论据【PENDING 阶段竞品恶意刷分】经独立验证【不可达】：rate 的消费前置条件(marketplace.service.ts:119-127)要求 purchase 记录(付费)或 pluginInstallation status=ENABLED(免费)。全 codebase 这两类记录的唯一创建入口——economy.service.ts:92 purchase()(line 55 要求 APPROVED+ENABLED)、marketplace.service.ts:100 install()(line 88 要求 APPROVED+ENABLED)、plugin.service.ts:141 installMarketplacePlugin()(line 134 要求 APPROVED+ENABLED+marketplace+PUBLIC)——全部强制要求插件处于 APPROVED+ENABLED。因此 PENDING 阶段无任何路径能让任何用户满足消费前置，无法评分。\n\n真实可达的是【场景B：DISABLED 后历史用户评分】：时序 APPROVED+ENABLED 时用户 purchase/install → adminUpdatePlugin 把 status 置 DISABLED(line 216 不动 marketplace/reviewStatus)→ 原购买/安装记录仍在 → rate 通过 marketplace=true(仍真) 查询与消费校验，已下架插件仍可被历史已购/已装用户评分，ratingSum/ratingCount(line 137-143) 被写入聚合。这是真实的状态机一致性缺口。\n\n但影响轻微需下调严重度：DISABLED 插件在 search(line 15-22 已过滤 status=ENABLED)、detail(line 49)、install(line 88) 中均已不展示、无法被新用户发现或安装。仅历史已购/已装用户能评分，且该聚合值对已下架插件不可见，不会污染市场排序(search 已过滤)。属健壮性/一致性瑕疵而非正确性 bug，故从 medium 下调为 low。"
  },
  {
    "id": "MKT-04",
    "title": "sort=rating 按 ratingSum 排序而非平均分，与 avg_score 字段语义冲突",
    "severity": "medium",
    "category": "logic-bug",
    "target": "A4-marketplace",
    "location": "apps/collab-api/src/modules/marketplace.service.ts:23-28",
    "description": "sort=rating 的 orderBy 是 `[{ ratingSum: 'desc' }, { ratingCount: 'desc' }]`，按评分总和排序而非平均值。同时响应字段把 avg_score 暴露给前端。ratingSum 天然偏向评分人数多的插件：100 条均 4.9 分（ratingSum=490）会排在只有 1 条 5 分（ratingSum=5）之前，与用户对“评分排序=按平均分排”的预期相反。",
    "evidence": "line 24-25 `sort === 'rating' ? [{ ratingSum: 'desc' }, { ratingCount: 'desc' }]`；而 avg_score(line 41) = ratingSum/ratingCount。例：插件A ratingSum=490/ratingCount=100(avg4.9) 排在 插件B ratingSum=5/ratingCount=1(avg5.0) 之前。",
    "trigger": "市场存在多个评分数量差异较大的 APPROVED 插件时，访问 GET /api/marketplace/search?sort=rating。",
    "impact": "市场排序与展示语义不一致，高分小众插件被埋没，热门但中低分插件占据顶部，影响插件发现与作者曝光。",
    "fix": "证据链已闭合：marketplace.service.ts:24-25 用 ratingSum 排序、:41 用 ratingSum/ratingCount 算 avg_score、Market.tsx:108 前端展示 avg_score——三者构成可见的语义不一致。修复方向（交实现者裁定）：方案A 直接按 avg_score 排序（最贴近「按评分」直觉，但小样本会冒头）；方案B 引入贝叶斯/下限加权评分（如 IMDb 式，平衡评分人数与均值，避免「1 条 5 分」占据顶部）。若团队确属有意采用加权策略，应在 design.md 显式记录为 accepted-risk 并给出公式，同时让 UI 展示值与排序键一致。",
    "reasoning": "亲自核对代码：apps/collab-api/src/modules/marketplace.service.ts:23-28 确认 sort==='rating' 的 orderBy 为 [{ ratingSum: 'desc' }, { ratingCount: 'desc' }]，按评分总和排序；而 :41 avg_score = ratingSum/ratingCount（当 ratingCount>0）按平均分。两套语义并存且分别用于排序/展示。\n\n触发场景验证（数学闭合）：插件A 100条均4.9分 → ratingSum=490、ratingCount=100、avg_score=4.9；插件B 1条5分 → ratingSum=5、ratingCount=1、avg_score=5.0。sort=rating 按 ratingSum desc 排序时 A(490) 排在 B(5) 之前，即 avg 4.9 排在 avg 5.0 之前——与用户对「按评分排序=按平均分」的预期相反。前端 apps/desktop/src/pages/Market.tsx:108 列表项展示 {p.avg_score} ({p.rating_count})，但排序键是 ratingSum，造成用户可见的「高分插件埋没」反直觉。\n\n对抗性核验「是否为有意加权」：即便按加权评分（IMDb 贝叶斯平均式）解释，ratingSum 仍不成立——100条均3分(ratingSum=300)会排在1条5分(ratingSum=5)前，违反任何合理定义的「按评分排序」。故非合理加权策略。\n\n文档取舍核查：清单中无 rating 排序算法项；design.md:80 仅列举 installs/rating/recent 三键未规定算法，:115 提及「内存算 avg/count」描述的是 avg_score 字段计算而非排序；无 explicit-skip/accepted-risk 标注 ratingSum 排序为有意取舍。故属清单之外的真实代码问题。\n\n严重度：维持 medium。这是排序展示与字段语义不一致的正确性缺陷（契约不一致），但不导致数据损坏/资金损失/鉴权绕过/功能失效，用户仍可浏览全部 50 条结果，有 workaround。不降到 low（不是健壮性细节，是真实正确性偏差），不升到 high（不影响交易、状态机、数据一致性）。"
  },
  {
    "id": "MKT-05",
    "title": "install 每次 upsert 都 increment installCount，重复/重装导致计数虚高",
    "severity": "medium",
    "category": "data-consistency",
    "target": "A4-marketplace",
    "location": "apps/collab-api/src/modules/marketplace.service.ts:99-106",
    "description": "marketplace.service.install 用 `pluginInstallation.upsert(update:{status:'ENABLED'}) + plugin.update(installCount: increment 1)` 包在同一数组事务里，无论 upsert 走 update 分支（已存在安装记录，如曾安装后被置 DISABLED 又重装）还是 create 分支，installCount 都无条件 +1。这与 plugin.service.installMarketplacePlugin（line 137-144 仅在真正 create 时 increment）语义不一致。",
    "evidence": "marketplace.service.ts:99-106 事务内 upsert 与 plugin.update({installCount:{increment:1}}) 平级、无条件执行；plugin.service.ts:137-144 `existing ? update : $transaction(create + increment)`。",
    "trigger": "同一团队对同一市场插件多次调用 POST /api/marketplace/install（卸载→重装，或多成员各装一次）。",
    "impact": "install_count 与 PluginInstallation 明细行数长期脱钩，sort=installs 与展示的 install_count 失真；同一团队多成员或重复重装会持续抬高计数。",
    "fix": "建议修复：与 plugin.service.ts:137-144 对齐，先 findUnique 判断 existing，仅 create 分支 increment；或用 upsert 的 update 分支里加条件 increment（需先读取是否已存在，因为 update 分支无法在事务数组里条件性跳过）。两个安装端点的 installCount 维护逻辑应统一为单一可复用方法，避免再次分化。",
    "reasoning": "亲自核实确认该 bug 真实存在。apps/collab-api/src/modules/marketplace.service.ts:99-106 将 `pluginInstallation.upsert` 与 `plugin.update({ installCount: { increment: 1 } })` 放进同一个 `$transaction([...])` 数组，increment 无条件执行——无论 upsert 实际走 create 还是 update 分支都会 +1。对照 plugin.service.ts:137-144 的并行实现 `installMarketplacePlugin`：先 `findUnique` 查 existing，仅在 `existing` 为空时才在 create 分支 increment；update 分支不动 installCount。两端点做同一逻辑操作（安装公共市场插件到当前团队）但 installCount 语义不同，证明非常意图。schema.prisma:230 `@@unique([pluginId, teamId])` 确认每个 (pluginId, teamId) 至多一行 PluginInstallation。触发场景具体可构造：同一团队对同一 pluginId 调用两次 `POST /api/marketplace/install`：第1次 upsert→create + installCount+1=1；第2次 upsert→update（status 重置为 ENABLED、version 不变）+ installCount+1=2；结果：明细 1 行但 installCount=2，发生脱钩。比原 finding 触发面更广——甚至无需任何 uninstall/DISABLED 动作，仅同一团队第二次请求即触发；团队内两成员各装一次同样如此。grep 全仓 `uninstall|DISABLED|PluginInstallation` 未发现任何端点把 PluginInstallation 置为 DISABLED，但这不削弱该触发路径。installCount 直接驱动默认排序 `[{ installCount: 'desc' }]`（marketplace.service.ts:28）并作为 install_count 暴露在 search(line 38)/detail(line 74)，故虚高计数直接扭曲热度排序与展示。"
  },
  {
    "id": "MKT-06",
    "title": "score 为小数（如 3.5）通过 1-5 校验后被 Prisma Int 列拒绝，返回 500 而非 400",
    "severity": "low",
    "category": "error-handling",
    "target": "A4-marketplace",
    "location": "apps/collab-api/src/modules/marketplace.service.ts:111-112; apps/collab-api/prisma/schema.prisma:306",
    "description": "rate 校验 `if (!(score >= 1 && score <= 5))` 对 3.5/4.2 等非整数值放行（落在 1-5 区间），但 PluginRating.score 在 schema.prisma:306 为 Int，Prisma 会以 PrismaClientValidationError 拒绝非整数。该错误非 AppError/HttpException，AppExceptionFilter 兜底成 500 internal_error 而非 400。",
    "evidence": "marketplace.service.ts:112 `if (!(score >= 1 && score <= 5)) throw badRequest('评分须为 1-5');`；schema.prisma:306 `score Int`；common.ts:85 兜底 500。",
    "trigger": "POST /api/marketplace/rate 传入 score: 3.5 或 4.2 等非整数值。",
    "impact": "前端送入小数评分得到误导性的 500，错误码与提示不符合契约；前端难以据此给用户清晰反馈。",
    "fix": "修复方向：将 marketplace.service.ts:112 的校验改为整数判定，例如 `if (!Number.isInteger(score) || score < 1 || score > 5) throw badRequest('评分须为 1-5 的整数');`。或在 controller 层引入 DTO+ValidationPipe(@IsInt,@Min(1),@Max(5))——但项目当前未启用 class-validator/ValidationPipe，引入需全局评估。最小代价是 service 内 Number.isInteger 判定，与既有 badRequest 契约一致，且无需新依赖。注意 admin.service.ts:44 注释表明团队已意识到 PrismaClientValidationError 风险并主动规避，此处 rate 同属一类问题但未规避。",
    "reasoning": "独立重读全部四处证据，逻辑链完整成立。marketplace.controller.ts:34 调 `Number(body.score)`，无全局 ValidationPipe（app.module.ts:9-17 仅有 APP_GUARD，无 useGlobalPipes；全仓 grep 无 class-validator/IsInt），故 JSON 原始浮点数 3.5 原样透传。marketplace.service.ts:112 守卫 `if (!(score >= 1 && score <= 5))` 对 3.5 为 `!(true&&true)=false` 放行。schema.prisma:306 `score Int`，Prisma 对 Int 字段在客户端校验阶段拒绝浮点数，抛 PrismaClientValidationError（非 AppError、非 HttpException），common.ts:85-89 兜底成 500 internal_error。对照相邻分支验证不一致确实存在：score=6/NaN 走 400 正确，score=3.5/4.2 走 500。文档取舍清单无任何条目覆盖评分端点或数值评分契约（清单涉及市场上传约束/状态机/Node-Python 沙箱等，与此无关），非文档取舍。触发场景具体可达：POST /api/marketplace/rate body `{\"plugin_id\":\"<已购买或已安装的市场插件>\",\"score\":3.5}`。影响为误导性错误码与契约不符（应为 400 bad_request，实为 500 internal_error），无数据损坏/资金损失/鉴权绕过。严重度 low 准确：错误处理契约不一致、前端可简单规避（只送整数）。"
  },
  {
    "id": "PLUGIN-02",
    "title": "marketplace.install 每次（含重复安装/重启用）都 +1 installCount，聚合指标失真",
    "severity": "high",
    "category": "data-consistency",
    "target": "A5-plugin-service",
    "location": "apps/collab-api/src/modules/marketplace.service.ts:99-106",
    "description": "marketplace.install 使用 prisma.$transaction([ upsert(installation), plugin.update({installCount:{increment:1}}) ])。无论 upsert 是 create 还是 update（即无论是否首次安装），installCount 都无条件 +1。且无幂等短路（对比 PluginService.installMarketplacePlugin 在已存在 installation 时走 update 且不 increment）。",
    "evidence": "marketplace.service.ts:99-106 `this.prisma.pluginInstallation.upsert({ where:{pluginId_teamId:{pluginId,teamId}}, update:{...}, create:{...} }), this.prisma.plugin.update({ where:{id:pluginId}, data:{ installCount:{increment:1} } })`。对照 plugin.service.ts:137-144 `const existing = await findUnique(...); const installation = existing ? update(不增 installCount) : $transaction(create + increment)`。",
    "trigger": "同一团队对同一已安装插件重复 POST /api/marketplace/install；或并发调用。",
    "impact": "installCount 聚合与真实安装明细脱钩，市场排序（默认按 installCount desc，marketplace.service.ts:28）失真；两套安装路由对同一指标语义不一致，审计/运营数据不可信。",
    "fix": "修复方向（供后续参考，非本次必做）：在 marketplace.install 内先按 {pluginId, teamId} findUnique 检查是否已存在 installation，仅当不存在时进入 $transaction(create + increment)，存在时走 update 不增计数，与 plugin.service.ts:131-147 保持一致。若还需防并发竞态（两请求同时查到不存在），可在 transaction 内用 SELECT ... FOR UPDATE 或依赖 pluginId_teamId 唯一约束 + 捕获唯一键冲突后不再 increment。两路由对同一聚合指标最好收敛到单一 helper，避免再次出现语义分裂。",
    "reasoning": "亲自核实代码无误。marketplace.service.ts:99-106 用 prisma.$transaction([ upsert(installation), plugin.update({installCount:{increment:1}}) ])，数组形式事务保证原子性但不保证幂等；plugin.update 的 increment 无条件执行，不区分 upsert 命中 create 还是 update。对照 plugin.service.ts:137-144 用 findUnique 后分支：existing 走 update 不增计数、不存在才 $transaction(create + increment)，幂等。schema.prisma:230 @@unique([pluginId, teamId]) 保证同一(插件,团队)只一行 installation，因此重复 /api/marketplace/install 必走 update 分支却仍 +1。marketplace.service.ts:28 默认排序 installCount desc，证明该聚合字段直接驱动可见市场排序。触发场景：同一团队重复 POST /api/marketplace/install 同一插件（或反复点击安装），单线程即可复现，无需并发。影响：installCount 与真实去重安装明细脱钩；市场默认排序失真；且可被任意有团队成员资格的用户通过反复调用对心仪公共插件刷量抬排名（rank 可被人为抬高，构成可利用的正确性缺陷）；两条面向用户的安装路由对同一聚合指标语义不一致。非文档取舍：清单无任何条目涉及 installCount 语义/市场安装幂等/排名可刷量，最接近的「市场审核状态机」「上传约束」「数据隔离」均不覆盖，原审查者 docTradeoffRef=(无) 成立。严重度维持 high 边界：虽然属数据一致性偏差而非崩溃/资金损失，但因 installCount 是默认可见排序键、且可被普通用户重复点击刷量抬排名、且两套安装路由语义分裂，符合 high「正确性 bug / 聚合与明细脱钩」定义；若仅视为不可利用的指标偏差则可降到 medium，但此处存在可被普通用户利用的排名刷量路径，故维持 high。"
  },
  {
    "id": "PLUGIN-03",
    "title": "marketplace.install 与 installMarketplacePlugin 并发安装可重复计数 installCount",
    "severity": "medium",
    "category": "logic",
    "target": "A5-plugin-service",
    "location": "apps/collab-api/src/modules/marketplace.service.ts:99-106",
    "description": "marketplace.install 把 installation upsert 与 installCount increment 放在 batch transaction 里。两个并发请求都走 create 分支或一个 create 一个 update，increment 都会执行一次，导致单次逻辑安装被计 2 次。无任何条件 increment（如 updateMany where 未安装）。",
    "evidence": "marketplace.service.ts:99-106 无条件 increment；与 PluginService.installMarketplacePlugin 的 existing 短路写法不同，后者并发下也有风险但至少做了 existing 检查（仍有 TOCTOU 窗口）。两处都没用 updateMany 条件 increment 或 SELECT FOR UPDATE。",
    "trigger": "同团队两个请求几乎同时 POST /api/marketplace/install 同一插件。",
    "impact": "计数偏高，市场热度指标失真，进而影响默认排序与其他依赖 installCount 的判断。",
    "fix": "修复方向：参照 plugin.service.ts:131-147 的正确写法，把 marketplace.install 改为先 findUnique 判 existing，仅在 create 分支才 increment installCount（保持单事务）。若想彻底消除并发 TOCTOU 窗口，两处都应改成条件 increment（如 updateMany where 未安装的 installation）或对 pluginInstallation 加 SELECT FOR UPDATE / 唯一约束已存在可依赖 upsert 但需区分 create 与 update——Prisma upsert 当前不返回走了哪个分支，故 findUnique 前置判定是更稳妥的修法。此外建议统一两个端点的实现（DRY），消除契约不一致。",
    "reasoning": "核实 marketplace.service.ts:99-106：$transaction 内 pluginInstallation.upsert 的 update 与 create 两个分支，配合 plugin.update 的 installCount:{increment:1}，increment 是无条件的——无论 installation 是新建还是已存在都会 +1。对照 plugin.service.ts:131-147 的 installMarketplacePlugin：用 findUnique 判 existing，仅在 create 分支（新建）才 increment installCount，update 分支不 increment。两处都是\"安装市场插件到当前团队\"的同一业务动作（marketplace.controller.ts:25-29 POST /marketplace/install 与 plugins.controller.ts:43-47 POST /plugins/:id/install），实现不一致。\\n\\n触发场景比原描述更宽松：不需要并发，串行重复安装就触发——同团队对已安装插件再次 POST /api/marketplace/install，upsert 走 update 分支，installCount 仍 +1。并发场景进一步放大为 +2（两请求都未见 installation → 一个 create 一个 update，两者都执行 increment）。无任何条件 increment（如 updateMany where 未安装）或 SELECT FOR UPDATE。\\n\\n影响：installCount 是 search 的默认排序键（marketplace.service.ts:28 orderBy 默认 installCount desc），热度指标失真并影响默认排序。两处实现差异本身也是契约/一致性缺陷。\\n\\n文档取舍核对：清单中无任何关于 installCount 计数精确性的取舍；状态机/上传约束/付费校验等均不覆盖此点。属清单之外的真实代码问题。\\n\\n严重度：维持 medium——仅影响统计指标与默认排序，不破坏核心数据完整性、无资金损失、有 workaround。\\n\\ncategory 修正为 logic（根因是无条件 increment 的逻辑错误，对比同库 installMarketplacePlugin 的正确实现说明是疏忽；并发只是放大场景而非机制本身）。"
  },
  {
    "id": "PLUGIN-04",
    "title": "editPluginDraft 可将已审核 APPROVED+PUBLIC 插件静默重置为 DRAFT+marketplace:false（绕过审核下架）",
    "severity": "medium",
    "category": "state-machine",
    "target": "A5-plugin-service",
    "location": "apps/collab-api/src/modules/plugin.service.ts:94-129",
    "description": "editPluginDraft 只挡 reviewStatus===PENDING（line 99），对 reviewStatus===APPROVED 的已上架插件无任何防护。编辑后强制写回 reviewStatus:'DRAFT', marketplace:false（line 120-124），把一个已通过平台审核并 PUBLIC 上架（可能已被其他团队购买/安装）的插件直接下架，且不写任何审批/下架审计理由。同理 submitPluginToMarketplace 对 APPROVED 状态也只挡 PENDING（line 79），可把已上架插件重置回 PENDING。",
    "evidence": "plugin.service.ts:99 `if (plugin.reviewStatus === 'PENDING') throw conflict(...)`；plugin.service.ts:107-126 update data 含 `reviewStatus:'DRAFT', marketplace:false`；对比 adminApprovePlugin (admin.service.ts:174-182) 才是唯一写入 APPROVED+marketplace:true+visibility:PUBLIC 的入口。状态机文档要求 DRAFT/REJECTED -> PENDING -> APPROVED/PUBLIC，但代码允许 APPROVED -> DRAFT 的非管理员转移。",
    "trigger": "作者对一个 reviewStatus=APPROVED/marketplace=true/visibility=PUBLIC 的自有插件调用 POST /api/plugins/:id/edit-draft。",
    "impact": "作者可单方面把已审核上架的付费插件下架，已购买用户在 detail/install 流程立即 notFound（marketplace.service.ts:48-52 require marketplace&&APPROVED）；已发生的 Purchase 与 PluginInstallation 成为孤儿。无平台审批留痕。",
    "fix": "修复方向（仅审查建议，未改动代码）：editPluginDraft 与 submitPluginToMarketplace 在 reviewStatus==='PENDING' 守卫之外，应额外拦截 reviewStatus==='APPROVED'（已上架插件不允许非管理员直接编辑/重提，需走 adminRejectPlugin 或新增带审批留痕的下架/发版流程）；同时若确需支持作者下架已上架插件，应新增显式状态（如 ARCHIVED）并写 marketplace_unpublished 审计记录，避免把 Purchase/PluginInstallation 变为孤儿并切断付费购买者的市场访问。",
    "reasoning": "独立核实成立。plugin.service.ts:99 editPluginDraft 仅守卫 reviewStatus==='PENDING'，未守卫 APPROVED；plugin.service.ts:107-126 无条件写回 reviewStatus:'DRAFT'、marketplace:false、reviewedById:null、reviewedAt:null。作者对自有 APPROVED+marketplace:true+visibility:PUBLIC 插件调用 POST /api/plugins/:id/edit-draft（plugins.controller.ts:37-41，受 ensurePluginManager 通过）即可触发：PENDING 守卫不命中 → update 把插件从已上架状态重置回 DRAFT+marketplace:false。后果：marketplace.service.ts:48-52（detail）与 87-90（install）因 require marketplace&&APPROVED 立即返回 notFound，已购买/已安装用户在市场访问被切断；Purchase(schema.prisma:285)/PluginInstallation(219) 记录成孤儿。无下架审计：仅写 plugin.draft.edited 审计，对比 admin.service.ts:169-184 才是合法 APPROVED 入口（带 PluginReview + admin.plugin.approved 审计）。submitPluginToMarketplace 同病（plugin.service.ts:79 仅挡 PENDING，APPROVED→PENDING 也会让 search/detail/install 失效）。触发场景具体、影响真实且跨用户。文档状态机 docs/collab-api.md:121 仅允许 DRAFT/REJECTED→PENDING→APPROVED/PUBLIC 或 REJECTED/TEAM，无 APPROVED→DRAFT；docs/collab-api.md:69 明确 edit-draft 仅用于「未审核中的插件草稿」。文档取舍清单中无「作者可自由下架/重提已审核插件」条目，故非取舍。严重度维持 medium：非鉴权绕过（作者拥有该插件）但确为状态机正确性 bug 且影响已购买用户的付费内容访问。"
  },
  {
    "id": "PLUGIN-05",
    "title": "uploadPlugin 并发同内容上传时去重 TOCTOU 失败抛 500 而非优雅去重",
    "severity": "medium",
    "category": "error-handling",
    "target": "A5-plugin-service",
    "location": "apps/collab-api/src/modules/plugin.service.ts:19-24",
    "description": "uploadPlugin 先 findUnique 查 (teamId, contentHash)，命中则返回 existing（line 19-22），未命中才 create（line 24-42）。schema 有 @@unique([teamId, contentHash])（schema.prisma:212）。两个并发同内容上传：都查到 null，都进入 create，第二个触发 Prisma P2002 唯一约束冲突。AppExceptionFilter 不识别 P2002，落到 catch-all 500 internal_error（common.ts:85-89），而非 conflict 409。",
    "evidence": "plugin.service.ts:19-22 findUnique 命中直接 return；plugin.service.ts:24 create 无 try/catch、无 upsert；schema.prisma:212 `@@unique([teamId, contentHash])`；common.ts:85-89 非 AppError/HttpException 一律 500。editPluginDraft（line 102-105）有显式冲突检查但同样无 create 时的竞态保护（不过 edit 是 update 非 create，约束冲突概率低）。",
    "trigger": "同一用户/同团队两客户端几乎同时上传完全相同内容（同 contentHash）的插件包。",
    "impact": "客户端收到误导性的 500 而非 409 deduplicated:true；上传重试体验差；错误码与契约不符。非数据损坏，仅错误处理不当。",
    "fix": "建议修复方向（非取舍）：在 uploadPlugin 的 create 处用 try/catch 捕获 PrismaClientKnownRequestError 并判别 error.code === 'P2002'，命中则重新 findUnique 返回 deduplicated:true（409 或 200 均可，但需与现有 deduplicated 契约语义一致）；或直接用 prisma.plugin.upsert。同样的 TOCTOU 在 installMarketplacePlugin 的 pluginInstallation 上也存在（plugin.service.ts:137-144 先 findUnique 后 create，依赖 @@unique([pluginId, teamId])），并发安装时第二个请求会触发 P2002 被吞为 500，且 installCount 在两并发事务里 increment 各自独立事务仍可能多算（后者属更隐蔽的计数竞态，本 finding 未覆盖）。审查范围可顺带核查 pluginInstallation 链路。",
    "reasoning": "已逐行核实。apps/collab-api/src/modules/plugin.service.ts:19-22 uploadPlugin 先 prisma.plugin.findUnique({ where: { teamId_contentHash } }) 命中则 return { deduplicated: true }；plugin.service.ts:24-42 命中 null 后直接 prisma.plugin.create(...)，无 try/catch、无 upsert、无 P2002 处理。apps/collab-api/prisma/schema.prisma:212 确有 `@@unique([teamId, contentHash])` 数据库级唯一约束。apps/collab-api/src/common.ts:55-90 AppExceptionFilter 仅识别 AppError(63行) 与 HttpException(73行)，其余一律落入 85-89 行 500 internal_error；全文 grep 未发现 PrismaClientKnownRequestError / P2002 映射，main.ts:13 注册的 AppExceptionFilter 是唯一全局过滤器。触发时序成立：两并发请求同 (teamId,contentHash) 各自 findUnique 都得 null → 都 create → 第二个被 DB 唯一约束拒抛 P2002 → 过滤器输出 500 而非契约 409/deduplicated:true。文档清单未将此列为取舍，反而在「插件上传约束」声明『团队内相同 contentHash 自动去重』为 design-decision，代码并发下直接违背该去重意图。DB 约束保护了一致性（最终仍一条记录、无资金/鉴权/数据损坏），故属错误处理/契约偏差，medium 准确。"
  },
  {
    "id": "PLUGIN-06",
    "title": "submitPluginToMarketplace 的 priceCents 无法置 0（免费化）",
    "severity": "low",
    "category": "logic-bug",
    "target": "A5-plugin-service",
    "location": "apps/collab-api/src/modules/plugin.service.ts:81",
    "description": "priceCents 默认值表达式 `Math.max(0, Number(input.priceCents || plugin.priceCents || 0))`。当客户端显式传 priceCents:0（想要免费上架）时，`0 || plugin.priceCents` 因 0 为 falsy 而回退到 plugin.priceCents，无法把已定价插件改为免费。",
    "evidence": "plugin.service.ts:81 `const priceCents = Math.max(0, Number(input.priceCents || plugin.priceCents || 0));`。对照 uploadPlugin（line 40）与 editPluginDraft 走 normalize 路径，submit 是唯一直接解析 priceCents 的入口。",
    "trigger": "对一个 priceCents=500 的已存在插件调用 submit-marketplace 传 body.priceCents=0。",
    "impact": "作者无法通过 submit-marketplace 把付费插件改为免费（必须先 edit-draft 改 priceCents 再 submit，或找平台管理员 adminUpdatePlugin）。功能性缺陷，非数据损坏。",
    "fix": "证据汇总：plugin.service.ts:81 唯一直解 priceCents 的非 admin 入口；同文件 editPluginDraft(line 107-126) data 对象不含 priceCents（自助降免费不可行）；admin.service.ts:217 是正确写法参照；economy.service.ts:58/marketplace.service.ts:40,76 证明 priceCents=0 即免费。无文档取舍覆盖。结论：真实 low 级逻辑缺陷。",
    "reasoning": "亲自核实 apps/collab-api/src/modules/plugin.service.ts:81 确为 `const priceCents = Math.max(0, Number(input.priceCents || plugin.priceCents || 0));`。控制器 plugins.controller.ts:33-35 直接把 body 透传给 submitPluginToMarketplace，input.priceCents 就是客户端 body.priceCents。\n\n触发链验证：已存在 plugin.priceCents=500（uploadPlugin 在 plugin.service.ts:40 `Math.max(0, Number(input.priceCents || 0))` 可写入非零价）。作者调用 POST /api/plugins/:id/submit-marketplace body={priceCents:0}：\n- input.priceCents=0 为 falsy\n- 0 || plugin.priceCents = 0 || 500 = 500\n- Math.max(0, Number(500)) = 500\n结果 priceCents 仍为 500，无法置 0（免费化）。第 85 行 update data.priceCents 写入此错误值。\n\npriceCents===0 是系统内有意义的状态：economy.service.ts:58 `if (plugin.priceCents <= 0) throw badRequest('免费插件无需购买，可直接安装')`；marketplace.service.ts:40,76 `is_free: p.priceCents === 0`。所以「免费上架」是真实预期功能。\n\n对照正确写法：admin.service.ts:217 `priceCents: input.priceCents === undefined ? undefined : Math.max(0, Number(input.priceCents))` 正确区分 undefined 与 0。submit 路径是唯一漏掉此判别的入口，uploadPlugin line 40 因回退值本身是 0（目标态）不暴露此问题。\n\n重新提交路径真实存在：reviewStatus 状态机 DRAFT/REJECTED -> PENDING，被拒后可再次 submit 改价；同时 uploadPlugin 已存的插件也走 submit-marketplace 上架。两类场景都触发。\n\n文档取舍清单核对：docs/collab-api.md:68 仅把 submit-marketplace 描述为「提交公共市场审核」，docs:111 示例 priceCents:0 即免费为有效值；清单中无任何条目将「不能通过 submit 把 priceCents 改 0」列为取舍/限制。属清单之外的真实代码缺陷。\n\n影响修正：原报告称可「先 edit-draft 改 priceCents 再 submit」作 workaround——但 editPluginDraft（plugin.service.ts:107-126）的 update data 对象根本不写 priceCents，自助改不了价格；唯一自助可见的修正是通过 adminUpdatePlugin（平台管理员）。因此作者无自助路径降免费，比原报告更严重一点，但仍是功能性缺陷（非数据损坏/资金损失/安全），维持 low/medium。综合判定 low。"
  },
  {
    "id": "PLUGIN-07",
    "title": "uploadPlugin 去重返回团队内他人创建的同内容插件，且无审计记录",
    "severity": "low",
    "category": "data-consistency",
    "target": "A5-plugin-service",
    "location": "apps/collab-api/src/modules/plugin.service.ts:19-22",
    "description": "uploadPlugin 命中同 (teamId, contentHash) 时直接 return existing（line 22），既不校验 existing.authorUserId 是否等于当前 userId，也不写任何审计。文档约束『团队内相同 contentHash 自动去重』本身是 design-decision（取舍清单），但实现忽略了去重返回的插件可能属于团队内另一成员——后续该用户对返回的 pluginId 调 editPluginDraft/submitMarketplace 会被 ensurePluginManager 拒绝（plugin-package.ts:213-216 authorUserId!==userId && role!==TEAM_ADMIN 抛 forbidden）。",
    "evidence": "plugin.service.ts:19-22 `if (existing) return { plugin: publicPlugin(existing, membership.teamId), deduplicated: true };` 无 ensurePluginManager、无 audit；plugin-package.ts:213-216 ensurePluginManager 校验 authorUserId。",
    "trigger": "团队内成员 B 上传与成员 A 已有插件字节完全相同（含 name/version）的包。",
    "impact": "返回一个调用者无权编辑的 pluginId，客户端若据此进入编辑/提交流程会 403；上传审计缺失使该次去重不可追溯。非安全漏洞（数据未泄漏给其他团队），属体验/审计一致性缺陷。",
    "fix": "修复方向（仅供参考，本次只读审查未改动）：在 plugin.service.ts:22 的 dedup 命中分支前补充 authorUserId 判断，若 existing.authorUserId !== userId 且 role !== 'TEAM_ADMIN'，可选其一：(a) 视为 B 自己的上传创建新记录（但 contentHash 唯一约束会冲突，需放宽唯一约束到含 authorUserId，与 docs/collab-api.md:120『团队内去重』语义冲突，不推荐）；(b) 直接抛 conflict 并提示『团队内已有相同内容插件（由他成员创建），请联系团队管理员』；(c) 命中 dedup 时仍写一条 audit action='plugin.upload.deduplicated'（包含 originalAuthorUserId）以补全审计链。当前实现的根因是 dedup 路径复用了『返回 existing』但未考虑 ensurePluginManager 契约与审计写入，与 line 43 正常创建路径的 audit 调用不对称。",
    "reasoning": "核实代码与触发链均成立。\n\n1) 代码确认（apps/collab-api/src/modules/plugin.service.ts:19-22）：\n   const existing = await this.prisma.plugin.findUnique({ where: { teamId_contentHash: { teamId: membership.teamId, contentHash: normalized.contentHash } } });\n   if (existing) return { plugin: publicPlugin(existing, membership.teamId), deduplicated: true };\n   dedup 分支无 ensurePluginManager、无 auditLog.create 调用，与正常创建分支（line 43 写 audit）不对称。\n\n2) contentHash 不含 authorUserId（plugin-package.ts:143）：createHash('sha256').update(JSON.stringify({ manifest: normalizedManifest, files }))。因此同团队两成员上传字节相同包产生相同 hash，必触发 dedup。\n\n3) ensurePluginManager 契约（plugin-package.ts:213-216）：plugin.authorUserId !== userId && role !== 'TEAM_ADMIN' 即抛 forbidden。submitPluginToMarketplace（plugin.service.ts:77）与 editPluginDraft（plugin.service.ts:98）均调用它。\n\n4) 触发时序（具体构造）：团队 T 内成员 A 上传包→创建插件 P（authorUserId=A）；成员 B（普通 MEMBER）上传字节完全相同的包→contentHash 相同→findUnique 命中 P→uploadPlugin 返回 {plugin:P, deduplicated:true}；前端 PluginCreatorHome.tsx:751-758 将 P 设为 cloudPlugin 并 toast『团队云端已有相同插件』；B 若随后点击『提交市场』（PluginCreatorHome.tsx:769-773 调 /api/plugins/P/submit-marketplace）→submitPluginToMarketplace(user=B, id=P)→ensurePluginManager 抛 forbidden 403。editPluginDraft 同理 403。故『返回一个调用者无权编辑/提交的 pluginId』属实。\n\n5) 审计缺失确认：dedup 分支无任何 auditLog.create，正常创建分支 line 43 写 audit；dedup 这一路上传动作不可追溯。\n\n6) 文档取舍边界判定：docs/collab-api.md:120『团队内相同 contentHash 自动去重并返回已有插件』属 design-decision（清单已列）。但文档只规定『返回已有插件』，并未规定『忽略作者归属返回跨成员拥有的 pluginId』『去重路径省略审计』。这两个实现细节在取舍之外——取舍的是『去重』这一行为，不是『返回一个调用者无操作权的 pluginId 且不写审计』。故非文档取舍，属真实实现缺陷。\n\n影响修正：返回的 pluginId 本身对 B 并非泄露（P 在 availablePlugins 中因 teamId+visibility=TEAM 已对 B 可见，plugin-package.ts:62），核心问题是返回了 B 无法 edit/submit 的死路 pluginId（导致 403 体验断裂）+ dedup 路径审计缺失。无数据损坏、无跨团队泄露、无鉴权绕过、无状态机破坏。原评 low 准确。"
  },
  {
    "id": "PLUGIN-08",
    "title": "availablePlugins 第 4 分支展示已被驳回/下架但仍存在 ENABLED 安装记录的插件",
    "severity": "low",
    "category": "state-machine",
    "target": "A5-plugin-service",
    "location": "apps/collab-api/src/modules/plugin.service.ts:56-71",
    "description": "availablePlugins 的 OR 第 4 分支 `{ installations: { some: { teamId, status:'ENABLED' } } }` 只看是否存在 ENABLED 安装记录，不再校验 plugin 当前的 marketplace/reviewStatus/visibility。一个曾被 APPROVED+PUBLIC 被安装、后被 admin reject（reviewStatus=REJECTED, marketplace=false, visibility=TEAM，admin.service.ts:193-196）或被作者 edit-draft 重置的插件，仍会出现在曾经安装过它的团队的可用列表里。",
    "evidence": "plugin.service.ts:65 `{ installations: { some: { teamId: membership.teamId, status: 'ENABLED' } } }` 无 marketplace/reviewStatus/visibility 限定；顶层仅 `status:'ENABLED'`（line 60）挡不住被驳回但仍 ENABLED 的插件。",
    "trigger": "团队曾安装某 PUBLIC 插件，随后该插件被平台 reject 或作者 edit-draft 重置。",
    "impact": "用户看到本应已下架/驳回的插件条目；点进 marketplace.detail 会 404（marketplace.service.ts:48-52），但 plugins.available 仍展示，体验不一致。非越权泄漏（仅本团队已安装过的），影响有限。",
    "fix": "修复建议（仅供裁定参考，未实施）：第4分支应增加与市场可见性等价的生命周期校验，例如 `{ installations: { some: { teamId, status:'ENABLED' } }, marketplace:true, reviewStatus:'APPROVED', visibility:'PUBLIC' }`，使「下架/驳回后不再展示」与 marketplace.detail 的 404 行为一致；或若设计意图是「已安装即保留」，则应同时让 marketplace.detail 对已安装团队返回详情（带下架标记）而非 404，二者需统一。当前两侧语义矛盾，是判定为真缺陷的核心依据。",
    "reasoning": "重新独立读代码确认第4分支确如描述。apps/collab-api/src/modules/plugin.service.ts:56-71 的 WHERE 为 `status:'ENABLED' AND (branch1 OR ... OR branch4)`，第4分支 line 65 `{ installations: { some: { teamId: membership.teamId, status: 'ENABLED' } } }` 仅校验是否存在 ENABLED 安装记录，未再校验 plugin 当前的 marketplace/reviewStatus/visibility。\n\n触发场景可构造（两条路径均已核实可达）：\n- 场景B（edit-draft 重置，最直接）：作者团队A 创建→submit→approve（line 177 把 visibility 设 PUBLIC、marketplace=true，但 teamId 仍=teamA）。团队B 经 /api/plugins/:id/install（plugin.service.ts:131）或 /api/marketplace/install（marketplace.service.ts:86）安装，留下 PluginInstallation(pluginId=P, teamId=teamB, status=ENABLED)。随后作者 editPluginDraft（plugin.service.ts:107-126）：reviewStatus→DRAFT、marketplace→false、visibility→TEAM/PRIVATE（normalizePluginPackage 只允许 private|tenant，不可能再是 PUBLIC）。此时团队B availablePlugins 仍命中 branch4 返回该插件，因为 Plugin.status 仍是 ENABLED（editPluginDraft 不改 status），而顶层只挡 ENABLED。\n- 场景A（re-submit→reject）：approve 后作者可再次 submitPluginToMarketplace（line 79 只挡 PENDING，APPROVED 可重入→PENDING），adminRejectPlugin（admin.service.ts:192-196）设 reviewStatus=REJECTED、marketplace=false、visibility=TEAM。团队B 同样仍能经 branch4 看到该插件。注意原描述「approve 后直接 reject」不可达（adminRejectPlugin line 190 要求 reviewStatus===PENDING），但 re-submit 后 reject 等价场景可达。\n\n全仓 grep 确认无任何代码会把 PluginInstallation.status 改成 DISABLED（仅 user/invitationCode 出现 DISABLED），也无 uninstall 接口；adminRejectPlugin/editPluginDraft 都不动 Plugin.status，所以顶层 status:'ENABLED' 挡不住。\n\n影响确证：marketplace.detail（marketplace.service.ts:48-52）要求 marketplace:true AND reviewStatus:APPROVED AND status:ENABLED，被驳回/重置后这些字段已失配，detail 抛 notFound(404)。而 available 仍展示该插件。两侧语义矛盾，构成真实的契约不一致/陈旧展示。\n\n非越权泄漏：仅本团队已安装过的插件可见（branch4 限定 teamId: membership.teamId）；被驳回后 reviewReason 等字段经 publicPlugin 仍会回显给曾安装团队，但这是该团队本就持有的数据。无资金/数据损坏/鉴权问题。\n\n严重度评估：属一致性/陈旧展示缺陷，影响面小（已安装团队仍可本地运行该插件，publicPlugin 内联 files），但「available 列出 / marketplace.detail 404」是明确的体验不一致，且第4分支缺生命周期校验确实是状态机实现偏差，low 合适（不到 medium：无 workaround 受损、无核心功能失效，仅有边界瑕疵）。"
  },
  {
    "id": "PLUGIN-09",
    "title": "adminUpdatePlugin 对不存在的 plugin id 抛 500（Prisma P2025 未捕获）",
    "severity": "low",
    "category": "error-handling",
    "target": "A5-plugin-service",
    "location": "apps/collab-api/src/modules/admin.service.ts:209-222",
    "description": "adminUpdatePlugin 直接 prisma.plugin.update({where:{id}}) 无 findUnique。目标 id 不存在时 Prisma 抛 P2025，AppExceptionFilter 不识别，落到 500 internal_error。同模块其他 admin 方法（adminApprovePlugin/adminRejectPlugin line 171/188）都有 findUnique + notFound 兜底，唯独 updatePlugin 没有。",
    "evidence": "admin.service.ts:211 `const plugin = await this.prisma.plugin.update({ where: { id }, data: {...} });` 无前置存在性检查；对比 admin.service.ts:171-172 approvePlugin 有 `findUnique` + `if(!plugin) throw notFound`。",
    "trigger": "平台管理员 PATCH /api/admin/plugins/{不存在的id}。",
    "impact": "管理端对不存在插件 PATCH 返回 500 而非 404，错误码与契约不符，排障困难。无数据损坏。",
    "fix": "同类低危问题在本文件中还有：adminUpdateUser(line49)、adminUpdateTeam(line94)、adminRevokeTeamAdmin(line131)、rejectApplication(line238) 均为裸 prisma.update 无 findUnique，对不存在 id 同样 P2025→500。如要修复，建议统一加 findUnique+notFound，或更省事地在 AppExceptionFilter(common.ts:55-90) 增加一条对 PrismaClientKnownRequestError P2025 的分支统一映射为 404，一次覆盖所有 update/upsert 路径。本条仅就 PLUGIN-09 裁定为 confirmed。",
    "reasoning": "逐行核实通过。admin.service.ts:209-222 的 adminUpdatePlugin 直接 await this.prisma.plugin.update({ where:{ id }, data:{...} })，无前置 findUnique。对比同文件 adminApprovePlugin(line 171-172)、adminRejectPlugin(line 188-189) 均有 findUnique + if(!plugin) throw notFound('插件不存在')，updatePlugin 唯独缺失。全局仅注册 AppExceptionFilter（main.ts:13 useGlobalFilters(new AppExceptionFilter())），全 src 搜索 P2025/PrismaClientKnownRequestError/PrismaFilter 零命中，无任何 Prisma 错误过滤器。Prisma update 在 where.id 不存在时抛 PrismaClientKnownRequestError(code='P2025')，该异常既非 AppError 也非 HttpException，落入 AppExceptionFilter 末尾分支（common.ts:85-89）返回 HTTP 500 + code:'internal_error'。触发可达：admin.controller.ts:116-120 @Patch('plugins/:id') 直接转发到 adminUpdatePlugin，路径参数无额外校验，平台管理员 PATCH /api/admin/plugins/{不存在id} 即触发。影响：管理端对不存在插件 PATCH 错误码 500 而非 404，与契约不符，影响排障；无数据损坏（ensurePlatformAdmin 仍执行，无鉴权绕过）。非文档取舍：取舍清单中无任何关于 404 处理后置/跳过 admin 插件更新/P2025→500 明确接受 的条目。严重度 low 准确。"
  },
  {
    "id": "ADMIN-02",
    "title": "软删除（DISABLED）用户不吊销已签发 JWT，被禁用账户仍可继续访问所有端点",
    "severity": "high",
    "category": "security",
    "target": "A7-admin",
    "location": "apps/collab-api/src/modules/admin.service.ts:54-59 + apps/collab-api/src/security.ts:23-31",
    "description": "JwtAuthGuard (security.ts:11-35) 只校验 JWT 签名与 payload 字段，从不回查数据库 user.status。adminDeleteUser 仅把 status 改为 DISABLED，已签发的 JWT（默认 7d，auth.service.ts:101 expiresIn '7d'）在到期前依然有效。代码中没有任何 token 黑名单/jti 撤销机制（grep revoke/blacklist/jti 全仓零命中）。",
    "evidence": "security.ts:26-30 request.user 直接来自 payload，未查库；auth.service.ts:49 登录才查 status；admin.service.ts:56 仅改库不撤 token；auth.controller.ts:42-45 logout 仅客户端清理",
    "trigger": "平台管理员禁用某用户（DELETE /api/admin/users/:id），该用户持有的有效 JWT 继续请求受保护接口",
    "impact": "被禁用/离职的管理员或用户在最长 7 天内仍可继续调用所有受保护端点（含 admin/*），软删除形同虚设，审计与封禁失效",
    "fix": "修复方向（仅诊断，不实施）：在 JwtAuthGuard 内对 payload.sub 做一次 prisma.user.findUnique 检查 status==='ACTIVE'（注意 cache 以避免每请求一次 DB 查询），或引入 jti + 撤销表/Redis 黑名单，或缩短 JWT 寿命并在 adminDeleteUser/adminUpdateUser(status=DISABLED) 时轮换 JWT_SECRET（粗暴但有效）。同时建议 ensurePlatformAdmin/ensureTeamAdmin/ensureCurrentTeam 增加 status 校验，使软删除在服务层真正生效。注意此问题与文档取舍清单中 'Node/Python 沙箱逃逸' 或 'run_plugin_script 绕过 capability' 等 accepted-risk 完全不同主题，不属同一取舍范畴。",
    "reasoning": "亲自复核代码，所有声称均成立：\n\n1) admin.service.ts:54-59 adminDeleteUser 仅 `prisma.user.update({ where: { id }, data: { status: 'DISABLED' } })` + 写审计日志，从不撤 token。\n\n2) security.ts:11-35 JwtAuthGuard 是全局唯一守卫（app.module.ts:14 通过 APP_GUARD 注册，grep @UseGuards/NestMiddleware/Interceptor 全仓零命中）。canActivate 只做 jwt.verify + 检查 payload.sub/email + 赋值 request.user，从不查库、从不读 user.status。payload.platformRole 也只是原样回填（line 29），即使 status=DISABLED 也照常通过。\n\n3) auth.service.ts:101 JWT 默认 expiresIn '7d'（JWT_EXPIRES_IN || '7d'）。login（auth.service.ts:49）确实检查 status!==ACTIVE，但那是登录路径，对已签发的 token 无效。\n\n4) auth.controller.ts:42-45 logout 仅返回 { ok:true }，纯客户端清理，无服务端撤销。\n\n5) 全仓 grep revoke|blacklist|jti|revoked 零 token 撤销机制命中（adminRevokeTeamAdmin 是另一回事——仅改团队管理员 role）。\n\n触发时序验证：平台管理员调 DELETE /api/admin/users/:id → adminDeleteUser 把 user.status=DISABLED → 该用户 7 天内拿原有 Bearer token 仍可请求所有受保护端点。更严重的是 ensurePlatformAdmin (auth.service.ts:129-133) 重新查了 user 但只判 platformRole，不判 status——所以被禁用的平台管理员 status=DISABLED 但 platformRole 仍为 PLATFORM_ADMIN，依然能继续调 /api/admin/*。软删除形同虚设。\n\n文档取舍核对：清单内 accepted-risk 四条（LLM 计费代价、两后端 JWT 临时不互通、Node/Python 沙箱逃逸、run_plugin_script 绕过 capability）与本缺陷主题完全不同，均不覆盖'collab-api 内禁用用户后 token 撤销'。无任何 explicit-skip/known-limitation 提及软删除不吊销 token。因此非文档取舍。\n\n严重度评估：原评 high 合理——属于授权/封禁失效，被禁用/离职管理员最长 7 天可继续访问含 admin/* 在内的全部端点。未升 critical，因为 (a) token 最终会过期；(b) 平台管理员可额外通过 adminUpdateUser 把 platformRole 改为 NONE 做二次封堵，但仅靠 adminDeleteUser 单点无法阻断。"
  },
  {
    "id": "ADMIN-04",
    "title": "adminCreateUser 对已存在（含 DISABLED）邮箱不预检，唯一约束触发 P2002 被 filter 吞成 500",
    "severity": "medium",
    "category": "error-handling",
    "target": "A7-admin",
    "location": "apps/collab-api/src/modules/admin.service.ts:33-40",
    "description": "adminCreateUser 直接 prisma.user.create，没有事先 findUnique 检查。schema.prisma:78 email @unique。当 email 已存在（含被软删除 DISABLED 的同邮箱）时抛 PrismaClientKnownRequestError P2002，AppExceptionFilter (common.ts:55-90) 只识别 AppError/HttpException，其它一律 500 internal_error。register (auth.service.ts:23) 有显式 conflict 检查，adminCreateUser 没有。同时被禁用账户的同邮箱永远无法经 adminCreateUser 重建（也无 re-enable 路径，adminUpdateUser 只能改 status 但需先拿到 id）。",
    "evidence": "admin.service.ts:37 直接 create 无预检；common.ts:85-89 catch-all 返回 500；schema.prisma:78 @unique；对比 auth.service.ts:23-24 有 exists 检查",
    "trigger": "POST /api/admin/users 传一个已存在（尤其已 DISABLED）的 email",
    "impact": "(1) 重复邮箱返回 500 而非语义化 409，前端无法正确提示；(2) 禁用账号的同邮箱无法重新启用，运营上需要手工改库",
    "fix": "修复方向：adminCreateUser 第37行 create 前加 const existing = await prisma.user.findUnique({ where: { email } }); if (existing) throw conflict('该邮箱已注册'); 与 register 对齐。注意纠正原描述：re-enable 路径实际存在（adminUpdateUser 改 status 回 ACTIVE），原描述\"无 re-enable 路径\"不准确，但此点不影响 ADMIN-04 核心缺陷成立。",
    "reasoning": "已逐行核实。apps/collab-api/src/modules/admin.service.ts:37 adminCreateUser 直接 prisma.user.create，无 findUnique 预检；schema.prisma:78 email @unique 确实存在。重复 email（含 adminDeleteUser 软删为 DISABLED 的同邮箱，见 admin.service.ts:56）会触发 PrismaClientKnownRequestError P2002。该异常非 AppError 非 HttpException，common.ts:85-89 AppExceptionFilter 的 catch-all 一律返回 500 internal_error。对比 auth.service.ts:23-24 register 有 findUnique+conflict('该邮箱已注册') 显式预检，adminCreateUser 缺该检查，契约不一致。触发场景：平台管理员 POST /api/admin/users（admin.controller.ts:25-29）传已存在 email → 500 而非 409。影响：前端无法据状态码给出\"邮箱已存在\"的语义化提示，与 register 行为不齐。文档取舍清单无任何条目涉及 admin 创建用户的重复邮箱/错误码处理，非取舍。原描述一处不准确：adminUpdateUser(admin.service.ts:42-52) 支持 status:'ACTIVE'，re-enable 路径实际存在（管理员列表拿 id 后 PATCH），但此点不改变核心缺陷（adminCreateUser 撞唯一约束返回 500）。"
  },
  {
    "id": "ADMIN-05",
    "title": "rejectApplication 不校验申请状态，可把已 APPROVED（已建团）的申请改回 REJECTED 造成状态与团队数据不一致",
    "severity": "medium",
    "category": "data-consistency",
    "target": "A7-admin",
    "location": "apps/collab-api/src/modules/admin.service.ts:236-241",
    "description": "rejectApplication (admin.service.ts:236-241) 仅 ensurePlatformAdmin 后无条件 update status='REJECTED'，不校验当前 status。而 approveApplication→createTeamForApplication (auth.service.ts:135-151) 会校验 status==='PENDING' 否则 conflict，并在事务里建团+置 APPROVED。状态机非法转移：一个已 APPROVED（已建团、用户已是 TEAM_ADMIN）的申请被再次 reject，会变成 REJECTED，但团队与成员关系仍存在，留下「申请被驳回但团队照常运行」的悬挂数据。",
    "evidence": "admin.service.ts:238 `update({ where: { id }, data: { status:'REJECTED', ... }})` 无 where status 条件；对比 auth.service.ts:139 `if (application.status !== 'PENDING') throw conflict`",
    "trigger": "先 approve 一条申请建出团队，再对同一申请 id 调 POST /api/admin/team-admin-applications/:id/reject",
    "impact": "审批状态与实际团队/成员关系脱钩，前端展示与真实权限矛盾；可被用来伪造「该团队未经审批」的假象，审计混乱",
    "fix": "证据确凿、非文档取舍、可触发。建议修复：在 admin.service.ts:238 rejectApplication 内复用插件审查的同一模式，update 前先 findUnique 校验 status==='PENDING'，否则 throw conflict（与 createTeamForApplication:139 对称）。可选增强：DB 层加状态转移 CHECK 约束或应用层状态机封装，集中 DRAFT/PENDING/APPROVED/REJECTED 的合法转移。同类风险：approveApplication 已通过 createTeamForApplication 间接防了 APPROVED→APPROVED，但 reject→approve（REJECTED→APPROVED）也被 createTeamForApplication 的 status!=='PENDING' 卡住，故只需补 reject 的对称校验即可闭合。",
    "reasoning": "逐行核实确认该发现为真实状态机瑕疵，非误报、非文档取舍。\n\n代码核实：\n- admin.service.ts:236-241 rejectApplication：仅 `await this.auth.ensurePlatformAdmin(actorId)` 后无条件 `this.prisma.teamAdminApplication.update({ where: { id }, data: { status: 'REJECTED', reviewReason: reason || '', reviewedById: actorId, reviewedAt: new Date() } })`，无任何 `status === 'PENDING'` 守卫。\n- auth.service.ts:135-151 createTeamForApplication：第139行 `if (application.status !== 'PENDING') throw conflict('该申请已处理')`，事务内建 Team + TeamMembership(role:'TEAM_ADMIN') + 置申请为 APPROVED。\n- admin.controller.ts:128-138：两条 POST 路由 /:id/approve、/:id/reject 都受 ensurePlatformAdmin 保护，HTTP 可触发。\n- ApplicationStatus 枚举（schema.prisma:34-38、migration.sql:8）仅 PENDING/APPROVED/REJECTED，DB 无状态转移 CHECK/触发器约束，故 DB 层不阻拦。\n\n触发时序（可复现）：\n1) 用户注册 wantsTeamAdmin → 申请 A 为 PENDING；\n2) 平台管理员 POST /api/admin/team-admin-applications/A/approve → createTeamForApplication 校验 PENDING 通过 → 建团 T、写 TeamMembership(TEAM_ADMIN)、A=APPROVED；\n3) 平台管理员对同一 A 调 POST /api/admin/team-admin-applications/A/reject → rejectApplication 无守卫 → A 变 REJECTED，但 T 与 TeamMembership 仍在。\n\n一致性破坏证据：\n- auth.service.ts:91-98 resolveOnboarding 优先按 membership.role === 'TEAM_ADMIN' 返回 'TEAM_ADMIN_SPACE'，所以用户仍保留团队访问；但 admin.service.ts:224-229 adminApplications 列表会显示该申请为 REJECTED，造成「申请被驳回但团队照常运行」的悬挂数据。\n- 审计日志记录两条动作（approved 建团 + rejected），语义自相矛盾。\n\n对比同文件内既有模式：adminApprovePlugin(173行) 与 adminRejectPlugin(190行) 均校验 `reviewStatus !== 'PENDING'`；approveApplication 经 createTeamForApplication 间接防 APPROVED→APPROVED；唯独 rejectApplication 缺失对称守卫。approve→reject（APPROVED→REJECTED）这一非法转移在代码与 DB 两层均未阻拦。\n\n文档取舍核对：清单中所有相关条目均不覆盖此场景——「团队域数据归属/隔离」「团队管理员能力边界」是权限模型而非审批状态机；「市场审核状态机 DRAFT/REJECTED→PENDING→APPROVED/PUBLIC 或 REJECTED/TEAM」仅针对插件 marketplace 审核流程，与 TeamAdminApplication 状态机无关；无任何条目声明 APPROVED→REJECTED 为允许或已知限制。故非文档取舍。\n\n严重度评估维持 medium：触发者必须是 PLATFORM_ADMIN（降低可利用面），不造成数据丢失或鉴权绕过，仅产生审批状态与实际团队/成员关系脱钩的状态机瑕疵（medium 的「状态机瑕疵」类别精确匹配）；功能仍可用（用户仍为 TEAM_ADMIN），故不上调到 high。"
  },
  {
    "id": "ADMIN-06",
    "title": "adminCreateTeam 建团与初始流水非事务，可致 balanceCents 与 ledger 不一致",
    "severity": "low",
    "category": "transaction",
    "target": "A7-admin",
    "location": "apps/collab-api/src/modules/admin.service.ts:83-90",
    "description": "adminCreateTeam 先 prisma.team.create（行86），再单独 prisma.balanceLedger.create（行87）。两步非事务。若第二步失败（如 ledger 表约束/连接中断），team 已落库但缺初始 CREDIT 流水，team.balanceCents>0 却无对应 ledger，与 adminAdjustBalance/team.consume 维护的「余额变更必有流水」不变量冲突。对比 approvePlugin（行174-182）正确用了 $transaction。",
    "evidence": "admin.service.ts:86-87 两句独立 prisma 调用无 $transaction 包裹；admin.service.ts:174-182 是 $transaction 正确示例",
    "trigger": "balanceCents>0 且第二步 ledger 写入因任何原因失败",
    "impact": "team 余额与 ledger 脱钩，后续对账/审计无法解释初始余额来源",
    "fix": "修复建议：将 admin.service.ts:86-87 的 team.create + balanceLedger.create 包进 prisma.$transaction，与同文件 approvePlugin(174-182)、adminAdjustBalance(139-148) 及 team.service.ts consume(126-130) 的写法对齐。注意 audit(88) 可留在事务外（与 adminAdjustBalance:149 的模式一致，audit 失败不回滚业务）。",
    "reasoning": "亲自核实代码与 schema 后确认发现属实，但严重度下调为 low。\n\n【代码核实】apps/collab-api/src/modules/admin.service.ts:86-87 确为两条独立 prisma 调用、无 $transaction 包裹：line 86 prisma.team.create 落库 team（balanceCents=Number(input.balanceCents||0)），line 87 仅当 team.balanceCents>0 时执行 prisma.balanceLedger.create（reason='initial_balance', direction='CREDIT'）。两步之间任一失败即产生 team 已落库但缺初始流水。\n\n【不变量核实】codebase 其他位置一致维护「balance 变更必成对写 ledger」并均用 $transaction：admin.service.ts:174-182 adminApprovePlugin、admin.service.ts:139-148 adminAdjustBalance、team.service.ts:126-130 consume。adminCreateTeam 是唯一违反点，属 codebase 内不一致，佐证非有意取舍。\n\n【schema 核实】schema.prisma:164-176 BalanceLedger 仅有 teamId/actorUserId 外键 + (teamId,createdAt) 索引，无 unique 约束；外键引用 Team(id) 与 User(id) 在 line 86 成功后均合法，故 ledger 写入不会因自身约束失败，失败只能是 DB 连接中断/进程崩溃/锁超时等运行期故障窗口——触发条件成立但发生概率低（罕见）。\n\n【触发场景】平台管理员建团 balanceCents=10000：line 86 成功→line 87 之前/期间 DB 断连或进程被杀→team 落库 balanceCents=10000 但无 initial_balance 流水。team.service.ts:117 ledger 端点返回 SUM(ledger)≠balanceCents，对账/审计无法解释初始 10000 来源。\n\n【影响范围（关键降级依据）】防超扣不依赖 ledger：consume（team.service.ts:127）与 adminAdjustBalance 的 DEBIT（admin.service.ts:142）均直接原子读 balanceCents:{gte:amount}，缺失初始流水不影响扣款正确性，无资金损失、无超扣、无状态机死锁。影响仅为审计/对账脱钩，且可用后续 adminAdjustBalance 手动修正。\n\n【文档取舍核实】清单中无任何条目覆盖建团/初始余额/创建期 balance+ledger 事务性；economy-system 设计决策仅述 cents 计价单事务结算（针对 adminAdjustBalance），未豁免 adminCreateTeam。故非文档取舍。\n\n结论：真实一致性缺陷（破坏 codebase 既定不变量），但触发罕见、影响限于审计脱钩、无资金/超扣/状态机后果、可手动修复，符合 low（健壮性/次要边界影响正确性），原评 medium 略高。"
  },
  {
    "id": "ADMIN-07",
    "title": "adminCreateTeam 的 balanceCents 用 Number(input.balanceCents || 0)，接受浮点/超大数进 Int 列",
    "severity": "low",
    "category": "boundary",
    "target": "A7-admin",
    "location": "apps/collab-api/src/modules/admin.service.ts:86",
    "description": "`Number(input.balanceCents || 0)` 对 'abc' 得 NaN（​​`NaN || 0`→0 勉强兜底），但对 '1.5'、1.5、'0.1' 得浮点直接传给 Int 列 balanceCents（schema.prisma:106 balanceCents Int）。Postgres 对 Int 列写 1.5 会报错（500），写极大数可能溢出。cents 应为整数，此处无 Math.round/整数校验。对比 adminAdjustBalance 用 Math.max(1, ...) 至少约束了下界，adminCreateTeam 完全无约束。",
    "evidence": "admin.service.ts:86 `balanceCents: Number(input.balanceCents || 0)` 无取整/范围校验；schema.prisma:106 Int 类型；admin.service.ts:138 Math.max(1,...) 对比",
    "trigger": "POST /api/admin/teams body 传 balanceCents: 1.5 或字符串 '1.5'",
    "impact": "传浮点余额导致建团 500 报错；或边缘场景下整数截断产生静默的 cents 误差",
    "fix": "建议修复方向：admin.service.ts:86 应先取整并限幅，例如 balanceCents: Math.max(0, Math.trunc(Number(input.balanceCents) || 0))，并视需要校验 Int32 范围（±2147483647）。同时建议为 controller 引入全局 ValidationPipe + DTO（@IsInt() @Min(0)），从根本上阻断浮点/字符串注入。注意 adminAdjustBalance:138 共享同类问题（Math.max(1,...) 仅约束下界，未取整），建议一并修复；adminUpdatePlugin:217 的 priceCents 虽用 Math.max(0, Number(...))，同样未取整，建议核对。",
    "reasoning": "已独立核实代码。apps/collab-api/src/modules/admin.service.ts:86 确为 `balanceCents: Number(input.balanceCents || 0)`，无 Math.round/取整/范围校验。schema.prisma:106（Team.balanceCents）与 schema.prisma:167（BalanceLedger.amountCents）均为 Int 列。main.ts 无 useGlobalPipes/ValidationPipe，admin.controller.ts:51 用内联 TS 类型 { balanceCents?: number } 而非 class-validator DTO，故请求体原样抵达 service。\n\n触发链构造（POST /api/admin/teams, body { name, balanceCents: 1.5 }）：1.5 || 0 → 1.5（truthy）；Number(1.5) → 1.5；写 Int 列。Postgres 行为两可：浮点入 integer 列要么 \"invalid input syntax/out of range\" → 500，要么静默舍入 → cents 漂移。字符串 '1.5' 同路径；'abc' → NaN → NaN || 0 → 0（静默兜底）。极大数（如 '99999999999999'）超过 Int32 ±2147483647 → 溢出 500。\n\n证据：admin.service.ts:86；schema.prisma:106/167；admin.controller.ts:51；main.ts（无 ValidationPipe）。\n\n严重度评估：维持 low。该接口受 ensurePlatformAdmin 保护，仅平台管理员可触发；最坏为自身操作 500（可重试）或微小 cents 误差，无资金损失/越权/数据损坏/安全影响。low 合理。\n\n补充：原审查者与 adminAdjustBalance（admin.service.ts:138 Math.max(1, Number(...))）的对比略弱——该路径通过 { increment: amount } 写 Int 列同样会传浮点，共享同一缺陷；但不影响 adminCreateTeam 这一具体主张成立。"
  },
  {
    "id": "ADMIN-08",
    "title": "adminSetTeamAdmin/adminRevokeTeamAdmin 不校验团队/用户存在与状态，可对 SUSPENDED 团队或 DISABLED 用户授/撤 TEAM_ADMIN",
    "severity": "medium",
    "category": "state-machine",
    "target": "A7-admin",
    "location": "apps/collab-api/src/modules/admin.service.ts:118-134",
    "description": "adminSetTeamAdmin 用 teamId_userId 复合键 upsert，create 分支无条件建新 ACTIVE TEAM_ADMIN 成员，不检查目标 team 是否 SUSPENDED、目标 user 是否 DISABLED、或该用户是否曾被 removeMember 置 REMOVED。对一个 REMOVED 成员的 (team,userId) 组合，upsert 命中 update 分支（行123）把 status 强置 ACTIVE+role TEAM_ADMIN，等于静默撤销 team.service.removeMember（行74）的移除决定。adminRevokeTeamAdmin（行131）用 update（非 updateMany），若该 (team,userId) 不存在抛 P2025 → 500。",
    "evidence": "admin.service.ts:120-124 upsert 无 team/user 状态过滤；team.service.ts:73-74 removeMember 设 REMOVED；admin.service.ts:131 update 无存在性预检",
    "trigger": "对 SUSPENDED 团队或 DISABLED 用户调 POST /api/admin/teams/:id/admins；或 revoke 一个不存在的 (team,user) 组合",
    "impact": "(1) 可给已停用团队/已禁用用户授 TEAM_ADMIN，产生僵尸管理员；(2) 平台管理员可绕过团队管理员的移除决定把被踢成员拉回并升权；(3) revoke 不存在成员关系返回 500 而非 404",
    "fix": "已核实事实链（全部亲自读代码确认）：\\n1) admin.service.ts:118-127 adminSetTeamAdmin 用 prisma.teamMembership.upsert(where:{teamId_userId:{teamId,userId:input.userId}}, create:{teamId,userId,role:'TEAM_ADMIN'}, update:{role:'TEAM_ADMIN',status:'ACTIVE'})——create/update 两分支均无 team.status==='ACTIVE'、user.status==='ACTIVE'、membership.status 过滤。\\n2) admin.service.ts:129-134 adminRevokeTeamAdmin 用 prisma.teamMembership.update(where:{teamId_userId:{teamId,userId}})（非 updateMany），不存在则抛 PrismaClientKnownRequestError code P2025。\\n3) common.ts:55-90 AppExceptionFilter（@Catch()）只识别 AppError 与 HttpException，对 P2025 无映射 → 落入 response.status(500).json({code:'internal_error'...})。main.ts:13 已 useGlobalFilters。\\n4) admin.controller.ts:67-77 路由 POST /api/admin/teams/:id/admins（body.userId）与 DELETE /api/admin/teams/:teamId/admins/:userId 直接传 service，service 内无预检。\\n5) auth.service.ts:129-133 ensurePlatformAdmin 仅校验 platformRole==='PLATFORM_ADMIN'，不校验目标 team/user。\\n6) team.service.ts:68-77 removeMember 置 status:'REMOVED'；team.service.ts:70 禁止移除自己，73 拒绝移除 TEAM_ADMIN——平台 admin 的 setTeamAdmin 可把这些 REMOVED 成员复活并升权。\\n7) schema.prisma:29-32 MembershipStatus {ACTIVE, REMOVED}；9-12 UserStatus {ACTIVE, DISABLED}；19-22 TeamStatus {ACTIVE, SUSPENDED}；TeamMembership 复合主键 @@id([teamId,userId]) 与 upsert where 键一致。\\n\\n建议修复方向（仅供参考，本次只读不实施）：adminSetTeamAdmin 前预检 team.status==='ACTIVE' 与 user.status==='ACTIVE'，并对已存在但 status==='REMOVED' 的成员关系决定是拒绝还是显式复活（应拒绝或要求显式 re-add）；adminRevokeTeamAdmin 改用 findUnique 预检或用 updateMany 并按 count===0 抛 notFound(404)；全局过滤器补充 PrismaClientKnownRequestError（P2025→404, P2002→409, P2003→400/404）以避免裸 500。",
    "reasoning": "亲自核对代码，描述全部属实。\\n\\n1) admin.service.ts:118-127 adminSetTeamAdmin 确实用 prisma.teamMembership.upsert(where:{teamId_userId:{teamId,userId:input.userId}}, create:{teamId,userId,role:'TEAM_ADMIN'}, update:{role:'TEAM_ADMIN',status:'ACTIVE'})——create 与 update 两分支均无 team.status/user.status/membership.status 过滤。\\n\\n2) admin.service.ts:129-134 adminRevokeTeamAdmin 用 prisma.teamMembership.update(where:{teamId_userId:{teamId,userId:targetUserId}}, data:{role:'MEMBER'})（非 updateMany）。\\n\\n3) 全局异常处理 common.ts:55-90 AppExceptionFilter 只识别 AppError 与 HttpException，对 PrismaClientKnownRequestError（P2025）无任何映射，落入 response.status(500) 分支。main.ts:13 useGlobalFilters 已挂载。故 revoke 不存在成员确返回 500 而非 404——证实。\\n\\n4) 触发时序成立：team.service.ts:68-77 removeMember 置 status:'REMOVED'（行74）；若平台管理员随后对该 (team,userId) 调 setTeamAdmin，upsert 命中 update 分支（行123）把 status 强置 ACTIVE+role TEAM_ADMIN，等于静默撤销团队管理员的移除决定。对一个 SUSPENDED 团队（adminDeleteTeam 行102 置 SUSPENDED）或 DISABLED 用户（adminDeleteUser 行56 置 DISABLED）调 setTeamAdmin 同样无条件成功，产生 ACTIVE TEAM_ADMIN 成员关系指向已停用团队/已禁用用户——僵尸管理员。\\n\\n5) schema.prisma 确认状态枚举：MembershipStatus{ACTIVE,REMOVED}、UserStatus{ACTIVE,DISABLED}、TeamStatus{ACTIVE,SUSPENDED}，复合主键 @@id([teamId,userId])（行125）与 upsert where 键一致。\\n\\n6) auth.service.ts:129-133 ensurePlatformAdmin 仅校验 platformRole==='PLATFORM_ADMIN'，不校验目标 team/user 状态，无额外护栏。\\n\\n非文档取舍：清单第16项讲的是团队/普通用户的隔离边界，平台管理员跨团队操作是设计意图，但「平台管理员可对 SUSPENDED 团队/DISABLED 用户/已 REMOVED 成员授权」在文档中无任何 accepted-risk/known-limitation 声明，docs/collab-api.md:80 仅描述功能。属清单之外的真实代码问题。"
  },
  {
    "id": "ADMIN-09",
    "title": "adminDeleteUser 允许禁用执行者自己或最后一个 PLATFORM_ADMIN，无自保护，可锁死所有平台管理员入口",
    "severity": "medium",
    "category": "logic-bug",
    "target": "A7-admin",
    "location": "apps/collab-api/src/modules/admin.service.ts:54-59",
    "description": "adminDeleteUser 把任意用户（含当前 actor 自己）置 DISABLED，无任何自我保护或末位管理员保护。seed-admin.ts:20-23 仅在「不存在任何 PLATFORM_ADMIN」时建管理员，但运行期把唯一平台管理员禁用后，没有任何机制阻断，且 ADMIN-02 导致被禁用管理员旧 token 仍可用 7 天，之后将彻底无平台管理员可登录管理端（除非重跑 seed-admin bootstrap，但那也需要 DB 直连）。",
    "evidence": "admin.service.ts:54-59 无自我/末位保护；seed-admin.ts:20 仅建管时判断无管理员；security.ts 不查 status（ADMIN-02）",
    "trigger": "平台管理员 DELETE /api/admin/users/<自己的 id>，或把唯一 PLATFORM_ADMIN 禁用",
    "impact": "误操作或恶意（多管理员共谋）可锁死整个平台管理入口，需 DB 层手工恢复",
    "fix": "修复建议：在 adminDeleteUser 与 adminUpdateUser（当 input.status 将被设为 DISABLED 时）入口加两道保护——(1) 拒绝 actorId === id（自禁用/自删除）；(2) 事务内先 count({ where: { platformRole:'PLATFORM_ADMIN', status:'ACTIVE' } })，若 <=1 且目标为活跃平台管理员则抛 conflict('不能禁用最后一个平台管理员')。另外 ensurePlatformAdmin 应同时校验 status==='ACTIVE'（顺带修 ADMIN-02 的禁用 token 仍可用问题），以及把 seed-admin.ts:20 的 findFirst 加 status:'ACTIVE' 过滤，让 bootstrap 能在唯一管理员被禁用时真正重建。",
    "reasoning": "核实属实。apps/collab-api/src/modules/admin.service.ts:54-59 的 adminDeleteUser 仅调 ensurePlatformAdmin(actorId) 后无条件 prisma.user.update({data:{status:'DISABLED'}})，无\"不能禁用自己(actorId===id)\"、无\"不能禁用最后一个 PLATFORM_ADMIN\"保护；同一缺陷在 adminUpdateUser(admin.service.ts:42-52, 当 input.status=DISABLED 时)同样存在。触发时序成立：环境仅 seed 默认 1 个平台管理员时，调 DELETE /api/admin/users/<自己id> 即被置 DISABLED。佐证链：auth.service.ts:129-133 ensurePlatformAdmin 只查 platformRole 不查 status、security.ts:24-30 JwtAuthGuard 只验 JWT payload 不查 status，故被禁用管理员旧 token 在 JWT_EXPIRES_IN||'7d'(auth.service.ts:101) 内仍可调所有 admin API；auth.service.ts:49 login 显式拒绝 status!==ACTIVE，token 过期后永久无法登录。非文档取舍：清单中团队管理员能力边界/平台管理员入口隔离/数据隔离模型等均不覆盖\"平台管理员可自禁用或禁用末位平台管理员\"，无 explicit-skip/known-limitation/accepted-risk 可援引。原描述一个细节不准确需更正——seed-admin.ts:20 的 findFirst({where:{platformRole:'PLATFORM_ADMIN'}}) 不带 status 过滤，被禁用的管理员 platformRole 不变仍被命中并 return(22-24)，故重跑 seed-admin 也救不回，实际恢复路径比原述更难（必须 DB 直连改 status 或改 platformRole/删行后重跑），这反而加强了影响。"
  },
  {
    "id": "ADMIN-10",
    "title": "adminAdjustBalance 的 CREDIT 分支用无条件 team.update，团队不存在时抛 P2025 被吞成 500",
    "severity": "low",
    "category": "error-handling",
    "target": "A7-admin",
    "location": "apps/collab-api/src/modules/admin.service.ts:144-146",
    "description": "CREDIT 分支用 `tx.team.update({ where: { id: teamId }, data })`，若 teamId 不存在抛 P2025 → filter 500。DEBIT 分支用 updateMany + count!==1 判断有妥善的 insufficientBalance 处理，但 CREDIT 没有等价的 notFound 处理。审计行 149 在事务外写，若事务内 CREDIT 抛 P2025，审计不会写（这点尚可接受）。",
    "evidence": "admin.service.ts:145 `await tx.team.update({ where: { id: teamId }, data })` 对不存在 id 抛 P2025；common.ts:85-89 非 AppError 转 500",
    "trigger": "POST /api/admin/teams/:teamId/balance-adjustments 传一个不存在的 teamId，direction=CREDIT",
    "impact": "前端拿到 500 internal_error 无法区分「团队不存在」，调试与用户体验差",
    "fix": "额外发现（不在本条范围内但同源）：DEBIT 分支（admin.service.ts:142-143）对不存在的 teamId 也会错误报告——updateMany 返回 count:0，触发 insufficientBalance() 返回 402 insufficient_balance，把「团队不存在」误报成「钱包余额不足」。两个分支都缺 notFound 处理，建议统一改为先 findUnique 校验团队存在，或 CREDIT 也用 updateMany + count!==1 模式区分 not_found/insufficient_balance。修复时一并补上 DEBIT 的 not_found。",
    "reasoning": "代码核实属实。admin.service.ts:144-146 CREDIT 分支用 `await tx.team.update({ where: { id: teamId }, data })`，对不存在的 teamId 会抛 Prisma P2025（PrismaClientKnownRequestError）。common.ts:85-89 的 AppExceptionFilter 兜底分支把非 AppError/HttpException 的错误统一转成 500 internal_error；grep 全 collab-api 无 P2025/PrismaClientKnownRequestError 处理。对比 DEBIT 分支（admin.service.ts:141-143）用 updateMany + count!==1 显式抛 insufficientBalance()，确实存在 CREDIT 缺少等价 notFound 处理的不对称。触发场景成立：admin.controller.ts:79-82 直接把 @Param('teamId') 透传进 adminAdjustBalance，无前置存在性校验，平台管理员 POST /api/admin/teams/<不存在teamId>/balance-adjustments direction=CREDIT 即触发 500。审计行 admin.service.ts:149 在 $transaction 外，事务内抛 P2025 时审计确实不写（与原评一致，尚可接受）。文档取舍清单无任何条目覆盖管理端余额调整错误码或 P2025 映射，非取舍。严重度维持 low：仅限平台管理员路径，无资金损失/数据损坏/鉴权绕过（CREDIT increment 只在存在的行上成功，资金不会被错误改动），仅是错误码不准确（500 而非 404），有明确 workaround。"
  },
  {
    "id": "XSEC-01",
    "title": "全局未启用 ValidationPipe，全部 @Body 直接透传，无白名单/类型/枚举校验",
    "severity": "medium",
    "category": "security",
    "target": "A8-crosscut",
    "location": "apps/collab-api/src/main.ts:9-34 + apps/collab-api/src/app.module.ts:9-16",
    "description": "全仓 grep `useGlobalPipes|ValidationPipe|usePipes` 与 class-validator 装饰器（IsString/IsEmail/IsEnum/Min/Max 等）均无任何命中。所有控制器统一用内联 `@Body() body: { ... }` 裸 TypeScript 类型注解，该注解在运行时（经 tsc/tsx 编译后）被完全抹除，Express 原样接收客户端 JSON。因此后端既不做字段类型校验，也不做范围/枚举校验，更不会剥离未知字段（whitelist）。例如 `POST /api/auth/register` 的 password 仅在 auth.service.register 里 `length < 8` 判空，但 email 仅判 `includes('@')`（`a@` 即合法），wantsTeamAdmin 任意值都会被当真。`POST /api/admin/teams/:id` 的 body 直接 `data: input` 透传进 Prisma（见下条）。整体缺少契约层防线。",
    "evidence": "Grep `useGlobalPipes|ValidationPipe|usePipes` -> No matches；Grep `class-validator|IsString|IsNumber|IsEmail|IsOptional|IsEnum|Min\\(|Max\\(@Body` 所有命中仅为 `@Body() body: {...}` 内联类型（如 auth.controller.ts:15、admin.controller.ts:27 等）。class-validator 与 class-transformer 虽在 package.json deps（行28-29）安装但从未被使用。common.ts 中亦无 ValidationPipe 注册。",
    "trigger": "向任意接口提交畸形 JSON（缺字段、错类型、超长串、未知枚举值、多余键）。",
    "impact": "非法/畸形/越权字段直达业务层与 Prisma；部分场景靠服务层手写判空兜底，但覆盖不一致，是大量后续 finding 的根因（越权字段透传、状态机非法值、整数运算 NaN 等均源于此）。",
    "fix": "若后续验证要把 XSEC-01 作为\"根因 finding\"，建议改为 info/medium 级架构性观察，并配套审计具体衍生 bug 是否成立，而不是把\"未启用 ValidationPipe\"本身按 high 上报——那会把风险量化抬得过高。固定方案可启用全局 ValidationPipe（whitelist+forbidNonWhitelisted+transform）或像 adminUpdateUser/plugin-package 那样在服务层显式字段筛选（adminUpdateTeam/team.service.createInvitation 等缺此处理）。注意 collab.module.ts 中 EconomyService/MarketplaceService 同时依赖 PluginService 间接依赖的 AuthService，引入 DTO 时不影响依赖图。",
    "reasoning": "独立核实确认：(1) apps/collab-api/src/main.ts:9-34 的 bootstrap() 仅注册 helmet/CORS/globalPrefix/AppExceptionFilter/Swagger，无 useGlobalPipes；(2) apps/collab-api/src/app.module.ts:9-16 providers 仅 PrismaService + APP_GUARD(JwtAuthGuard)，无 APP_PIPE；(3) 全仓 grep useGlobalPipes|ValidationPipe|usePipes 零命中，class-validator/class-transformer 仅在 package.json:28-29 作为依赖声明存在，源码零使用（IsString/IsEmail/IsEnum/Min/Max 等装饰器无任何命中）；(4) 8 个业务 controller 全部用内联 @Body() body:{...} 裸 TS 类型注解，该注解经 tsc/tsx 编译后运行时抹除，NestJS 默认 @Body 直接返回 Express JSON，无 whitelist/transform。文档取舍清单无任何条目覆盖契约校验/输入白名单（唯一相关条目\"插件上传约束\"已在 plugin-package.ts:86-151 normalizePluginPackage 落地，与全局 ValidationPipe 无关），故非文档取舍。触发场景验证：POST /api/admin/teams/:id body {name,balanceCents} 经 admin.controller.ts:57 → admin.service.ts:94 prisma.team.update({where:{id},data:input}) 直接透传，可静默改 balanceCents 而绕过 adminAdjustBalance(142-147) 的 BalanceLedger 审计流水（聚合字段与流水脱钩），但此路径需 PLATFORM_ADMIN；auth.service.ts:21 email 仅 includes('@')，'a@' 即合法；auth.service.ts:33 wantsTeamAdmin 任意 truthy 值触发。畸形字段触发 PrismaClientValidationError，common.ts:85-89 把 error.message（含 Prisma 内部模型/字段名）直接返回客户端，信息泄漏。综合：核心事实属实、非误报、非取舍，是真实缺陷。但作为独立 finding 直接危害有限——写操作仍受 JwtAuthGuard+ensurePlatformAdmin/ensureTeamAdmin 保护，金额操作有 updateMany 条件防超扣（economy.service.ts:79-83、team.service.ts:127-128、admin.service.ts:142-143），类型混淆多由 Number(x||0)/Math.max 兜底，无直接资金损失或鉴权绕过证据。原评 high 偏高，下调 medium（系统性契约防线缺失致边界不一致+局部信息泄漏，是衍生 finding 的根因而非独立高危 bug）。"
  },
  {
    "id": "XLOG-01",
    "title": "adminUpdateTeam 将未清洗的请求体 data: input 直接透传给 Prisma",
    "severity": "medium",
    "category": "logic-bug",
    "target": "A8-crosscut",
    "location": "apps/collab-api/src/modules/admin.service.ts:92-97",
    "description": "`PATCH /api/admin/teams/:id` -> `adminUpdateTeam` 直接 `await this.prisma.team.update({ where: { id }, data: input })`，未像 adminUpdateUser（行45-48 显式取 displayName/status/platformRole）那样做字段白名单。由于无全局 ValidationPipe 剥离未知键，客户端传 `{name:'x', status:'ACTIVE', balanceCents:99999, createdAt:'...'}` 时，`balanceCents`/`createdAt` 等字段会被原样喂给 Prisma。Prisma 会对未知列抛 PrismaClientValidationError（500），但对合法列（如 balanceCents）会直接写入——即团队管理员/平台管理员可通过 PATCH 团队接口直接改写团队共享余额，绕过专用的 `POST /api/admin/teams/:teamId/balance-adjustments`（后者带事务+流水+审计）。文档明确「团队管理员不能修改团队余额」（collab-desktop-client.md），而此处连平台管理员也能不经流水静默改余额，审计断链。",
    "evidence": "admin.service.ts:92-97 `async adminUpdateTeam(actorId, id, input: {name?:string; status?:'ACTIVE'|'SUSPENDED'}) { await this.auth.ensurePlatformAdmin(actorId); const team = await this.prisma.team.update({ where: { id }, data: input }); ... }` —— 对比 adminUpdateUser（行42-52）显式构建 data 对象只取三字段。schema.prisma Team 模型含 balanceCents（行106）。",
    "trigger": "平台管理员（或团队管理员若误授权）调用 PATCH /api/admin/teams/:id 携带 balanceCents 字段。",
    "impact": "团队余额可被静默改写且无 BalanceLedger 流水、无审计日志，破坏账本完整性；同时多余字段触发 Prisma 校验错误时被映射为 500（见 XERR-01）。",
    "fix": "修复方向（仅建议，未实施）：将 adminUpdateTeam 改为显式白名单构建 data 对象，与 adminUpdateUser（行45-48）保持一致，仅取 name/status，并在 status 变化时考虑是否需流水。更稳妥地：引入全局 ValidationPipe + DTO 类（class-validator whitelist:true），统一剥离未知键。同时复核同文件其他直接透传 input 的入口（如 adminUpdatePlugin 行211-219 已部分白名单但 description/status 仍裸传 input 字段、adminRevokeTeamAdmin 等），评估是否同类风险。证据文件均为绝对路径：O:/lingfang-platform/apps/collab-api/src/modules/admin.service.ts；O:/lingfang-platform/apps/collab-api/src/modules/admin.controller.ts；O:/lingfang-platform/apps/collab-api/src/main.ts；O:/lingfang-platform/apps/collab-api/prisma/schema.prisma；O:/lingfang-platform/docs/collab-api.md。",
    "reasoning": "亲自核实结论：\n\n1) 代码确实如描述。admin.service.ts:92-97 `adminUpdateTeam` 直接 `await this.prisma.team.update({ where: { id }, data: input })`，未做字段白名单。对比 adminUpdateUser（行45-48）显式构建 data 对象只取 displayName/status/platformRole 三字段。\n\n2) controller（admin.controller.ts:55-59）把裸 `@Body() body` 整体传入 adminUpdateTeam，无任何 DTO/transform。\n\n3) 全局无 ValidationPipe：grep `ValidationPipe|useGlobalPipes|class-validator|class-transformer` 在整个 src 零命中；main.ts（apps/collab-api/src/main.ts）仅 useGlobalFilters + helmet + cors + swagger。TypeScript 注解类型 `{ name?: string; status?: ... }` 是编译期类型擦除，运行期 input 就是反序列化的整个请求体。因此 NestJS 不会剥离未知键，Prisma 拿到的 data 是完整请求体。\n\n4) Team 模型（schema.prisma:101-114）含可写标量 balanceCents/name/slug/status。客户端传 `{\"name\":\"x\",\"balanceCents\":99999}` 时，data 即 `{name:\"x\", balanceCents:99999}`，Prisma 直接写入合法列 balanceCents。\n\n5) 账本不变量确认：所有其他 balanceCents 修改都在 $transaction 内同时写 BalanceLedger —— adminCreateTeam:87、adminAdjustBalance:139-148、team.service.consume:126-130、economy.service。唯 adminUpdateTeam 例外，绕过流水。文档另设专用接口 POST /api/admin/teams/:id/balance-adjustments（collab-api.md:81）专用于余额调整，updateTeam 越权绕开它。\n\n6) 触发场景真实：带 JWT 的平台管理员调用 PATCH /api/admin/teams/:id 携带 balanceCents，即静默改写团队余额且无 BalanceLedger 流水。ensurePlatformAdmin（行93）确实硬性拦截团队管理员，所以可触发角色仅限平台管理员——原描述「或团队管理员若误授权」属夸大，应以平台管理员为准。\n\n文档取舍核对：清单中相关项只覆盖「团队管理员不能修改团队余额」（collab-desktop-client.md:37）与「团队管理员能力边界」，属 design-decision。该约束由 ensurePlatformAdmin 满足（团队管理员在此路由被拒），不构成对本 bug 的豁免。代码实现的「平台管理员可经 updateTeam 静默改余额且断流水」是清单之外的真实问题，不属任何 explicit-skip/known-limitation/future-work/accepted-risk。\n\n严重度纠正：原评 high 偏高。该路径需平台管理员（高可信角色）权限，非跨租户/低权限提权；且通用 auditLog 仍记录 `admin.team.updated` 及 metadata（行95）。真正受破坏的是「余额变更必须配 BalanceLedger 流水」的账本可审计不变量——属数据一致性与审计链路问题。综合评 medium 更准确。\n\n附带确认原描述引用的次级点：传入未知非模型列（如 createdAt）会触发 PrismaClientValidationError（XERR-01 相关），亦成立，但非本条核心。"
  },
  {
    "id": "XCONC-01",
    "title": "邀请码兑换 usedCount 检查在事务外读取、事务内无条件递增，并发可超 maxUses",
    "severity": "medium",
    "category": "concurrency",
    "target": "A8-crosscut",
    "location": "apps/collab-api/src/modules/team.service.ts:35-51",
    "description": "`POST /api/invitations/redeem` -> `redeemInvitation`：先在事务外 `findUnique` 读 `invite.usedCount`（行36），事务外比较 `invite.usedCount >= invite.maxUses`（行39），命中后再在 `$transaction` 内 `invitationCode.update({ data: { usedCount: { increment: 1 } } })`（行47）。这是经典的读-判-改分离，无行锁/无条件更新。schema.prisma 的 InvitationCode 无 `CHECK (usedCount <= maxUses)` 约束，DB 层不兜底。对照团队余额扣款（team.service.ts consume 行127）与钱包购买（economy.service.ts 行79-83）都正确使用了 `updateMany where balanceCents:{gte:amount}` 的条件原子更新，唯独邀请码没用同样模式。",
    "evidence": "team.service.ts:35-49：`const invite = await findUnique(...)`（行36，事务外）/ `if (invite.usedCount >= invite.maxUses) throw`（行39，事务外）/ `$transaction(async tx => { upsert membership; tx.invitationCode.update({ where:{id:invite.id}, data:{usedCount:{increment:1}} }) })`（行41-49，无条件 increment）。schema InvitationCode（行147-162）无 CHECK 约束。",
    "trigger": "maxUses=N 时，N+1 个用户持同一邀请码并发 POST /api/invitations/redeem。",
    "impact": "团队人数可超出邀请码设定的 maxUses 上限，绕过团队规模限制。",
    "fix": "修复模板（与 consume/purchase 一致）：把行41-49 事务内的无条件 increment 改为条件原子更新——await tx.invitationCode.updateMany({ where:{ id:invite.id, usedCount:{ lt:invite.maxUses }, status:'ACTIVE' }, data:{ usedCount:{ increment:1 } } })，并检查 result.count===1 否则 throw badRequest('邀请码已达到使用次数上限')。expiresAt 校验也可一并下推到 where 子句或保持事务外预检+事务内条件更新双保险。注意：invite 对象的事务外快照（status/maxUses）与事务内实际行可能因并发状态变化（如管理员 DISABLED）而脱节，条件更新能覆盖 status/maxUses 变化场景。",
    "reasoning": "独立核对代码全部成立。team.service.ts:36 在事务外 prisma.invitationCode.findUnique 读 usedCount（Prisma 默认 PostgreSQL READ COMMITTED，findUnique 不加行锁）；行39 事务外比较 usedCount >= maxUses 命中则 throw；行41-49 的 $transaction 内用 invitationCode.update({ where:{id:invite.id}, data:{usedCount:{increment:1}} }) 无条件递增，是 update 非 updateMany，不构成条件原子操作。schema.prisma:147-162 与 migration.sql:59-71 均确认 InvitationCode 无 CHECK(usedCount<=maxUses) 兜底。\\n\\n时序验证（maxUses=1 两用户并发）：T1 两者 findUnique 都读到 usedCount=0；T2 两者 0>=1 均 false 通过校验；T3 各自事务 upsert membership 并 increment；终态 usedCount=2 且两成员均创建——经典 TOCTOU/check-then-act 竞态，触发成立。\\n\\n对照证据成立：consume(team.service.ts:127) 用 tx.team.updateMany({ where:{ id, balanceCents:{gte:amount} } }) + updated.count!==1；purchase(economy.service.ts:79-83) 用 tx.wallet.updateMany 同模式原子扣款。邀请码是唯一未用条件原子更新的计数器。\\n\\n文档取舍清单无任何条目涉及邀请码并发/maxUses 软约束/TOCTOU；\"团队数据隔离\"是访问控制语义非并发保证。非文档取舍。\\n\\n严重度从 high 纠正为 medium：邀请码超卖后果仅\"团队超额接纳成员\"（准入边界），非资金（consume/purchase 才是 high 资金正确性）、非鉴权绕过、需并发触发、团队管理员可事后 disableInvitation/removeMember 兜底。"
  },
  {
    "id": "XCONC-02",
    "title": "rate 评分聚合存在 TOCTOU：existing 读在事务外，ratingSum/ratingCount increment 基于过期值",
    "severity": "medium",
    "category": "data-consistency",
    "target": "A8-crosscut",
    "location": "apps/collab-api/src/modules/marketplace.service.ts:111-147",
    "description": "`rate` 先事务外 `pluginRating.findUnique`（行130）拿到 `existing.score`，再在 `$transaction([upsert, plugin.update({ratingSum:{increment: existing? score-existing.score : score}, ratingCount:{increment: existing?0:1}})])`（行131-144）。两次并发 rate（同一 user+plugin）都会读到同一个旧 existing.score，各自计算差值并都 `increment`，导致 ratingSum 多加、聚合与明细（PluginRating 单行经 upsert 最终只保留最后一次 score）脱钩。同样 ratingCount 的 `existing?0:1` 依赖事务外读，首评时两次并发都看到 existing==null 都 increment 1，ratingCount 比 PluginRating 实际行数多。",
    "evidence": "marketplace.service.ts:130 `const existing = await findUnique(...)`（事务外）/ 行131-144 `$transaction([ pluginRating.upsert({update:{score,comment}, create:{...score,comment}}), plugin.update({data:{ratingSum:{increment: existing?score-existing.score:score}, ratingCount:{increment: existing?0:1}}}) ])`。两次并发 A/B 读到相同 existing.score=3、都设 score=5 -> 两次 increment(2) -> ratingSum +=4 而非 2。",
    "trigger": "同一用户对同一插件并发提交两次评分（双击/多标签页），或首评时并发。",
    "impact": "plugin.ratingSum/ratingCount 与 PluginRating 明细长期不一致；市场排序按 ratingSum/ratingCount（search 行25、detail 行67 计算平均分）失真。",
    "fix": "修复方向（非取舍、属真实缺陷）：把 existing 读 + 差值计算整体放进单一事务并对 Plugin 行做悲观锁，或改用「无 existing 时事务内 INSERT...ON CONFLICT + 触发器/聚合重算」。最稳妥的修法是放弃 increment 差值维护，改为：在事务内对 PluginRating upsert 后，执行 `UPDATE plugin SET ratingSum=(SELECT COALESCE(SUM(score),0) FROM PluginRating WHERE pluginId=?), ratingCount=(SELECT COUNT(*) FROM PluginRating WHERE pluginId=?) WHERE id=?`，让聚合从权威明细重算，彻底消除差值/计数依赖外部读。注：Prisma $transaction 数组形式不支持原生 SQL 锁，需用 $transaction(async tx => {...}) 形式并在 tx 内读 existing，配合 SELECT FOR UPDATE（Prisma 需 queryRaw）。",
    "reasoning": "代码与发现描述逐行吻合，已亲自核对 apps/collab-api/src/modules/marketplace.service.ts:111-147。\n\n证据链：\n1. schema.prisma:312 `@@unique([pluginId, userId])` 确认 PluginRating 单插件单用户唯一一行；schema.prisma:200-201 确认 plugin.ratingCount/ratingSum 是聚合字段；marketplace.service.ts:41 与 :67 确认平均分与排序依赖该聚合。\n2. marketplace.service.ts:130 `const existing = await this.prisma.pluginRating.findUnique({...})` 在 $transaction 之外执行——这是 TOCTOU 的「check」。\n3. marketplace.service.ts:131-144 `$transaction([upsert, plugin.update])`，其中 :140 `ratingSum:{increment: existing? score-existing.score : score}` 与 :141 `ratingCount:{increment: existing?0:1}` 的增量值在 JS 层基于事务外读到的 existing 计算，再交给 Prisma 转成 SQL。Prisma $transaction 数组形式默认 READ COMMITTED，对 Plugin 行无 FOR UPDATE 锁；两次并发读 existing 必然读到同一旧值。\n\n时序验证（改分场景）：初始 PluginRating.score=3、ratingSum=3、ratingCount=1。两并发请求都读到 existing.score=3、都提交 score=5 → 两次 increment(2) → ratingSum 最终=7（正确值5），ratingCount 仍=1。PluginRating 最终单行 score=5（upsert 唯一约束保证只一行），但聚合 ratingSum=7 与明细脱钩，多加 2。\n\n时序验证（首评场景）：初始无评分行。两并发请求都读到 existing=null，都走 create→第二请求 upsert 命中唯一约束改走 update（最终一行 score=5）。但两次都 ratingSum +=5 → ratingSum=10（正确值5），两次 ratingCount +=1 → ratingCount=2（实际行数1）。ratingCount 比实际明细行数多。\n\n影响：search 排序（:24-25 ratingSum desc）与详情平均分（:41/:67 ratingSum/ratingCount）失真，且数据长期不一致、无法自愈。\n\n触发现实性：前端 Market.tsx:168-177 有 rating 去抖（setRating(true)/finally false，按钮请求中应禁用）降低概率，但多标签页、多设备、curl/curl 并发、网络重试、或去抖失效时仍可触发；这是真实的并发竞态而非纯理论。\n\n严重度：保持 medium。它是竞态致持久化数据不一致（属 high 边界），但前端去抖缓解、需特定并发、影响是排序/平均分数值偏移而非核心功能失效或资金损失，且可通过重算聚合修复，未达 high 的「核心功能失效」门槛，也不属于 critical。\n\n文档取舍核对：清单中无任何项涵盖评分聚合并发正确性；最相关的「数据隔离模型」「市场提交流程状态机」「插件上传约束」均不涉及聚合字段维护的竞态。未被标记为 accepted-risk/known-limitation。非文档取舍，为真实缺陷。"
  },
  {
    "id": "XERR-01",
    "title": "AppExceptionFilter 将非 AppError/HttpException（含所有 Prisma 错误）落入 500 并回显原始 error.message",
    "severity": "medium",
    "category": "error-handling",
    "target": "A8-crosscut",
    "location": "apps/collab-api/src/common.ts:55-90",
    "description": "`@Catch()` 全兜底 filter 仅识别 AppError 与 HttpException 两类，其它（含 PrismaClientKnownRequestError、PrismaClientValidationError、ZodError、原生 TypeError 等）全部落入行85-89 的 500 分支，直接把 `error.message` 当 message 返回客户端。Prisma 的错误消息通常包含表名/字段名/约束名（如 `Foreign key constraint failed on the field: teamId`、`Unknown argument balanceCents`、`Unique constraint failed on the fields: (email)`），等于把内部 schema 结构泄露给调用方，且把本应是 409/400 的业务冲突（唯一冲突、外键、校验错误）统一错误映射成 500。例如 register 的 email 唯一竞争（auth.service.ts:23-24 的 findUnique+create 之间存在窗口）会被吞成 500+prisma 文案，而非 conflict 409。",
    "evidence": "common.ts:85-89 `response.status(500).json({ code:'internal_error', message: error instanceof Error ? error.message : '服务内部错误', requestId })`；行73-83 仅处理 HttpException，行63-71 仅处理 AppError。Grep `stack|stackTrace` -> No matches（确认未记录栈，这点是好的，但 message 仍泄漏）。",
    "trigger": "任何触发 Prisma 错误的路径：唯一冲突、外键失败、未知字段（XLOG-01）、连接错误等。",
    "impact": "信息泄漏（schema 结构）+ HTTP 状态错误映射（业务冲突返回 500）+ 客户端无法据 code 做正确分支处理。",
    "fix": "建议修复：在 AppExceptionFilter 增加 Prisma 错误分支，import { Prisma } from '@prisma/client' 后对 Prisma.PrisismaClientKnownRequestError 按 error.code 映射：P2002 -> 409 conflict（已存在/已购买）、P2025 -> 404 not_found、P2003 -> 409/400 关联冲突、PrismaClientValidationError -> 400 bad_request；并对 500 兜底分支用通用 message（如'服务内部错误'）替换 error.message 以避免 schema 泄漏，仅日志记录原始消息。同时在 register/purchase 的并发路径用 try/catch 捕获 P2002 显式转 conflict，而非依赖全局 filter。",
    "reasoning": "重新读 common.ts:55-90 与触发链，描述完全属实。\n\n1) 代码核实（apps/collab-api/src/common.ts:55-90）：@Catch() 全兜底 filter 的 catch(error) 仅三类分支：行63-71 AppError（带 status/code/message），行73-83 HttpException，行85-89 兜底 500。兜底分支确实执行 `response.status(500).json({ code:'internal_error', message: error instanceof Error ? error.message : '服务内部错误', requestId })`，把任意 Error 的原始 message 直接回显。\n\n2) 无任何 Prisma 专项处理：Grep `instanceof Prisma|PrismaClientKnownRequestError|P2002|Prisma.PrismaClient` 在整个 collab-api/src 仅命中 admin.service.ts:44 的一条注释（说明那里手动挑字段避免 PrismaClientValidationError），没有任何 filter/interceptor/pipe 捕获 Prisma 错误。main.ts:13 `app.useGlobalFilters(new AppExceptionFilter())` 是唯一全局 filter；app.module.ts 未注册任何全局 pipe/interceptor。因此 PrismaClientKnownRequestError（P2002/P2003/P2025）、PrismaClientValidationError、ZodError、原生 TypeError 等全部落入 500 + 原始 message。\n\n3) 触发场景已构造验证：schema.prisma:78 `email String @unique`，auth.service.ts:23-26 register 是 findUnique+create 经典 TOCTOU，两个并发同 email 注册请求都通过第24行的 exists 检查后，第二个 create 在第26行触发 P2002，message 形如 `Unique constraint failed on the fields: (email)`，filter 落到 500。同样 economy.service.ts:67 的 `purchase.findUnique` + 行77-108 单事务 purchase.create（schema.prisma:297 `@@unique([pluginId, buyerUserId])`）在并发购买时第二个 create 触发 P2002 → 500，而本应是 409 already_purchased。两处都是真实可触发的业务冲突被错误映射为 500。\n\n4) 影响：信息泄漏（表名/字段名/约束名如 `Foreign key constraint failed on the field: teamId`、`Unique constraint failed on the fields: (email)`），业务冲突错误映射成 500（客户端无法按 code 做正确分支），且购买并发场景下用户感知「内部错误」而非「已购买」。注意事务回滚保证了余额一致性（不会超扣），所以核心资金正确性未破，仅是错误码/message 不当。\n\n5) 文档取舍清单核查：无任何条目涉及错误处理/Prisma 错误映射/信息泄漏；ADR-0002 accepted-risk 仅指「看不到细粒度计费/依赖网关」，与此无关。非取舍、非已知限制、非未来工作。属清单之外的真实代码缺陷。"
  },
  {
    "id": "XSEC-02",
    "title": "CORS origin 在未配置时回退为 true（reflect 任意 Origin）且 credentials:true",
    "severity": "low",
    "category": "security",
    "target": "A8-crosscut",
    "location": "apps/collab-api/src/main.ts:14-21",
    "description": "`app.enableCors({ origin: origins.length ? origins : true, credentials: true })`。当 `CORS_ALLOWED_ORIGINS` 未设置或为空（origins.length===0）时，origin 回退为 `true`，即 reflect 任意请求来源到 `Access-Control-Allow-Origin`；配合 `credentials: true`，浏览器会在携带凭证（cookie/Authorization）时允许任意站点跨域读取响应。文档（04-engineering.md §2）说密钥统一在 .env，但本机配置后端 URL 时若运维忘配 CORS_ALLOWED_ORIGINS，默认即开放给所有来源。生产应显式 fail-close（空则拒绝或报错），而非 reflect。",
    "evidence": "main.ts:14-21 `const origins = (...).split(',').map(trim).filter(Boolean); app.enableCors({ origin: origins.length ? origins : true, credentials: true })`。",
    "trigger": "部署环境未设 CORS_ALLOWED_ORIGINS（或误清空）后启动服务。",
    "impact": "若部署时漏配 CORS_ALLOWED_ORIGINS，任意恶意站点可携带受害者凭证跨域调用本 API（CSRF-like 数据外泄/操作）。本地默认 .env 有配置可缓解，但代码兜底策略不安全。",
    "fix": "建议修复：空 origins 时 fail-close（如固定 origin: false 或抛错拒绝启动），而非回退为 reflect。同时因鉴权不靠 cookie，可考虑 credentials:false。注意：原描述把影响夸大为\"携带受害者凭证跨域\"——Bearer-in-localStorage 模型下该路径不成立，应在 review 中如实修正为\"任意源可跨域访问公开端点+preflight 全放行\"。另可顺带核对 main.ts:24 的 JWT_SECRET 默认值 'dev-collab-change-me' 同样是 fail-open，但属另一发现范畴。",
    "reasoning": "亲自核对 apps/collab-api/src/main.ts:14-21，代码确如描述：`const origins = (...).split(',').map(trim).filter(Boolean); app.enableCors({ origin: origins.length ? origins : true, credentials: true })`。当 CORS_ALLOWED_ORIGINS 未设或为空时，origin 回退为 true（reflect 任意 Origin）。这是真实的 fail-open 兜底，且非文档取舍（清单无任何 CORS 默认开放条目；04-engineering.md §2/§6 仅说\"统一在 .env 管理\"并给示例值，未声明\"未配置即开放\"为可接受取舍）。\n\n触发场景成立：运维漏配/误清空 CORS_ALLOWED_ORIGINS 即生效（main.ts:14 `process.env.CORS_ALLOWED_ORIGINS || ''` → split → 全空 → origins.length===0 → origin:true）。监听 0.0.0.0:3000（main.ts:33）意味着绑定所有网卡。\n\n但原评 medium 的\"携带受害者凭证跨域读取响应（CSRF-like 数据外泄）\"影响论证不成立，需修正降级：\n1) 全仓 grep 'cookie' 在 apps/collab-api 下零命中——服务端不发任何 cookie，无 Set-Cookie，无 cookie-parser。\n2) 鉴权走 Authorization: Bearer（security.ts:20），token 存前端 localStorage（desktop/src/lib/api.ts:2,90,101）并由脚本手动注入请求头（api.ts:134 `headers.Authorization = Bearer ${authToken}`）。\n3) CORS\"凭证\"指 cookie/HTTP-auth/client-cert。Bearer token 在受害者另一个 origin 的 localStorage，恶意页面 fetch 不会自动携带、JS 也读不到；因此 origin:true+credentials:true 在本模型下无法用于劫持受害者凭证读取私有端点。\n4) 真实可触发影响：任意源可对 @Public() 端点（health、auth/register、auth/login 等）跨域读/写，preflight 对 application/json 全放行——危害有限（注册垃圾账号等），而非凭证劫持/数据外泄。\n\n综上：是真实缺陷（fail-open 兜底不安全，应 fail-close），非文档取舍，可触发真实但较低危害的影响。严重度从 medium 纠正为 low。"
  },
  {
    "id": "XSEC-03",
    "title": "Swagger /api/docs 与 /api/docs-json 在所有环境无条件公开，未按生产关闭或加鉴权",
    "severity": "medium",
    "category": "security",
    "target": "A8-crosscut",
    "location": "apps/collab-api/src/main.ts:23-30",
    "description": "`SwaggerModule.setup('api/docs', app, document, { jsonDocumentUrl: '/api/docs-json' })` 无条件挂载，无 NODE_ENV 守卫、无 @Public 之外的鉴权。`/api/docs` 与 `/api/docs-json` 对未登录任意访问者开放，完整暴露所有控制器/路由/参数结构（含 /api/admin/* 全套管理端接口、钱包、购买、插件上传等内部字段）。`Grep NODE_ENV|production` 仅命中 main.ts:30 自身的 setup 调用，无任何环境判断。文档明确市场审核/平台管理员入口隔离，但 API 文档面未隔离。",
    "evidence": "main.ts:29-30 `const document = SwaggerModule.createDocument(app, config); SwaggerModule.setup('api/docs', app, document, { jsonDocumentUrl: '/api/docs-json' })`。Grep `NODE_ENV|process.env.NODE_ENV|production` -> 仅 main.ts:30（setup 调用本身），无任何守卫。Admin 所有接口 @ApiBearerAuth + 文档化（admin.controller.ts:7-9）。",
    "trigger": "任意未鉴权访问 GET /api/docs 或 GET /api/docs-json。",
    "impact": "攻击者零成本获得完整接口拓扑与字段契约，降低攻击门槛（信息泄漏）。虽不直接绕权，但显著放大 XLOG-01/XCONC-01 等可利用性。",
    "fix": "确认 XSEC-03 为真实缺陷。修复方向：在 main.ts:29-30 用 `if (process.env.NODE_ENV !== 'production')` 守卫 SwaggerModule.setup 调用；或保留 setup 但给 /api/docs 与 /api/docs-json 加一层鉴权（如 Basic Auth 中间件，仅平台管理员）。鉴于 ADR 与文档取舍清单明确「平台管理员入口隔离」与「密钥隔离」，对生产环境的 API 文档面同样应隔离。注意 collab-api 首发非公开互联网服务，但仍应遵循最小暴露面原则。该问题不阻断核心功能，但应纳入安全加固 backlog。",
    "reasoning": "独立核实通过。(1) main.ts:29-30 代码确实如描述：const document = SwaggerModule.createDocument(app, config); SwaggerModule.setup('api/docs', app, document, { jsonDocumentUrl: '/api/docs-json' })，无 NODE_ENV/production 守卫。Grep NODE_ENV|process.env.NODE_ENV|production 在 apps/collab-api/src 下零命中。(2) 触发场景成立：app.module.ts:14 注册全局 APP_GUARD=JwtAuthGuard，security.ts:11-21 对无 Bearer token 请求一律 unauthorized()；但 NestJS SwaggerModule.setup 通过 Express 直接注册 handler，不经 NestJS 控制器路由系统，故全局 JwtAuthGuard 不作用于 /api/docs 与 /api/docs-json，二者确实对未鉴权访问者开放。(3) 影响真实：admin.controller.ts:7-9（@ApiTags('Admin')+@ApiBearerAuth()）、wallet.controller.ts、marketplace.controller.ts、plugins.controller.ts 等全部带 @ApiOperation 文档化，admin.controller.ts:25-27 createUser 的 email/password/platformRole 等字段契约随 OpenAPI 全量暴露。GET /api/docs-json 即可零成本获得全部接口拓扑与字段结构。(4) 文档取舍清单 40 条均未覆盖 API 文档面隔离；docs/collab-api.md:5-6、docs/collab-deployment.md:16-17/199 虽将 /api/docs 当默认入口写入文档，但未在任何 accepted-risk/known-limitation/explicit-skip 中声明 Swagger 公开可接受；清单中\"平台管理员入口隔离\"指的是 Web 管理端登录入口隔离，不涵盖 Swagger 文档面。故属清单之外的真实代码问题。严重度 medium 准确：信息泄漏降低攻击门槛但不直接绕权/致数据损坏，且后端为团队内部/桌面部署非公开互联网服务，未达 high。"
  },
  {
    "id": "XSEC-04",
    "title": "jwt.verify 未指定 algorithms 白名单；JWT_SECRET 弱默认兜底",
    "severity": "low",
    "category": "security",
    "target": "A8-crosscut",
    "location": "apps/collab-api/src/security.ts:23-24",
    "description": "`jwt.verify(token, process.env.JWT_SECRET || 'dev-collab-change-me')` 未传 `algorithms` 选项。jsonwebtoken@9 默认不再接受 alg=none（已修复历史 CVE），但仍存在算法混淆面：当 secret 为字符串时，未限定 algorithms 会使 verify 在 RS256/HS256 之间按 token 头选择，理论可被「公钥当 HMAC 密钥」类攻击（若将来 secret 换成非对称材料）。此外三处共用弱默认兜底 `dev-collab-change-me`（security.ts:24、auth.service.ts:104，与本处 main 间接相关），生产若忘配 JWT_SECRET 则全系统签发/校验用同一公开常量，token 可被任意伪造。.env.example 行3 也用该弱默认做示范。",
    "evidence": "security.ts:23-24 `const payload = jwt.verify(token, process.env.JWT_SECRET || 'dev-collab-change-me') as jwt.JwtPayload`（无第三参 options，即未限定 algorithms）。auth.service.ts:102-106 issueToken 同样 secret。package.json 行33 jsonwebtoken ^9.0.3。.env.example 行3 `JWT_SECRET=\"dev-collab-change-me\"`。",
    "trigger": "JWT_SECRET 未配置（兜底弱密钥）或 secret 使用非对称材料场景下被构造 alg 混淆 token。",
    "impact": "algorithms 缺失：算法混淆理论攻击面；弱兜底：忘配 JWT_SECRET 时 token 可伪造，全系统鉴权形同虚设（fail-open）。",
    "fix": "建议修复方向（按项目习惯，仅指出不改代码）：1) 在 main.ts 启动时对 process.env.JWT_SECRET 做 fail-fast 校验——未配置或等于 dev-collab-change-me 时直接抛错退出，避免静默兜底；2) 删除 security.ts:24 与 auth.service.ts:104 的 || 'dev-collab-change-me' 兜底；3) jwt.verify 显式传 { algorithms: ['HS256'] } 收紧；4) 注意 apps/collab-api/.env 含真实强 secret 已入仓（本发现范围外，但与\"密钥永不入仓\"红线相关，建议另查）。这些改进符合 .env.example 改用占位提示而非真实弱默认值的常规做法。",
    "reasoning": "已亲自核实代码与描述一致。security.ts:24 确为 `jwt.verify(token, process.env.JWT_SECRET || 'dev-collab-change-me')`，无第三参 options 即未限定 algorithms 白名单（grep 全仓 `algorithms` 零命中）。auth.service.ts:100-107 issueToken 同样使用 `process.env.JWT_SECRET || 'dev-collab-change-me'` 作为 Secret。package.json:33 jsonwebtoken ^9.0.3。.env.example:3 确为 `JWT_SECRET=\"dev-collab-change-me\"`。main.ts/bootstrap 与 app.module.ts 均无对 JWT_SECRET 的启动期 fail-fast 校验（grep 仅两处引用，均在 `||` 兜底分支）。\\n\\n两条子主张分述：\\n（A）弱兜底——这是最实质的问题：若运维忘配 JWT_SECRET（.env 缺失/未设该变量），系统静默回退到 .env.example 公开记录的常量，任何拿到源码者可伪造任意用户 JWT（含 platformRole:'PLATFORM_ADMIN'，security.ts:29 据此授予平台管理员权限），构成 fail-open 鉴权。注意当前仓内 .env:3 实际配置了一个强随机 secret（这本身另涉\"密钥永不入仓\"红线但不在本条 finding 范围），故默认场景下不可触发，仅在配置失误时触发。\\n（B）algorithms 缺失——jsonwebtoken@9 已修复 alg=none 历史漏洞，且当前 secret 形态是对称字符串（非 RS 公钥材料），经典\"公钥当 HMAC 密钥\"混淆在此配置下无实际利用面，属 defense-in-depth 加固建议而非当前可利用漏洞。\\n\\n文档取舍核对：清单中\"桌面端只保存后端 URL，不保存后端密钥（JWT_SECRET 统一在 apps/collab-api/.env）\"（docs/04-engineering.md §2 行32）只规定密钥存放位置，并未授权运行时硬编码公开默认值兜底；algorithms 项无任何文档取舍条目对应。故非文档取舍。\\n\\n严重度评估：维持 low。algorithms 缺失实际不可利用（info/low 加固）；弱兜底为真实 fail-open 但需运维配置失误触发且有现成强 secret 兜底场景，medium 与 low 之间，结合项目全局 CLAUDE.md 明确将安全置最低优先级的约束，low 合理。"
  },
  {
    "id": "XLOG-02",
    "title": "marketplace install 每次都 increment installCount，包括已存在 Installation 的 upsert-update（重装），installCount 虚高",
    "severity": "medium",
    "category": "data-consistency",
    "target": "A8-crosscut",
    "location": "apps/collab-api/src/modules/marketplace.service.ts:86-109",
    "description": "`install` 用 `pluginInstallation.upsert`（命中已存在则 update version/status，未命中则 create），但无论命中与否，紧随其后无条件 `plugin.update({ data: { installCount: { increment: 1 } } })`。因此同一团队反复 install（禁用后重装、版本刷新）会持续累加 installCount，而实际「有效安装团队数」并未增长。市场排序默认按 installCount desc（search 行28），统计失真直接影响排序公平性。该 increment 也未受 pluginInstallation 是否新增的判定保护。注意 plugin.service.ts:138-144 的 installMarketplacePlugin 对「已存在」分支只 update 不 increment，逻辑更正确——两处实现不一致。",
    "evidence": "marketplace.service.ts:99-106 `$transaction([ pluginInstallation.upsert({ where:{pluginId_teamId}, update:{version,status:'ENABLED',installedById}, create:{...} }), plugin.update({ where:{id:pluginId}, data:{installCount:{increment:1}} }) ])`。对比 plugin.service.ts:138-144：existing 分支仅 update installation 不 increment installCount。search 行28 默认 orderBy installCount desc。",
    "trigger": "同一团队对同一插件多次 POST /api/marketplace/install（重新安装/刷新）。",
    "impact": "installCount 虚高，市场默认排序（按安装量）失真，影响插件曝光公平性。",
    "fix": "建议修复方向：参考 plugin.service.ts:131-144 的正确写法——先 findUnique installation 判定是否已存在；仅在「不存在→新建」分支才 increment installCount，重装(update)分支不 increment。两处 install 入口语义应统一。若需更严谨，可改为以「distinct 团队安装数」做派生计数或在 installation 上维护 firstInstallAt。注意 marketplace.service.ts 的 upsert 写法天然无法区分 create/update，需拆为 findUnique + 条件分支或使用 upsert 的 create 侧做 increment（但 Prisma upsert 不支持 update/create 差异化聚合更新，故需拆分）。",
    "reasoning": "亲自核实代码确认：(1) apps/collab-api/src/modules/marketplace.service.ts:99-106 的 install() 用 pluginInstallation.upsert 后无条件 plugin.update({ data:{ installCount:{ increment:1 } } })，与 upsert 命中 create 还是 update 完全无关；(2) 默认排序触发链成立：marketplace.controller.ts:16 默认 sort='installs' → marketplace.service.ts:23-28 非rating/recent 走 [{ installCount:'desc' }]，且行38/74 把 install_count 暴露给前端；(3) 同业务的另一入口 plugin.service.ts:137-144 (POST /api/plugins/:id/install) 仅在 create 分支 increment、existing 分支不 increment，证明「重装不应重复计数」是作者已知的正确语义，marketplace.service.ts 是遗漏实现 bug；(4) 全仓 grep installCount 无任何 cron/重算/补偿任务修正；(5) docs grep install_count/安装数 无语义定义，文档取舍清单无任何条目涉及安装计数口径——非文档取舍。触发时序：同团队 T 对同插件 P 反复 POST /api/marketplace/install（重装/刷新），每次 installCount+1，第 N 次后 installCount=N 但有效安装团队数恒为 1，致市场默认排序（按 installCount desc）与对外 install_count 双双失真，影响插件曝光公平性。无资金/崩溃/数据丢失，可重算恢复，故不到 high；但属聚合字段与明细脱钩 + 两 install 入口契约不一致 + 对全部市场插件生效，故 medium。"
  },
  {
    "id": "XCONTRACT-01",
    "title": "Swagger 文档版本号 0.1.0 与 package.json 0.0.1 不一致",
    "severity": "low",
    "category": "contract-mismatch",
    "target": "A8-crosscut",
    "location": "apps/collab-api/src/main.ts:26 + apps/collab-api/package.json:4",
    "description": "main.ts Swagger DocumentBuilder `setVersion('0.1.0')`（行26），而 package.json version 为 `0.0.1`（行4）。health.controller.ts 故意用 `require('../package.json').version`（行8）返回 0.0.1 供 collab-admin 版本比对（注释行17）。结果：Swagger 文档展示版本 0.1.0，/api/health 返回 0.0.1，同一服务对外暴露两套不一致的版本号。collab-admin 若据 Swagger 版本判断「有新版本」会与据 health 判断得出相反结论。",
    "evidence": "main.ts:26 `.setVersion('0.1.0')`；package.json:4 `\"version\": \"0.0.1\"`；health.controller.ts:8 `const { version } = require('../package.json')`，行18 返回 `{status,service,version}`。",
    "trigger": "运维/前端同时读取 Swagger 版本与 /api/health 版本做比对。",
    "impact": "版本管理语义混乱，依赖版本号做兼容/升级判断的客户端（collab-admin）可能误判。",
    "fix": "真实缺陷：main.ts:26 硬编码 setVersion('0.1.0') 与 package.json:4 '0.0.1' 脱钩，Swagger 文档展示错误版本号。修复方向：改为 require('../package.json').version 动态读取（与 health.controller.ts 同源），消除双源不一致。注意原发现影响链需纠正：collab-admin 不读 Swagger。",
    "reasoning": "代码事实全部亲自核实属实。apps/collab-api/src/main.ts:26 为 `.setVersion('0.1.0')`（硬编码），经 main.ts:30 `jsonDocumentUrl: '/api/docs-json'` 与 `/api/docs` 对外暴露，Swagger 文档 info.version=0.1.0。apps/collab-api/package.json:4 为 `\"version\": \"0.0.1\"`。apps/collab-api/src/health.controller.ts:8 `require('../package.json')` → /api/health 返回 version=0.0.1。两套版本号不一致客观成立，且 0.1.0 是与 package.json 真值脱钩的硬编码，非文档取舍（清单无版本号管理条目）。\\n\\n但原发现描述的影响链不成立：对 apps/collab-admin/src 全量 Grep（api/docs|docs-json|Swagger|swagger）零匹配，collab-admin 完全不读 Swagger。checkUpdate() (App.tsx:92-116) 唯一来源是 /api/health 的 data.version（=0.0.1），比对对象是 collab-admin 自身 pkg.version（App.tsx:42 import pkg from '../package.json'，collab-admin package.json:4 同为 0.0.1），二者相等 → setUpdateResult('current')，更新检查不会误判。故「据两者得出相反结论」无依据。\\n\\n真实影响仅为 Swagger UI/docs-json 上版本号错误展示（0.1.0），对读文档者轻微误导，且为维护陷阱（改 package.json 不会同步到硬编码）。无功能损坏、无资金/鉴权风险、无客户端实际触发。severity=low 合理维持。"
  },
  {
    "id": "XSM-01",
    "title": "rejectApplication 无状态机校验，可对已 APPROVED/已 REJECTED 申请重复驳回改写审计",
    "severity": "medium",
    "category": "state-machine",
    "target": "A8-crosscut",
    "location": "apps/collab-api/src/modules/admin.service.ts:42-52,236-241",
    "description": "`rejectApplication`（admin.service.ts:236-241）直接 `teamAdminApplication.update({ where:{id}, data:{status:'REJECTED',...} })`，无 `status === 'PENDING'` 前置检查；而对应的 approve 路径 createTeamForApplication（auth.service.ts:139）有 `if (application.status !== 'PENDING') throw conflict`。结果：对已 APPROVED 或已 REJECTED 的申请可重复 reject，反复覆盖 reviewReason/reviewedAt/reviewedById，审计时间线被改写。同时 adminUpdateUser（行42-52）允许平台管理员把自己的 platformRole 改为 NONE 或 status 改为 DISABLED，无「不能降级/禁用自己」保护——若系统中只有一个 PLATFORM_ADMIN（seed-admin bootstrap 场景），自降级后系统将无任何平台管理员，所有 /api/admin/* 接口永久不可用，需手动跑 seed 重建。",
    "evidence": "admin.service.ts:236-241 rejectApplication 无状态检查直接 update；对比 auth.service.ts:139 `if (application.status !== 'PENDING') throw conflict('该申请已处理')`。admin.service.ts:42-52 adminUpdateUser 无 actorId===id 自保护（行49 直接 update）。seed-admin.ts:20 `findFirst({where:{platformRole:'PLATFORM_ADMIN'}})` 仅当完全无平台管理员时才 bootstrap。",
    "trigger": "reject：管理员对已处理申请再次点驳回。updateUser 自降级：唯一平台管理员把自己改 NONE/DISABLED。",
    "impact": "reject：申请审计被反复改写、reviewer 身份失真。updateUser 自降级：单管理员系统锁死，需人工介入数据库重建管理员。",
    "fix": "修复方向：1) rejectApplication 在 update 前先 findUnique，复用 auth.service.ts:139 同款 `if (application.status !== 'PENDING') throw conflict('该申请已处理')` 检查，与 adminApprovePlugin/adminRejectPlugin 的状态守卫对齐；并避免已 APPROVED（已建团队）的申请被错误回写为 REJECTED 造成状态-数据不一致。2) adminUpdateUser 增加 `if (actorId === id && (input.platformRole !== undefined || input.status !== undefined))` 自保护，禁止平台管理员降级或禁用自己；或更严格：禁止对自己 platformRole 降为 NONE、status 置 DISABLED。3) 可选：自降级前检查当前系统是否仍存在至少一名其他 PLATFORM_ADMIN（防止唯一管理员锁死，恢复需手动跑 seed-admin.ts:10 的 PLATFORM_ADMIN_BOOTSTRAP_ENABLED=true 重建）。",
    "reasoning": "亲自核实代码，发现成立。\n\n子问题 A（rejectApplication 无状态机校验）：admin.service.ts:236-241 的 rejectApplication 直接 `prisma.teamAdminApplication.update({ where: { id }, data: { status: 'REJECTED', reviewReason: reason || '', reviewedById: actorId, reviewedAt: new Date() } })`，无 `status === 'PENDING'` 前置检查。对照：\n- createTeamForApplication（auth.service.ts:139）有 `if (application.status !== 'PENDING') throw conflict('该申请已处理')`；\n- adminApprovePlugin（admin.service.ts:173）有 `if (plugin.reviewStatus !== 'PENDING') throw conflict('插件不在审核中')`；\n- adminRejectPlugin（admin.service.ts:190）同款保护。\n唯独 rejectApplication 缺失。控制器 admin.controller.ts:134-138 仅转发，无补救。\n\n触发时序：1) 平台管理员对申请 P1 调 POST /api/admin/team-admin-applications/P1/approve → 申请 APPROVED、团队已创建（auth.service.ts:142 tx.team.create）。2) 同一管理员（或另一管理员）对 P1 调 POST /.../P1/reject → admin.service.ts:238 直接把 status 覆盖为 REJECTED，reviewReason/reviewedById/reviewedAt 被新值改写。结果：(a) 团队已实际创建但申请显示为 REJECTED，状态-数据不一致（非团队成员看到的申请状态误导）；(b) 原审批准者的 reviewedById 被覆盖为最后驳回者，审计时间线失真。同理对已 REJECTED 的申请可反复驳回改写审计。这是状态机非法转移（APPROVED→REJECTED/REJECTED→REJECTED 应为非法）+ 契约不一致（与同类 plugin 路径不对称）。\n\n子问题 B（adminUpdateUser 无自降级/自禁用保护）：admin.service.ts:42-52 接受 platformRole/status 并在行 49 直接 `prisma.user.update({ where: { id }, data })`，无 actorId === id 自保护；控制器 admin.controller.ts:31-35 接受任意 :id，body.platformRole 可传 'NONE'。seed-admin.ts:20-24 仅在 `findFirst({where:{platformRole:'PLATFORM_ADMIN'}})` 为 null 时 bootstrap。\n\n触发场景：唯一平台管理员（seed bootstrap 后未新增第二管理员，常见）调 PATCH /api/admin/users/<自己id> { platformRole:'NONE' } 或 { status:'DISABLED' }。结果：自己 platformRole 降为 NONE 或 status DISABLED。所有 /api/admin/* 接口经 ensurePlatformAdmin（auth.service.ts:129-133）校验要求 platformRole==='PLATFORM_ADMIN'，自降级后无任何用户满足，系统无任何平台管理员，管理端全部接口永久不可用。恢复需手动跑 npm run seed（seed-admin.ts 会检测无 admin 后重建）或直接改库。\n\n两子问题证据严密、触发场景成立、影响真实。文档取舍清单无相关条目（\"团队管理员能力边界\"\"平台管理员入口隔离\"\"插件市场审核状态机\"均不覆盖），不属 explicit-skip/known-limitation/future-work/accepted-risk。"
  },
  {
    "id": "XERR-02",
    "title": "logout 为无操作 no-op，丢失更新语义：JWT 在 logout 后仍完全有效",
    "severity": "low",
    "category": "security",
    "target": "A8-crosscut",
    "location": "apps/collab-api/src/modules/auth.controller.ts:40-45",
    "description": "`POST /api/auth/logout` 仅 `return { ok: true }`（行43-45），不在服务端做任何 token 失效（无黑名单/无版本号/无 redis）。JWT 无状态，签发后到 JWT_EXPIRES_IN（默认 7d，.env 行4）前始终有效。注释（行42）虽承认「当前版本为无状态 JWT 客户端清理」，但这意味着 token 泄漏后无法主动撤销，只能等过期。结合 XSEC-04 的 7d 长过期与弱兜底密钥，泄漏窗口可达一周。文档（collab-platform.md）未把无状态 JWT 列为 accepted-risk 的撤销能力缺失，仅 collab-api 收敛说明提到 JWT 临时不互通。",
    "evidence": "auth.controller.ts:40-45 `@Post('logout') ... logout() { return { ok: true } }`。auth.service.ts:101 `expiresIn: process.env.JWT_EXPIRES_IN || '7d'`。security.ts:23-34 verify 不查任何撤销表。",
    "trigger": "token 泄漏后用户点退出登录期望失效，但旧 token 仍可用。",
    "impact": "token 泄漏后无法主动失效，攻击窗口最长 7 天；前端「退出登录」给用户虚假安全感。",
    "fix": "建议（只读审查、不实施）：要么从契约中移除 logout 端点、前端纯本地清理并在 UI 明确告知\"无服务端撤销\"，要么引入 tokenVersion 字段（user 表加 tokenVersion，JWT payload 携带，JwtAuthGuard 与 user.tokenVersion 比对）让 logout/改密/封禁能即时吊销所有旧 token——后者与既有 platformRole/status 校验风格一致，改动面可控。若维持无状态，至少把 JWT_EXPIRES_IN 在生产建议下调（如 1d）并配合 refresh，缩短泄漏窗口。注意：全局 CLAUDE.md 的「安全性原则」要求优先级最低/不新增安全控制，但本任务的安全类发现按指令仍需上报；是否修复由项目方依其安全策略裁定。",
    "reasoning": "亲自核对，代码确实如描述且触发场景成立：\n\n1. apps/collab-api/src/modules/auth.controller.ts:40-45 — `@Post('logout') logout() { return { ok: true }; }`，确为纯 no-op，服务端无任何状态变更，仅靠注释「当前版本为无状态 JWT 客户端清理」自证。\n\n2. apps/collab-api/src/modules/auth.service.ts:101 — `expiresIn: (process.env.JWT_EXPIRES_IN || '7d')`；apps/collab-api/.env:4 `JWT_EXPIRES_IN=\"7d\"`，故默认 7 天有效期确认。\n\n3. apps/collab-api/src/security.ts:23-34 — `jwt.verify(token, JWT_SECRET ...)` 仅校验签名与 exp，不查任何撤销表/黑名单/tokenVersion/sessionStore。Grep 全仓无 revoke/blacklist/tokenVersion 命中（admin.controller 的 revokeTeamAdmin 是撤销团队管理员角色，与 JWT 撤销无关）。\n\n触发链验证：用户登录获 7d JWT → token 泄漏 → 用户点退出登录，前端调 POST /api/auth/logout 收到 {ok:true} 并清本地 token，但服务端无动作 → 攻击者持旧 token 仍可通过 JwtAuthGuard（只验签名+exp）访问所有受保护端点，最长达 7 天。退出登录给用户虚假安全感这一点成立。\n\n非文档取舍：已通读所附文档取舍清单全部条目，没有任何一条把「JWT 缺乏主动撤销 / logout 不可吊销」列为 explicit-skip / known-limitation / accepted-risk / future-work。docs/collab-platform.md 的「JWT 不互通（临时约束）」仅讲两套后端 token 不复用，不涵盖单后端 logout 撤销缺失。docs/collab-api.md:35 仅把 POST /api/auth/logout 列为「退出登录」未承诺撤销语义。controller 内注释虽诚实承认无状态，但这是实现说明，不是文档层面的正式取舍记录。故属清单之外的真实问题。\n\n严重度评估：维持 low。无状态 JWT 本身是主流架构取舍；首发定位为团队内部自用（非公开市场）；token 主要泄漏面是客户端本地存储被窃，桌面环境下面有限。但叠加 7d 长过期确实把窗口放大到一周，比典型 access token（15min~1h）+ refresh token 模型差，故不降为 info。原评 low 准确。\n\n类目修正建议：原列 error-handling 可接受，但更准确的根因/影响是鉴权撤销缺口，建议归 security（本任务要求安全类仍上报但标注 category=security）。"
  },
  {
    "id": "PPK-01",
    "title": "付费市场插件可经 /api/plugins/:id/install 免费安装（缺购买校验，绕过付费墙）",
    "severity": "critical",
    "category": "security/economy",
    "target": "A6-plugin-package",
    "location": "O:\\lingfang-platform\\apps\\collab-api\\src\\modules\\plugin.service.ts:131-147 (installMarketplacePlugin)；对照 marketplace.service.ts:94-97 (有付费校验的同语义路径)",
    "description": "installMarketplacePlugin 校验链仅检查 status==='ENABLED' && reviewStatus==='APPROVED' && marketplace && visibility==='PUBLIC'（第 134 行），随后直接 pluginInstallation.findUnique → existing?update:$transaction(create+installCount increment)（137-144），全程无 priceCents / purchase / payment_required 校验。对照 marketplace.service.ts:94-97 的 install 在 priceCents>0 时显式 `purchase.count===0 -> throw new AppError(402, 'payment_required')`。两条安装路径行为不一致，文档主推的 /api/plugins/:id/install（docs/collab-api.md:70）可绕过付费墙。",
    "impact": "付费市场插件被免费安装、installCount 虚增、作者无法获得分成（EconomyService.purchase 的扣款/分成流程被完全旁路），破坏插件经济模型；设计文档 design.md:82 明确要求 install 必须做付费校验，属设计契约违反。",
    "trigger": "任意已登录团队成员对任意 APPROVED+PUBLIC+marketplace 且 priceCents>0 的插件调用 POST /api/plugins/:id/install",
    "fix": "plugin.service.ts:137 附近插入与 marketplace.service.ts:94-97 等价的付费校验；理想应抽取共享 ensurePurchased 辅助函数消除两条路径的契约分歧。",
    "reasoning": "全新独立发现，不在上一轮确认清单内。与 SCHEMA-06（installCount 双计数）同处两条 install 路径、属同域不同面的契约不一致缺陷。"
  },
  {
    "id": "PPK-02",
    "title": "availablePlugins 向任意已登录用户泄露全部市场插件源码（含付费插件），客户端可直接运行",
    "severity": "high",
    "category": "data-consistency",
    "target": "A6-plugin-package",
    "location": "O:\\lingfang-platform\\apps\\collab-api\\src\\modules\\plugin.service.ts:56-71 (availablePlugins)；plugin-package.ts:194 (publicPlugin.files 无条件回传)",
    "description": "availablePlugins 第三 OR 子句 `{ marketplace: true, reviewStatus: 'APPROVED', visibility: 'PUBLIC' }`（第 64 行）不带 teamId/purchase/installation 过滤，把所有团队已上架的公共市场插件拉回。第 70 行对每个命中插件调 publicPlugin，而 publicPlugin 第 194 行 `files: plugin.files` 无条件返回 JSONB 内联的完整源码。对照 marketplace.service.ts:69-83 detail 仅返回 capabilities、不回 files，存在脱敏契约不一致。",
    "impact": "付费插件源码被任意用户无需购买即可提取并在桌面端运行（iframe srcDoc 直接渲染，nodejs/python 经 run_plugin_script 落盘执行），作者智力成果被白嫖；与 marketplace.detail 的脱敏+付费前置模型矛盾。",
    "trigger": "任意已登录用户 GET /api/plugins/available",
    "fix": "publicPlugin 对市场/非购买来源的 files 做脱敏，或拆分作者态/团队态/市场态三套 DTO；与 marketplace.detail 的脱敏模型对齐。",
    "reasoning": "全新独立发现。MKT-02（评分聚合 TOCTOU）同处 marketplace.service.ts 但问题域不同；PPK-05 同源于 publicPlugin 过度披露（reviewReason/reviewedById），是同一 DTO 的另一泄露维度。"
  },
  {
    "id": "AUTH-01",
    "title": "/auth/refresh 为已禁用用户主动签发新 token（sessionFor 不校验 user.status）",
    "severity": "high",
    "category": "state-machine",
    "target": "A1-auth",
    "location": "O:\\lingfang-platform\\apps\\collab-api\\src\\modules\\auth.service.ts:63-89 (sessionFor)；auth.controller.ts:33-38 (refresh)、:26-31 (me)",
    "description": "sessionFor 是 /auth/me 与 /auth/refresh 的共享入口，仅 `if (!user) throw unauthorized()`（第 68 行），从不校验 user.status，随后 issueToken（第 76 行）签发全新 7d token。对照 login（第 49 行）显式 `if (!user || user.status !== 'ACTIVE') throw unauthorized()`，两路径对 status 处理不对称。已亲自复核 sessionFor:68 与 login:49。",
    "impact": "被封禁/禁用用户只要持有一个未过期 token，即可通过 refresh 永久续命并持续读取 /me 会话数据；refresh 本应是重新校验资格的检查点，却主动为已禁用账号签发新凭证，导致管理员通过 adminDeleteUser/adminUpdateUser(status:DISABLED) 实际上无法终止其会话。",
    "trigger": "管理员用 adminDeleteUser 或 adminUpdateUser status:DISABLED 禁用用户后，该用户在旧 token 未过期前调用 POST /api/auth/refresh",
    "fix": "sessionFor:68 的 `if (!user) throw unauthorized();` 后追加 `if (user.status !== 'ACTIVE') throw unauthorized('账号已被禁用')`，与 login:49 的状态机对齐，同时覆盖 /auth/me 与 /auth/refresh 两条路径。",
    "reasoning": "是上一轮 ADMIN-02（软删除不吊销 JWT）的更深变体：ADMIN-02 = 既有 token 自然过期窗口（最长 7d）；AUTH-01 = refresh 主动为 DISABLED 用户签发新凭证，危害可无限期续期，禁用操作实质失效。属独立的更深新发现。"
  },
  {
    "id": "PPK-03",
    "title": "submitPluginToMarketplace 无法把插件从付费改回免费（0 被 || 吞掉）",
    "severity": "high",
    "category": "logic-bug",
    "target": "A6-plugin-package",
    "location": "O:\\lingfang-platform\\apps\\collab-api\\src\\modules\\plugin.service.ts:81 (submitPluginToMarketplace)；对照 admin.service.ts:217 正确写法",
    "description": "priceCents 取值用 `Math.max(0, Number(input.priceCents || plugin.priceCents || 0))`（第 81 行）。JS 中 0 是 falsy，当作者显式提交 priceCents:0 而原插件 priceCents>0 时，`0 || plugin.priceCents` 求值为 plugin.priceCents，新价格被静默丢弃。对照 admin.service.ts:217 用 `=== undefined` 正确区分未传与显式 0。前端 PluginCreatorHome.tsx:773 在每次 submitMarketplace 时硬编码 `body: { priceCents: 0 }`，证实生产必然命中。",
    "impact": "作者无法将自己已定价的插件改为免费重新提交审核；需平台管理员介入 adminUpdatePlugin 才能改价，业务流程不一致；桌面端创建者唯一上架通道被锁死。",
    "trigger": "插件已 priceCents>0，作者再次提交市场时 body 显式传 priceCents:0",
    "fix": "plugin.service.ts:81 改为 `input.priceCents === undefined ? plugin.priceCents : Math.max(0, Number(input.priceCents))`，与 admin.service.ts:217 对齐。",
    "reasoning": "全新独立发现。"
  },
  {
    "id": "AUTH-02",
    "title": "rejectApplication 缺少 status==='PENDING' 守卫，可对已 APPROVED/REJECTED 申请重复驳回",
    "severity": "medium",
    "category": "state-machine",
    "target": "A1-auth",
    "location": "O:\\lingfang-platform\\apps\\collab-api\\src\\modules\\admin.service.ts:236-241 (rejectApplication)；对照 auth.service.ts:139 (approve 有守卫)",
    "description": "createTeamForApplication（auth.service.ts:139）有 `if (application.status !== 'PENDING') throw conflict()` 守卫，但同源 rejectApplication 直接 update 无任何前置状态检查。对已 REJECTED 申请重复驳回会覆盖 reviewedBy/reviewedAt/reviewReason 污染审计轨迹；对已 APPROVED 申请（用户已成 TEAM_ADMIN、团队已建）执行驳回会把 application.status 改 REJECTED 但 Team/TeamMembership 留存。",
    "impact": "状态机非法转移 + 契约不一致：用户 onboarding 仍走 TEAM_ADMIN_SPACE（teamRole 优先于 application 分支），但 me.application.status 返回 REJECTED，前端展示与实际权限矛盾；审计日志重复写入。",
    "trigger": "平台管理员对任意非 PENDING 的 TeamAdminApplication 调用 POST /api/admin/team-admin-applications/:id/reject",
    "fix": "与 approve 路径对齐：先 findUnique，若 status!=='PENDING' 则 throw conflict('该申请已处理')，或直接 update({ where: { id, status: 'PENDING' } }) 利用 Prisma 更新 0 行判定。",
    "reasoning": "全新独立发现，与 ADMIN-02（软删不吊销 JWT）不同关注点。"
  },
  {
    "id": "AUTH-03",
    "title": "register 的 user.create 与 teamAdminApplication.create/audit 非原子，部分失败留孤儿用户",
    "severity": "medium",
    "category": "transaction",
    "target": "A1-auth",
    "location": "O:\\lingfang-platform\\apps\\collab-api\\src\\modules\\auth.service.ts:26-42 (register)；对照 :141-150 (createTeamForApplication 用 $transaction)",
    "description": "register 先 await user.create（已落库提交），再在 wantsTeamAdmin 分支分别 await teamAdminApplication.create 与 auditLog.create，三者无 $transaction 包裹。若 application.create 或 audit 抛错（DB 抖动、连接断开、IO 错误等瞬时故障），user 已持久化为 ACTIVE 但错误以 500 冒泡。对照同文件 createTeamForApplication:141-150 把全流程包进 $transaction，证明作者明知应包事务的不对称疏漏。",
    "impact": "客户端看到注册失败（500），但邮箱已占用；重试得 409 conflict，被迫改走 login（login 对 ACTIVE 用户正常放行）——用户实际已注册成功却误以为失败。产生孤儿 User 记录（无对应 application/audit），team_admin_application 审计链断裂，resolveOnboarding 落 NEEDS_INVITATION 而非 PENDING_APPROVAL。",
    "trigger": "注册时 wantsTeamAdmin=true，且在 user.create 成功后、application.create/audit 执行期间发生 DB 或运行时异常",
    "fix": "将 register 内 user.create + application.create + audit 包入 this.prisma.$transaction，与 createTeamForApplication 一致；或在 application/audit 失败时不冒泡而降级为普通用户注册成功。",
    "reasoning": "全新独立发现。"
  },
  {
    "id": "AUTH-04",
    "title": "JWT_SECRET 缺失时静默回退到公开已知默认值，无启动期断言",
    "severity": "medium",
    "category": "security",
    "target": "A1-auth",
    "location": "O:\\lingfang-platform\\apps\\collab-api\\src\\modules\\auth.service.ts:100-107 与 O:\\lingfang-platform\\apps\\collab-api\\src\\security.ts:23-24；main.ts 无启动断言",
    "description": "issueToken 与 jwt.verify 均用 `process.env.JWT_SECRET || 'dev-collab-change-me'`（auth.service.ts:104、security.ts:24）。该默认值明文出现在源码、.env.example 与 git 历史，属公开可猜测密钥。bootstrap()（main.ts:9-34）不校验生产是否设置 JWT_SECRET，也未 fail-fast。",
    "impact": "若部署时遗漏 JWT_SECRET，所有 JWT 用公开已知密钥签发/校验，知密钥者可为任意已知 userId 签发合法 token 绕过登录冒充该用户（认证绕过），并以该用户的 DB 角色获得对应团队级权限。非管理员提权（ensurePlatformAdmin 回查库防伪造 platformRole claim）。属配置层纵深防御缺失（无运行时护栏 + 公开默认值兜底）。",
    "trigger": "部署环境未设置 JWT_SECRET 环境变量即启动 collab-api",
    "fix": "main.ts bootstrap 内对生产环境 fail-fast 校验 JWT_SECRET 存在性；或移除源码内的公开默认值兜底。",
    "reasoning": "全新独立发现。原发现『完整鉴权绕过（伪造 platformRole）』被反驳：所有管理端权限经 ensurePlatformAdmin 回查 DB。"
  },
  {
    "id": "PPK-04",
    "title": "Express 默认 100KB JSON body 限制使 plugin-package 的 2MiB/256KiB 上传约束不可达",
    "severity": "medium",
    "category": "logic-bug",
    "target": "A6-plugin-package",
    "location": "O:\\lingfang-platform\\apps\\collab-api\\src\\main.ts:9-13 (无 bodyParser limit)；plugin-package.ts:45-47 (MAX 2MiB/256KiB)；docs/collab-api.md:119 宣称 2MiB",
    "description": "main.ts:10 `NestFactory.create(AppModule)` 未设置 bodyParser limit，NestJS 默认 Express body-parser json limit='100kb'（body-parser@2.2.2 源码已逐行核实 default 100kb=102400 字节）。任何超过 100KB 的上传请求体在进入 normalizePluginPackage 之前就被 Express 以 413 PayloadTooLarge 拒绝，MAX_PLUGIN_TOTAL_BYTES=2MiB、MAX_PLUGIN_FILE_BYTES=256KiB 永远无法触发。",
    "impact": "真实业务上 ~100KB 以上的插件包（HTML+CSS+JS 很常见）无法上传，作者得到非业务语义的 413 而非 400+明确提示；文档约束误导排障。单文件上限本身 256KiB 已 >100KB，故『完全无法上传』区间很大、极易命中。",
    "trigger": "上传一个 manifest+files 总字节介于 100KB~2MB 的合法插件包",
    "fix": "main.ts 改为 `NestFactory.create(AppModule, { bodyParser: { limit: '2mb' } })` 即可与 MAX_PLUGIN_TOTAL_BYTES 对齐。",
    "reasoning": "全新独立发现。原 finding 把版本说成 Express 4.x，实际 express ^5.2.1 + body-parser 2.2.2，但默认 limit 行为一致，不影响结论。"
  },
  {
    "id": "SCHEMA-01",
    "title": "WalletTransaction 缺 (userId, reason) 唯一约束致 signup_bonus 重复赠送",
    "severity": "medium",
    "category": "concurrency",
    "target": "A9-prisma",
    "location": "O:\\lingfang-platform\\apps\\collab-api\\prisma\\schema.prisma:271-283 (WalletTransaction 无 @@unique)；economy.service.ts:17-31 (ensureWallet 非原子 findFirst+create)",
    "description": "ensureWallet() 先 wallet.upsert 创建/读取钱包，再用 findFirst 判定是否已存在 signup_bonus 流水，命中缺失则在事务外 create 一条 CREDIT 流水。两个步骤不在同一事务内也无唯一约束：WalletTransaction 仅 @@index([userId, createdAt])。代码注释自称『幂等』但幂等性仅靠读后写实现，未由 schema 兜底。触发路径真实：desktop main.tsx:20 启用 React.StrictMode，Wallet.tsx:37-46 useEffect 双触发，新用户首次打开钱包即并发两 GET /api/wallet 各插一条。",
    "impact": "注册赠送流水可被重复写入，钱包流水出现重复 signup_bonus 行，财务对账失真；verify-economy.ps1 断言『存在该流水』不会失败但计数偏。无资金放大（wallet.upsert 因 @unique 原子，余额不变）。",
    "trigger": "同一新用户在首次访问钱包时发起 ≥2 个并发请求（如登录后前端同时拉 me、getWallet、refresh）",
    "fix": "给 WalletTransaction 加 signup_bonus 专用去重标记列（如 wallet.grantedSignupBonus Boolean，事务内 updateMany where grantedSignupBonus=false 返回 0 则跳过），或对 signup_bonus 用 INSERT...ON CONFLICT DO NOTHING。",
    "reasoning": "全新独立发现。"
  },
  {
    "id": "SCHEMA-06",
    "title": "Plugin.installCount 在重新启用安装时重复自增（计数失真）",
    "severity": "medium",
    "category": "data-consistency",
    "target": "A9-prisma",
    "location": "O:\\lingfang-platform\\apps\\collab-api\\src\\modules\\marketplace.service.ts:99-106 (install 事务)；schema.prisma:199 (Plugin.installCount)；对照 plugin.service.ts:138-144 正确写法",
    "description": "marketplace.install() 用 $transaction([upsert installation, plugin.update installCount+1])（99-106）：当 PluginInstallation 已存在走 update 分支，但同事务第二条 plugin.update 仍无条件 increment。create 与 update 两条路径都 +1，但语义上只有 create 才是真正『新安装』。对比 plugin.service.ts:138-144 的 installMarketplacePlugin 走了正确分支（existing ? update 无 increment : transaction+increment），唯独 marketplace.install 漏判。触发门槛比原报告更低：根本不存在把 PluginInstallation.status 置 DISABLED 的服务端路径，纯重试（同团队重复点 install）即触发。",
    "impact": "市场默认排序（search 默认 sort='installs'，按 installCount desc）失真，热门榜被反复重装/重试刷高；install_count 字段语义从『累计安装团队数』退化为『安装请求次数』。",
    "trigger": "同一团队对同一插件重复调用 POST /api/marketplace/install（用户重复点击/客户端重试）",
    "fix": "与 plugin.service.installMarketplacePlugin 对齐：先 pluginInstallation.findUnique，existing 走 update 不 increment，仅 create 走 $transaction(create+increment)。",
    "reasoning": "与 PPK-01 同处两条 install 路径，是 install 链一致性缺陷的另一面。属全新独立发现。"
  },
  {
    "id": "PPK-05",
    "title": "publicPlugin 向非作者团队成员泄露 reviewReason / reviewedById（审核内部态）",
    "severity": "low",
    "category": "security",
    "target": "A6-plugin-package",
    "location": "O:\\lingfang-platform\\apps\\collab-api\\src\\modules\\plugin-package.ts:199-201 (publicPlugin)；经 plugin.service.ts:56-71 (availablePlugins) 暴露",
    "description": "publicPlugin 无条件回显 reviewReason / reviewedById / reviewedAt（199-201）。availablePlugins 对团队成员返回本团队 visibility=TEAM 的插件（不论 reviewStatus，第一 OR 子句不过滤 reviewStatus），故被平台 REJECTED 的团队插件会带着 reviewReason（审核员拒绝理由）和 reviewedById（平台管理员 UUID）暴露给本团队非作者成员；对所有 APPROVED+PUBLIC 市场插件，reviewReason 虽清空但 reviewedById 仍对任意已登录用户可见。design.md:619 明确 reviewReason 目标受众是作者。",
    "impact": "内部审核状态（拒绝理由、审核/批准管理员 UUID）向非作者团队成员或任意市场浏览者泄露；损害有限（reviewedById 与 authorUserId 同级别敏感度，reviewReason 仅对同团队成员泄露且对 PUBLIC 市场插件恒为 ''），但与最小披露原则不符。",
    "trigger": "任意团队成员 GET /api/plugins/available（看到本团队 REJECTED 插件的拒绝理由+审核员 UUID），或任意已登录用户查看市场插件时经 available 看到批准管理员 UUID",
    "fix": "publicPlugin 对非作者场景下 reviewReason 仅回显给作者本人或 TEAM_ADMIN；reviewedById 不应在团队外（尤其 PUBLIC 市场浏览者）可见，或拆分作者态/团队态/市场态三套 DTO。",
    "reasoning": "与 PPK-02 同源于 publicPlugin 过度披露，是同一 DTO 的另一泄露维度。"
  }
]
