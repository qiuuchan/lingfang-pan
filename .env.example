# 复制为 .env 并按需修改（全部可选，不配置则用下列默认值）。
# 数据库默认使用内嵌 SQLite（单文件、零安装、随服务自动创建），无需任何外部数据库。
# 相对路径用单冒号写法；如需自定义到绝对路径用三斜杠：DATABASE_URL=sqlite:///绝对/路径/lingfang.db?mode=rwc
DATABASE_URL=sqlite:lingfang.db?mode=rwc
JWT_SECRET=dev-change-me
KEY_ENCRYPTION_SECRET=dev-change-me-please-32bytes-min
BIND_ADDR=127.0.0.1:8787
# 逗号分隔的前端来源白名单；留空时使用开发期 permissive CORS。
# 示例：CORS_ALLOWED_ORIGINS=http://localhost:1420,tauri://localhost,https://desktop.example.com
CORS_ALLOWED_ORIGINS=
RUST_LOG=info
# 平台审核员邮箱：启动时标记该用户为审核员（可进入「审核」页处理待审插件）。
# 留空亦可——首个注册用户会自动成为平台管理员。
PLATFORM_ADMIN_EMAIL=
