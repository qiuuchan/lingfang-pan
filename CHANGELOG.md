# 更新日志

本文件记录灵方桌面端面向用户的版本变更。

## v0.1.10 · 2026-07-31

本版本重点完善桌面端更新系统（下载体验、更新提示、健壮性），并修复一个会阻断安装包构建的配置问题。

### 改进

- **更新下载进度体验**：下载时进度条显示实时下载速度与预计剩余时间；安装包总大小未知时改用左右滑动动画表示「进行中」，不再显示固定 36% 的误导进度。
- **更新可用提示增强**：启动静默检查或手动检查发现新版本后缓存结果，进入设置页直接显示「发现新版本」横幅并可一键更新，无需重新请求后端检查。
- **下载失败可重试**：下载失败后「立即更新」按钮变为「重试下载」，直接重新下载，无需重新检查更新。

### 问题修复

- 更新下载改用连接超时 + 分段读取超时（替代旧的整体超时），大安装包在慢速网络下「慢但持续」的下载不再被误判为超时失败。
- 更新开始前清理临时目录历史残留的安装包，下载 / 校验失败时删除半截文件，避免磁盘占用累积。
- 修复 `tauri.conf.json` 残留 `buildInfo` 字段导致 `tauri build` 构建失败的问题（版本同步脚本不再向 tauri.conf.json 注入该字段）。
- 修复桌面端若干测试模块缺失导入导致 `cargo test` 无法编译的问题。

### 打包信息

- Windows 安装包：`release/LingFang-Setup-0.1.10.exe`（约 578.5 MB，606,598,076 bytes）。
- SHA-256：`9aaf04886dcce089872b739f0b30e3edff8af86089079c30edd76df39f5de738`。
- 安装包继续内置 Python、Node.js、FFmpeg、Chromium 等插件运行时，安装后可直接运行内置插件。

## v0.1.9 · 2026-07-29

### 新功能

- **新增音频生成能力**：RBFLow 插件支持音频克隆，并增强视频生成能力（含自动重试机制）。

### 改进

- **中继错误处理优化**：上游 401/403 统一映射为 502 以触发重试机制；`image.edit` 鉴权 / 余额错误不再无效重试。
- **新增版本同步脚本**（`scripts/sync-version.mjs`）：以根 package.json 为唯一版本源，统一同步各子包版本号并生成构建元信息。

### 问题修复

- 修复协作管理端渠道列表对异常响应兜底，避免 `setList(undefined)` 白屏。
- 修复协作管理端测试渠道对话框因列表项缺 `models` 崩溃。

## v0.1.8 · 2026-07-27

### 改进

- **插件与草稿页面列表支持分页**：插件中心「已安装 / 团队库 / 插件市场」三个列表，以及插件开发页「本地草稿 / 已发布」两个列表均改为分页展示，默认每页 5 条。
- **每页条数可自定义**：列表底部新增「每页 N 条」选择器，提供 5 / 10 / 20 / 50 四档；切换条数或筛选条件时自动回到第 1 页，数据变少时页码自动夹紧。
- **插件审核队列描述自动截断**：插件审核队列中过长的描述自动省略为最多 2 行，不再撑开卡片布局。

### 打包信息

- Windows 安装包：`release/LingFang-Setup-0.1.8.exe`（约 578.5 MB，606,589,793 bytes）。
- SHA-256：`c02c9bb52002fc63695dbeaf0cdc1981cb516c2d516b734bf3ada0e1020648f7`。
- 安装包继续内置 Python、Node.js、FFmpeg、Chromium 等插件运行时，安装后可直接运行内置插件。

## v0.1.5 · 2026-07-25

本次版本汇总近期桌面端、插件平台和视频能力更新，并修复插件发布工作台在窄窗口下的布局问题。桌面端各构建入口已统一到 0.1.5。

### 新功能

- **新增 RBFLow 动作迁移视频插件**：上传参考图片和视频即可生成动作迁移视频，支持多图片 × 多视频批量任务、任务队列排序、实时进度、失败重试和自定义输出目录。
  - 视频生成经桌面桥统一转发，RBFLow 地址和密钥只保留在平台侧，插件进程不会接触这些凭证。
  - 按参考视频时长使用 `PER_SECOND` 计费，后台新增视频能力的价目表和管理配置，并提供服务连通性测试。
- **创建器支持手动压缩上下文**：在上下文详情面板中可将较早对话合并为摘要，保留近期对话和插件包内容，降低长对话的上下文占用。

### 改进

- **视频插件持续升级**：补充视频时长探测和内置 `ffprobe` 路径处理，生成视频缩略图，优化任务面板、动态主题和已保存视频的打开方式；`rbflow-video` 更新至 0.3.5，并加入 `ai-video-mixer` 0.1.4 制品。
- **大插件制品兼容性提升**：插件单文件 / 制品大小上限调整为 1 GiB，发布和归档流程可处理包含大型视频、字体或音频资源的插件。
- **插件中心布局统一**：已发布插件行改为横向对齐，操作按钮不再挤到下一行；插件市场顶部「已安装 / 团队库 / 插件市场」Tab 改为横向等分排列，在窄窗口下仍保持可读。

### 问题修复

- 修复 `video.generate` 能力在发布校验中的白名单遗漏，避免视频插件发布时报「kind 不受支持」。
- 修复管理端下拉菜单层级不足导致的遮挡问题。
- 为已发布插件列表和插件中心 Tab 增加几何布局回归测试，并同步更新固定后端地址的测试断言，降低后续布局回归风险。

### 打包信息

- Windows 安装包：`release/LingFang-Setup-0.1.5.exe`（约 577 MB，605,048,678 bytes）。
- SHA-256：`bc74ae8974fcb9cdf7b4435b54b72148e95e588fb1bbfb8023f1707ecb587081`。
- 安装包继续内置 Python、Node.js、FFmpeg、Chromium 等插件运行时，安装后可直接运行内置插件。

## v0.1.1

灵坊桌面端跨入 0.1 系列。本次更新带来一个全新的「本地定时任务」能力：让应用按你设定的时间自动跑任务，不再需要每次手动点开。同时修复了若干已知问题。

### 新功能

- **新增「本地定时任务」**：参考 CodeBuddy WorkBuddy 的自动化功能，应用现在可以按指定时间自动执行任务，无需人工触发。从左侧栏新增的「定时」入口进入管理页即可使用。
  - **三种执行体，覆盖常见自动化场景**：
    - **Agent Prompt**：到点自动跑一段 AI 任务。适合每日 / 每周的内容生成与汇总，例如「每天 9 点生成昨日工作日报」「每周一汇总待我审查的 PR」。
    - **插件 Action**：自动调用已安装插件的某个能力（本版本为通道预埋，正式对接将在下个版本补齐）。
    - **系统通知**：仅弹一个桌面提醒。适合做简单的到点提醒，例如「下午 4 点提醒我同步进度」「晚上 8 点该下班了」。
  - **两种触发方式**：
    - **一次性**：指定未来某个时间点跑一次，跑完自动归档。
    - **周期性**：用 cron 表达式自由描述频率，例如 `0 9 * * 1-5`（工作日早 9 点）、`0 0 1 * *`（每月 1 号凌晨）、`*/30 * * * *`（每 30 分钟）。时区默认跟随系统，也可手动指定（如 `Asia/Shanghai`）。
  - **完整的管理页**：「定时」页支持新建、编辑、暂停、恢复、删除、立即运行、查看历史。每条任务都能看最近 200 条执行记录，包含状态（成功 / 失败 / 超时 / 跳过）、耗时、输出摘要或错误原因。
  - **Agent 可在对话中直接创建定时任务**：跟创建器对话时，自然语言说一句即可落地为定时任务——例如「每天早上 9 点帮我生成昨日工作日报」「每周五下午 5 点提醒我同步进度」。Agent 会自动调用 `ScheduleCreate` 工具按你描述的频率和时间生成任务；也支持 `ScheduleList`（查看现有任务）、`SchedulePause`（暂停）、`ScheduleResume`（恢复）、`ScheduleDelete`（删除）。
  - **每次执行都推送通知**：任务跑完后无论成功、失败、超时还是跳过，都会同时推送应用内通知与系统桌面通知，让你不在屏幕前也能第一时间知道结果。如果觉得太频繁，可在设置里配置「勿扰时段」（例如 `23:00–07:00`），勿扰时段内只保留应用内通知、不弹系统通知。
  - **失败红点提醒**：侧边栏「定时」入口在有任务执行失败且尚未确认时会亮起红点，进入页面后红点自动消除。
  - **与现有「关窗行为」联动**：如果你的关窗偏好是「询问」，且当前有正在运行的定时任务，关窗对话框会提示任务数量。选择「最小化到托盘」可让任务继续按计划触发；选择「直接退出」会暂停所有任务（且漏掉的不会补跑）。

### 说明与限制

- **任务仅在应用运行时触发**：定时任务依赖桌面端进程在线，关窗（未最小化到托盘）或退出应用会暂停所有任务。这期间错过的任务不会自动补跑，等下一个计划时间点继续。
- **串行执行 + 30 分钟硬超时**：同一时刻最多运行 1 个任务（避免资源与模型配额争抢）；如果某个任务还没跑完又到了下一个触发点，新到的会记为「跳过（previous run still active）」。单个任务最长跑 30 分钟，超时会自动终止并记为「超时」，同时推送通知。
- **插件 Action 通道为预埋**：本版本 `PLUGIN_ACTION` 类型的任务会直接记为成功并给出占位说明，真正对接插件能力调用通道将在下个版本补齐。Agent Prompt 与系统通知两种类型已经完全可用。

### 问题修复

- **修复安装后启动直接闪退的问题**：上一个安装包在启动时触发了 panic「there is no reactor running, must be called from the context of a Tokio 1.x runtime」——本地定时任务调度器在 Tauri 同步启动阶段直接调用了裸 `tokio::spawn`，而此时 Tokio runtime 还没初始化，导致进程一启动就崩溃。已改为复用 Tauri 自带的异步运行时启动调度器，并补齐了系统通知插件所需的权限声明。

## v0.0.29

### 问题修复

- **修复部分用户插件页报"服务内部错误"的问题（后端地址写死）**：此前若本机残留过旧的后端地址（IP:端口形式），桌面会优先用它、连到没更新迁移的旧服务，导致插件市场等接口 500。现已将后端地址写死为正式域名，忽略本机缓存与历史配置，所有请求一律走正式后端。升级后无需任何手动设置。

## v0.0.28

### 新功能

- **右键菜单：新增刷新 / 复制 / 粘贴 / 控制台**：在桌面任意位置右键，会弹出一个统一的主题化菜单（自动适配明暗模式），包含刷新页面、复制、粘贴，以及打开开发者工具（控制台）。此前右键只有 WebView 默认的那套（且发布版打不开控制台）；现在右键即可刷新页面、复制粘贴选中文本、随时开控制台排障。控制台在正式安装包里也能用。

## v0.0.27

### 变更

- **默认后端服务地址切换为正式域名 `https://lingfang.guiyuanzi.com`**：新安装或未手动配置过后端地址的用户，现在默认连接正式 HTTPS 域名，不再指向旧的 IP:端口地址。已手动设置过后端的用户不受影响（保留你已保存的地址）。

## v0.0.26

### 问题修复

- **修复图片编辑类插件（如 AI 换装）一调用就报 502、出不了图的问题**：v0.0.25 新上线的「参考图图片编辑」能力，桌面端把参考图转发给平台模型服务时，图片字段名误用了 `image[]`，而上游图片接口只认标准的 `image`，导致所有走图片编辑的插件（换装、换脸、风格迁移、海报生成等）每次调用都返回 502「上游模型调用失败」、生成不出图。现已改为标准的 `image` 字段名（多张参考图以多个同名 `image` 部分传递），图片编辑类插件可正常出图。配合本次发布，另有两个图片编辑插件（AI 详情页海报生成器、AI 换装批量版）可用。

## v0.0.25

### 新功能

- **新增「参考图图片编辑」能力，支持 AI 换装等图片编辑类插件**：插件现在可以调用平台大模型做「带参考图的图片编辑」——上传一张原图加一张参考图（或文字描述），即可生成换装、风格迁移、换背景等编辑结果，全程走平台计费、无需填任何模型密钥。桌面端随附一个「AI 换装」示例插件可直接体验：上传人物照和服装参考，生成换装效果图。这也是后续各类图片编辑插件的通用通路。

### 问题修复

- **修复启动插件时窗口卡死「未响应」、日志面板看不到安装输出的问题**：装好一个 Python 或 Node 插件后第一次点启动，需要先创建虚拟环境、安装依赖（pip / pnpm install，依赖多时可能跑几十秒到几分钟）。此前这段时间整个窗口会变成「未响应」——拖不动、点不了，转圈动画还在转，但日志面板里一行安装输出都刷不出来，得等依赖全部装完才一口气蹦出来。根因是这几个启动命令原本跑在应用主线程上，把界面事件循环堵死了，连「逐行实时输出」的事件也投递不出去（所以 v0.0.21 加的实时日志，在首次安装依赖这个最该看进度的场景下其实一行都看不到）。现已把耗时的环境准备挪到后台线程，主线程保持空闲——启动期间窗口随时可拖动、可切到别的插件，venv 创建、pip / pnpm 安装的每一行输出都实时流到日志面板。

- **修复发布体积较大的插件（如内嵌字体、音乐资源的短视频插件）时报「解析发布响应失败（HTTP 408 Request Timeout）」的问题**：上传大体积 .lfplugin 制品（可达数百 MB）时，若网络较慢、总耗时超过服务端默认请求超时（5 分钟），上传会在中途被掐断、返回 408；又因为该超时响应不是标准的 JSON 错误体，桌面端还会连带报一句「error decoding response body」、把真实原因盖住。现已放宽服务端对大上传的请求超时（运维可用环境变量调整），桌面端上传等待时间同步延长，并改进了失败提示——以后即便网关层返回非标准错误，也能直接看到真实原因，不再被二次解码报错误导。

## v0.0.24

这次更新是个「大件」：安装包现在自带了 Python、Node.js、FFmpeg、Chromium 浏览器四件套，装完就能跑所有插件，再也不用等运行时现下；插件的 AI 调用改成统一走平台托管（不用自己填密钥）；视频下载器加了新手引导；还修了更新安装时「文件被占用」的老毛病。

### 新功能

- **安装包自带完整运行环境，开箱即用**：以前装完灵坊后，第一次跑插件还得联网现下 Python、Node.js 这些运行环境（几十到几百 MB，国内网络下经常卡半天）。现在这些全打包进安装包了——Python 3.12、Node 22、FFmpeg 8.1、Chromium 149 浏览器全部内置，装完直接能用，断网也不怕。这也意味着安装包体积变大了（约 560 MB），但换来的是插件再也不会因为运行时下不下来而启动失败。
- **插件调 AI 统一走平台托管**：插件需要用大模型（写文案、生图等）时，不用再自己配 API Key 和地址了，全部走灵坊平台的模型服务。平台会自动管理计费、用量、安全审核——扫描插件源码，禁止偷连第三方模型接口、禁止硬编码密钥。换句话，团队管理员在后台开通模型权限后，所有插件就能直接用，不用碰任何配置。
- **视频下载器（videodl）升级到 v0.2.0**：
  - 首次打开会弹图文教程，手把手教你三步下载（粘链接 → 点解析 → 选好下载），看完点「开始使用」就行，之后随时点右上角「使用帮助」能再看。
  - 自动记住你上次选的下载文件夹和粘过的链接，下次打开直接填好。
  - 日志加了颜色：下载成功是绿色、失败是红色、警告是黄色，一眼看出结果。
  - 界面美化了：按钮有 hover 效果、列表圆角、标题加粗放大。

### 问题修复

- **修复更新/重装时「文件被占用」报错（os error 32）**：以前覆盖安装或更新时，如果灵坊还在运行（或者它拉起的 Python、Node、FFmpeg 子进程还在后台没退），会报「文件被占用」装不上。现在安装前会按安装目录自动找到并关掉所有相关进程（包括子进程），等文件锁释放后再装，不再靠固定的等待时间赌运气。
- **修复管理后台页面和 Docker 部署的若干问题**。

### 内部改进

- **插件包管理重构**：本地插件包的导入、校验、安装流程整体重写（v4 制品格式），打包工具也升级了，二进制文件直接存原始数据、ZIP 结构更标准、零外部依赖。
- **插件创建工作区重构**：用 AI 对话创建插件的工作区界面和逻辑做了整理。
- **运行时按需解析**：虽然运行时都内置了，但解析逻辑改为统一入口、只查应用内置目录、不碰系统 PATH，避免装了多个 Python/Node 版本时串台。

## v0.0.23

### 问题修复

- **修复 FaceFusion 模型下载仍走 GitHub（国内极慢）的问题**：v0.0.22 声称把模型下载源切到了国内镜像 hf-mirror.com，但实际没生效——上游 facefusion 的 `download_providers` 默认值是 `['github', 'huggingface']`，按顺序先 ping GitHub，GitHub 通了就直接用，hf-mirror 永远轮不到。`facefusion.ini` 里虽写了 `download_providers = huggingface`，但该配置在插件安装目录下未被正确读取，回退到全量默认值。现已改为在启动命令行显式传 `--download-providers huggingface github`（hf-mirror 优先，GitHub 兜底），命令行参数优先级最高，直接钉死下载源顺序。修复后模型下载速度从 50–150 kB/s 提升到镜像正常速度。
- **修复 FaceFusion hash 文件下载被跳过导致校验失败（`validating hash for nsfw_* failed`）**：上游 facefusion 的 `conditional_download` 用 HEAD 请求拿 Content-Length 决定是否需要下载。hf-mirror 对文件走 xet CDN（302 重定向），HEAD 在重定向环境下拿不到 content-length（返回 0），于是 `initial_size(0) < download_size(0)` 为 False，**直接跳过下载**——hash 文件根本没创建，校验永远失败。已修复 `download.py`：`download_size == 0` 时仍发起下载（让 curl 自己跟随重定向完成传输），并改用进程轮询替代纯大小比较来判断下载完成。
- **修复 FaceFusion yoloface_8n 等模型校验失败导致启动退出（退出码 2）**：若 `.assets/models/` 里有之前（github 慢速下载 / 中断）留下的残缺 `.onnx`，其大小 >= 错误的 download_size，facefusion 就会跳过下载，直接 hash 校验失败退出。现已在启动上游前扫描所有 `.onnx`：对每个有对应 `.hash` 文件的模型做 crc32 校验，不过就删掉残缺文件强制重下。
- **FaceFusion 启动失败自动重试**：模型下载/验证失败（退出码 2）时，自动清理残缺的 `.onnx` 和 `.hash` 文件后重新启动上游，最多重试 3 次。hf-mirror 偶发抖动（xet CDN 不稳定、签名过期）不再导致用户卡在首次启动。
- **修复 FaceFusion 模型文件删除时 WinError 32（文件被占用）**：curl 子进程下载完成后可能未完全释放文件句柄，导致 facefusion 删除校验失败的残缺 `.onnx` 时报 `PermissionError: [WinError 32]`。已在 `download.py` 中确保 curl 进程完全退出（`wait` + `kill` 兜底）后再继续，避免文件锁竞争。
- **FaceFusion 启动前预检导入**：在拉起上游 facefusion 子进程前，先执行 `import facefusion.core` 预检。若 venv 依赖损坏（cv2/numpy/onnxruntime 缺失或损坏）或 vendor 目录不完整，预检会打印**完整的 Python traceback**（而非被日志面板截断），并给出具体的修复建议（删除 venv 目录让平台重建）。

## v0.0.22

本次更新主要做了三件事：大插件上传现在有实时进度条（百分比 + 速度 + 已传大小）、FaceFusion 在别的电脑上启动失败的问题修好了、插件报错退出后日志面板不再清空。

### 新功能

- **大插件上传显示进度**：上传插件到团队 / 市场时，现在会弹一个进度对话框，实时显示上传百分比、传输速度、已传 / 总大小。底层改用 Rust 流式上传（`reqwest` + `Channel`），上传几百 MB 的插件也能看到进度条一点点走，不再是点了上传后干等半天不知道发生了什么。

### 问题修复

- **修复 FaceFusion 在别的电脑上启动报错退出**：之前 FaceFusion 插件换到别的电脑上启动会直接退出（退出码 2），原因是模型权重没随插件包带过去，每次都要从 GitHub 重新下载 1.3 GB——国内网络下经常下到一半超时失败。现已把模型下载源切换到国内镜像 hf-mirror.com，下载稳定得多；插件退出时也会打印明确的诊断提示，告诉你是不是模型下载的问题、怎么排查。
- **插件退出后日志面板不再清空**：之前插件进程一退出，启动页的日志面板就跟着消失了，报错信息全没了，想看 traceback 还得去翻文件。现在改成退出后日志面板一直保留，还显示退出码和进程信息，异常退出会标红并保留「让 AI 修复」按钮。不管是秒退、跑一半崩了、还是正常结束，输出都在那儿，不会丢。
- **修复 2048 小游戏启动报错**：内置的 2048 小游戏插件之前一启动就报 `require is not defined` 直接退出，原因是它用了 CommonJS 写法但被当成了 ES 模块。已修复，现在可以正常启动并在浏览器里打开了。

### 后端

- **上传接口体积上限提高到 300 MB**：配合大插件上传，后端 JSON body 解析上限从 2 MB 提到 300 MB（与插件包单文件 60 MB / 总计 300 MB 的限制对齐），此前上传超过 2 MB 的插件会直接报「无法连接后端」。

## v0.0.21

本次更新重点解决了插件「启动后看不到任何输出」和「依赖 FFmpeg 的插件无法运行」两大痛点：插件启动的全过程（创建虚拟环境、安装依赖、运行入口）现在逐行实时显示在应用内的日志面板；FFmpeg 也作为内置运行时随安装包分发，插件不再需要用户在系统上单独安装。同时修复了 facefusion 首次启动打开空白页的问题。

### 新功能

- **插件启动全流程输出实时显示**：启动 Python / Node 插件时，创建虚拟环境（venv）、安装依赖（pip / pnpm install）、运行入口（python main.py / npm start）的全部输出——包括进度、警告、错误——都会逐行实时显示在启动页面的日志面板里，而不是只看到转圈动画。这样无论是依赖下载慢、安装冲突、还是代码报错，都能直接看到原因。闪退时日志面板保留完整 traceback，不再需要独立控制台窗口。
- **内置 FFmpeg 运行时**：应用现在内置 FFmpeg 8.1.2（和内置 Python / Node.js 一样随安装包分发）。所有插件进程的 PATH 自动包含内置 FFmpeg 目录，插件代码里的 `shutil.which("ffmpeg")` 会直接找到内置版本，**不再需要用户在系统上单独安装 FFmpeg**。facefusion（换脸）、videodl（视频下载）等依赖 FFmpeg 的插件开箱即用——此前这些插件因为沙箱清空了宿主机 PATH、又找不到内置 FFmpeg，启动后会报 `ffmpeg is not installed` 直接退出。

### 问题修复

- **修复 facefusion 启动后网页空白**：facefusion 首次启动需要从 GitHub / HuggingFace 下载约 1.4 GB 的模型权重，Gradio 界面在模型下载完成之后才会监听端口。此前插件在启动 4 秒后就打开了浏览器，此时模型还在下载、界面尚未就绪，用户看到的是空白页。现已改为轮询端口就绪后再打开浏览器，并将子进程的下载进度实时转发到日志面板，用户可以清楚看到「正在下载 xxx 模型」的进度。
- **修复启动日志面板一直显示「等待输出」**：`plugin:output` / `plugin:start-progress` 事件的 payload 字段未转驼峰（仍是 `plugin_id`），前端按 `pluginId` 过滤时全部静默失效，导致上一条「全流程输出实时显示」实际在已打包版本里一行也显示不出来。现已为事件结构补上驼峰序列化，日志面板能正常流式显示 venv / pip / python 的每一行输出。
- **Python 插件依赖损坏自动修复**：少数情况下（杀毒软件拦截、磁盘写入中断），pip 安装出来的包文件会被写坏（例如 streamlit 的源码文件混入空字节，启动时报 `SyntaxError: source code string cannot contain null bytes`）。此前应用只检查 pip 是否装成功、检测不到这种落盘后的损坏，导致插件永远启动失败，只能手动删除虚拟环境目录。现在启动前会对依赖做一次导入自检，发现损坏会自动重建环境并重新安装；正常启动过一次的插件会缓存校验结果，后续启动不增加额外开销。
- **修复 Windows 插件启动即崩溃且无任何输出**：为安全起见，插件进程的 PATH 此前只包含内置运行时目录、清空了系统目录，但这导致 Python / Node 启动时找不到依赖的系统 DLL（如 VCRUNTIME）而立即崩溃，连一行错误都输不出来。现在 PATH 会补回 `System32`、`Wbem`、`WindowsPowerShell` 三个必需的系统目录，既保持安全隔离又不会让进程起不来。
- **插件秒退时生成崩溃转储**：插件启动后立即退出时，除了错误提示，还会在插件目录的 `data/.crash.log` 写入完整诊断——包括可直接复制执行的复现命令、工作目录、环境变量（敏感值已脱敏）以及进程 stderr 输出。错误信息里也会附上这个文件路径和复现命令，方便直接在那台机器上手动重跑定位真正的报错。
- **新增启动流水线日志**：插件目录下的 `data/.launch.log` 现在会记录每次启动的各个阶段（环境检查、依赖安装、进程拉起）及其成功或失败原因，并带时间戳。排查「插件起不来但没线索」时，可以直接看这个日志定位卡在哪一步。

### 注意事项

- **facefusion 插件需重新导入**：本次修复了 facefusion 启动后网页空白的问题，但该修复在插件本身的 `main.py` 中，不随安装包自动更新。已安装旧版 facefusion 的用户请在插件列表中删除后，重新导入新版 `facefusion.lfplugin`。
- **facefusion 首次启动需下载模型**：facefusion 首次运行会从 GitHub / HuggingFace 下载约 1.4 GB 的模型权重（仅一次，后续启动直接使用缓存）。下载进度会显示在日志面板，请耐心等待；模型下载完成后 Gradio 界面会自动在浏览器中打开。
- **安装包体积增大**：因内置 FFmpeg 运行时，安装包体积由约 89 MB 增至 267 MB。

## v0.0.20

本次更新围绕插件管理做了一次补全：本地插件与草稿插件现在都能导出为压缩包、从压缩包导入，本地插件也支持发布到团队空间。同时附带一个示例视频下载器插件，以及几处 Agent 稳定性修复。

### 新功能

- **本地插件一键发布到团队**：本地插件列表每一行新增「发布」按钮，可直接把插件上传到团队空间；之后在「团队插件」里提交市场审核即可上架，无需再进 AI 创建器。
- **插件导出为 `.lfplugin` 压缩包**：本地插件与草稿插件都支持导出。点击「导出」会下载一个 ZIP 压缩包，内含完整源码与清单，方便备份、迁移或分享给同事。
- **从压缩包导入插件**：插件中心的「本地插件」和「我的草稿」都新增「导入」入口，选择 `.lfplugin` 压缩包即可装入。导入时按来源保持——本地导出的包装回本地插件，草稿导出的包装回草稿，ID 冲突会自动改名，绝不覆盖现有插件。
- **示例插件：视频下载器**：基于 [videofetch](https://github.com/CharlesPikachu/videodl) + PySide6 的视频下载 GUI，支持 B 站、抖音、YouTube、快手、小红书等 100+ 平台。HLS 源需自行安装 FFmpeg。
- **插件运行弹独立控制台窗口**：启动 Python / Node 插件时，Windows 上会弹出一个 cmd 控制台窗口，实时显示插件的完整运行日志（标准输出与错误流）。进程退出后窗口**不会自动关闭**，会显示退出码并等待按键，方便排查崩溃原因。退出时应用内也会同步展示错误摘要，带「让 AI 修复」按钮一键定位修复。
- **`.lfplugin` 支持二进制大包**：导入导出格式升级到 v3，压缩包现在能完整承载字体、图片、音频等二进制文件（base64 编码），上限提高到单文件 60 MB、总计 300 MB、最多 300 个文件。这意味着把含 vendored 上游源码（如 FaceFusion、Pixelle 等）的大插件整体打包、迁移成为可能。

### 问题修复

- **Agent 输出不再重复**：工具调用跨轮次时，AI 不再把上一轮已经输出过的内容重复吐一遍，正文更干净。
- **上游截断可感知**：检测到中转服务返回被 `max_tokens` 截断时不再静默吞掉，会明确提示，避免半截结果被当成完成。
- **工具参数解析错误可见**：AI 生成的工具调用参数格式有误时，错误会回灌给模型重试，而不是静默用空参数执行导致工具「什么都没做」。
- **草稿加载容忍二进制文件**：草稿目录里若有图标等二进制文件，加载时不再整体失败，改为跳过二进制继续加载其余文本文件。

### 技术改进（开发者可见）

- **`.lfplugin` 格式升级到 v3（ZIP + 二进制）**：旧的 JSON 单文件格式（v1）已废弃，现在是标准 ZIP 压缩包（v2 为纯文本、v3 增加二进制 base64 承载）。结构清晰（`_meta.json` 标识 + `manifest.json` + 源文件，二进制文件在 `_meta.binaryFiles` 名单内），便于跨工具流通。v2 包仍可读取导入（向后兼容）。
- **本地插件上传复用创建器通道**：本地上传走已有的 `submitStagedPlugin` → `/api/plugins/upload`，市场审核复用团队行的 `PluginSubmitDialog`，不重复造市场提交流程。

### 发布

- **版本号统一为 0.0.20**：应用、安装器和 Cargo.lock 都已更新到同一版本。

## v0.0.19

本次更新对插件创建 Agent 系统做了一次彻底重构。旧的 AI 对话引擎（基于第三方 SDK 的适配层）已完全替换为灵坊自研的轻量循环，修复了多个长期困扰的稳定性问题，对话也更聪明、更耐用了。

### 问题修复

- **修复草稿编辑读不到文件**：从「我的草稿」点编辑进入创建器后，AI 现在能稳定读取到插件文件，不再提示「当前还没有插件」而重新创建一个。根因是插件目录绑定逻辑存在竞态（写盘未完成就扫描磁盘），现已用原子化绑定彻底修复。
- **修复右侧草稿面板不显示**：编辑草稿跳转后，右侧预览面板能立即显示插件信息，不再出现空白。兜底逻辑已强化：即使磁盘扫描失败，只要插件携带了文件就会构建预览。
- **修复上下文窗口严重错配**：此前压缩阈值硬编码为 5000 字符（约 3300 token），而后台配置的窗口是 256000 token——只用到了 1.3% 就开始丢弃历史对话。现已改为从后台实时读取真实窗口，256K 窗口下约到 27 万字符才触发压缩，长对话不再轻易丢失早期需求。
- **修复上下文读取权限**：旧版本通过管理员专用接口读取上下文窗口，普通用户会因权限不足而拿不到值。现已改用普通用户可访问的接口。
- **减少工具重复调用**：续跑或修改需求时，AI 不再把已完成的工具调用结果丢掉重跑。改用原生的函数调用历史格式（带工具调用编号和返回值），模型能精确读到「上次搜过什么、结果如何」，直接从断点继续。

### 优化

- **对话更耐网络波动**：遇到限流（429）或上游波动（503）时，AI 会自动指数退避重试，不再动不动就报错让用户手动点重试。只有真正的连接失败才提示。
- **失控保护**：单个对话的工具调用轮次现在有软上限（40 轮），防止 AI 陷入死循环（如反复搜索同一个词）。到上限会给出友好提示，可点重试从上次进度继续。
- **创建器界面更清晰**：AI 的思考过程、正文、工具调用、提问卡片现在按时序更整齐地链式展示，暗色模式下也更易读。

### 技术改进（开发者可见）

- **完全移除第三方 Agent SDK**：不再依赖 `@openai/agents` 和 `@openai/agents-extensions`，改为自研约 400 行的轻量 agent 循环，直接对接灵坊中转服务，协议更干净，消除了旧适配层的多处脏类型断言。
- **插件状态单一真相源**：引入状态管理 store，把原本散落在 5 处的插件 ID 副本收口为单一字段，消除了多处状态不同步的隐患。
- **创建器组件拆分**：悬浮创建器从 1867 行拆分为多个独立子组件（消息列表、提问卡片、历史弹窗、技能弹窗），后续维护更清晰。
- **后端暴露上下文窗口**：中转服务的模型列表接口现在返回每个版本的真实上下文窗口大小，供桌面端动态调整压缩策略。

### 发布

- **版本号统一为 0.0.19**：应用和安装器都已更新到同一版本。

## v0.0.17

本次更新重点打磨「灵坊工作台」的插件开发体验。AI 创建插件更顺手，遇到问题时更容易修复，暗色界面也更清爽耐看。

### 新功能

- **插件创建输入栏更完整**：底部输入栏现在支持方案模式、执行模式、快速 / 高级选择、技能选择、引用插件、上传文件、优化提示词和语音输入。
- **一键优化提示词**：点击星星按钮后，AI 会把当前需求整理得更清楚，再放回输入框，方便你确认后发送。
- **上传文件给 AI 参考**：点击 `+` 可以选择文本文件或文件夹，让 AI 在本次对话里参考这些内容；原有「导入为插件」能力仍然保留。
- **随时查看 AI 将参考的内容**：即使还没开始对话，也可以打开查看窗口，确认当前输入、已选插件、上传文件和剩余可用空间。
- **创建后自动记住插件文件夹**：AI 创建或修复插件后，会记住对应的插件文件夹，后续继续修改时不用再手动选择文件。
- **选择已有插件继续改**：输入栏里的「选择插件」会打开最近插件列表，选中后 AI 可以直接在这个插件基础上继续修改。
- **运行失败后更方便修复**：插件运行失败时，点击「让 AI 修复」会直接带上出错插件，AI 可以更准确地定位并修改问题。

### 优化

- **底部输入区更统一**：输入框和工具栏合成一个灰色圆角区域，按钮颜色更克制，整体更像现代桌面应用。
- **顶部栏更精致**：应用标题、运行 / 开发切换和窗口按钮都做了细节调整，看起来更稳、更干净。
- **对话内容更好读**：运行结果、检查结果和任务清单之间的间距更舒适，暗色模式下的文字也更清晰。
- **查看窗口更清楚**：AI 会参考哪些内容、还剩多少可用空间，现在看起来更直观。
- **窗口按钮更好点**：最小化、最大化、关闭按钮的点击区域更大，鼠标悬停反馈更明显。
- **安装器标题自动跟随版本**：后续发版时，安装器显示的版本号会自动更新，减少漏改。

### 问题修复

- **修复 AI 无法读取插件文件的问题**：从「让 AI 修复」进入开发页后，现在会自动带上正确的插件文件夹，不再提示「当前还没有插件」。
- **修复回复内容重复显示**：AI 回复过程中，部分文字偶尔重复出现的问题已修复。
- **修复语音输入重复追加**：同一句语音识别结果不会再被重复写入输入框。
- **减少本地记录占用**：上传文件的完整内容只在本次对话中使用，不再长期塞进历史记录。
- **插件启动失败提示更有用**：插件一启动就退出时，现在会显示更多可排查信息，方便判断是文件、入口还是运行方式出了问题。
- **移除容易误导的等待提示**：删除「网络较慢」提示，避免正常等待时让人误以为出错。

### 发布

- **版本号统一为 0.0.17**：应用和安装器都已更新到同一版本。
- **Windows 安装包已生成**：本次提供标准安装包和自定义安装包，方便不同发布渠道使用。
  - 标准安装包：`release/LingFang-0.0.17-Setup.exe`，约 50.7 MB，校验码 `80B489DF80B462B10D3298E13FED5E39A89161F5259E20976147F6177019664A`
  - 自定义安装包：`release/LingFang-Setup-0.0.17.exe`，约 86.6 MB，校验码 `134DDB4507E29F44E71D9F2C403FEFDFA98F73CC6B9844C6981542C28D2A123C`

## v0.0.16

本次更新全面重构了插件页面的视觉和交互，把「运行插件」和「开发插件」拆成两个独立工作区，并大幅打磨了 AI 创建器的输入体验。

### 新功能

- **插件页拆成「运行 / 开发」两个独立工作区**：顶部标题栏新增分段切换器，一键在运行插件（浏览、固定、启动）和开发插件（AI 对话生成）之间切换，切换时有全屏过渡动画提示「正在切换工作区」。
- **AI 创建器重构为 Claude 风格工作台**：左侧垂直导航栏直接显示历史对话（不再需要点弹窗），底部居中圆角输入框，下方提供代码 / 学习 / 创作 / 写作 / 生活五类例子卡片，点击即可填入需求。
- **输入框改为 Agent 风格工具栏**：参考 ChatGPT / Claude 的布局，输入区更高更宽敞，底部一行统一排布思考模式、附件、@ 引用已有插件、上下文查看、模型切换和发送按钮。模型切换从文字改为图标（闪电 = 快速、皇冠 = 高级）。
- **@ 引用已有插件**：在输入框直接点 @ 即可选择一个已有插件注入源码到上下文，让 AI 在其基础上做增量修改，不再需要去侧边栏找。
- **两个侧边栏统一风格**：运行插件和开发插件的侧边栏现在共用同一套样式，都支持折叠 / 展开（折叠后以图标方式显示对话和功能入口），开发插件侧边栏底部也增加了用户菜单入口。

### 优化

- **标题栏加高美化**：标题栏从 36px 加高到 56px，「灵坊工作台」标题字更大更醒目，侧边栏折叠按钮也同步放大。窗口最小化 / 最大化 / 关闭按钮从贴边满高改为紧凑圆角按钮，更精致。
- **UI 色彩和动画精简**：整体配色改用自适应语义 token（深浅主题自动跟随），所有过渡动画时长缩短，页面切换简化为纯淡入，操作更干脆利落。
- **模式切换器视觉增强**：运行 / 开发切换按钮增加了边框外壳，未选中的按钮有灰色底色，选中状态更突出。
- **空状态例子卡片**：开发插件页在没有对话时展示五张带图标的例子卡片，悬停有上浮和图标变色反馈，点击直接开始。

### 问题修复

- **修复了浏览器自动化类插件首次启动失败的问题**：使用了浏览器自动化（如 Playwright）的插件，之前启动时会立刻退出并报「找不到浏览器」——因为平台只装了 Playwright 这个库，却没下载它实际要用到的浏览器内核。现在平台会在启动这类插件时自动补下载所需的 Chromium 内核（走国内镜像，速度正常），装完即可正常运行，后续启动会自动跳过重复下载。

## v0.0.15

本次更新重点打磨了插件中心的使用体验、给插件补齐了真实可用的能力，并让 API Key 能一键验证是否好用。

### 新功能

- **团队插件也能直接运行了**：团队插件现在和本地插件一样，有「运行 / 停止 / 查看信息」等按钮；未安装的脚本类插件会提示「安装后运行」，不再点进去只看到一堆代码。
- **草稿一键接着改**：在「我的草稿」里点「编辑」，会自动打开 AI 创建器并加载之前的对话和文件，不用再手动找右下角的 + 按钮。
- **API Key 能即时测试**：新建 API Key 后当场就能点「测试对话（say hi）」或「测试生图」，直接用这个 Key 调一次平台，看看回复内容或生成的图片，马上知道 Key 好不好用。
- **插件能写文件、用剪贴板、能截屏了**：补齐了三个之前只声明却用不了的能力——`fs.write`（写文件）、`clipboard`（读写剪贴板）、`system.screenshot`（截屏）。其中截屏会先弹窗问你是否允许，保护隐私。
- **脚本插件也能生图了**：修复了 Node.js / Python 脚本插件无法调用生图能力的问题，现在对话和生图在所有插件类型里都能用。
- **新增 AI 能力演示插件**：内置一个示例插件，演示脚本插件如何经平台安全调用大模型对话和生图，方便开发者参考。

### 优化

- **关于页更清爽**：移除了「关于」里重复的版本通道卡片（Beta / 正式版切换统一在「更新」里），不再两处显示让人混淆。
- **插件能力对齐契约**：清理了已废弃的 `code-assistant` 能力（旧本地 AI 助手残留，实际从未生效），现在契约里声明的每个能力都有真实实现，不再误导插件作者。

### 发布

- **版本统一到 0.0.15**：桌面端、Tauri 配置、安装器版本号同步更新。

## v0.0.14

本次更新让 AI 做插件更靠谱（能自己测试、自己改错），使用体验更顺手，并修复了一批问题。

### 新功能

- **AI 会自己测试插件了**：AI 写完代码后会自动运行一遍，如果跑不起来就根据报错自己改，改好再跑，直到插件能正常用。你不用再担心「看着没问题但实际跑不起来」。
- **AI 更聪明了**：AI 现在会自己整理代码（删文件、移动文件、搜索代码），改完插件还会自动更新版本号。
- **插件能力自动识别**：AI 会检查代码里用了哪些功能（比如联网、读写文件），自动补全所需权限，避免插件装好后用不了。
- **插件更新更方便**：作者更新插件后直接生效，不用等管理员审核；已安装的用户会看到「更新」按钮，点一下就能升到最新版。
- **复制对话内容**：把鼠标放到任意一条对话上，右上角会出现复制按钮，点一下就能复制这条消息。

### 优化

- **任务清单更好看了**：AI 干活时的任务进度条挪到了底部，默认收起只显示概要（完成几个、在做什么），点开能看明细，对话区更清爽。
- **上下文用量一目了然**：新增了用量仪表盘，能直观看到当前对话占了多少空间、离自动整理还有多远。
- **插件管理更顺手**：本地插件多了能翻页看，还能勾选多个一起删除。
- **安装体验提升**：安装界面会显示版本号；如果安装时软件还开着，会提醒你先关掉再装，避免装失败。
- **长对话更流畅**：优化了长对话的处理方式，聊很久也不会变卡。

### 修复

- **后台统计修复**：修好了后台「AI 生成质量」的调用次数统计（之前一直显示 0）。
- **关闭插件状态修复**：修好了关掉运行中的插件后界面还显示「运行中」的问题。
- **安全修复**：修补了几个安全漏洞，使用更放心。
- **其他修复**：修好了复制按钮在 AI 回复时点不了、插件更新后别人看不到等问题。

### 发布

- **版本统一到 0.0.14**：桌面端、Tauri 配置、安装器版本号同步更新。

## v0.0.13

本次更新新增代码试运行能力、优化长对话体验，并修复多个底层稳定性与安全问题。

### 新功能

- **代码试运行**：创建 Node.js 或 Python 插件后，AI 会自动试运行插件并查看运行结果。若运行报错，AI 会根据错误信息自动修复并重试，直到插件能正常跑起来，确保交付的插件是可运行的。

### 优化

- **长对话更省资源**：优化了上下文管理，对话过长时不再把全部文件内容重复塞给模型，改为按需查看，长任务下更流畅、更省用量。
- **后台调用统计恢复**：修复了后台「AI 生成质量」看板调用次数一直为 0 的问题，现可正确统计模型调用次数、成功率与平均耗时。

### 修复

- **安全加固**：堵住了网页抓取可能被用于探测内网的风险，并修复了余额耗尽后仍可调用模型的计费漏洞。

### 发布

- **版本统一到 0.0.13**：桌面端、Tauri 配置、安装器版本号同步更新。

## v0.0.12

本次更新修复对话调用失败的问题，并优化了错误提示。

### 问题修复

- **修复快速版对话调用失败**：使用快速版与 AI 对话时会提示「调用失败」，导致无法正常使用。现已修复，对话功能恢复正常。
- **优化错误提示**：当模型调用出现问题时，提示信息更加清晰，不再显示无意义的错误代码。

### 发布

- **版本统一到 0.0.12**：桌面端、Tauri 配置、安装器版本号同步更新。

## v0.0.11

本次更新精简创建器界面、修复复杂插件创建失败问题，并引入 UI 自动化测试体系。

### 问题修复

- **修复复杂/大文件插件无法创建**：创建含多文件、大段源码（如 400 行以上的 Node.js 脚本插件）时，AI 会放弃工具调用、改为直接吐出代码，并提示「工具调用参数存在问题」。根因是 AI 尝试把全部文件内容一次性塞进 `CreatePlugin` 工具，生成的参数 JSON 过长被截断或转义出错，触发工具调用失败。已从三处根治：
  - **创建流程改为分步**：`CreatePlugin` 现在只初始化最小骨架（入口文件占位 + 依赖描述），完整源码由 AI 随后用 `Write` 分文件逐个写入，单次工具调用的参数体量始终可控，不再因过长而失败。
  - **文件内容入参容错**：工具接收文件内容时，对 AI 偶尔传成的对象/嵌套/畸形结构自动归一化为字符串（此前严格校验会直接报错），与联网搜索工具已验证的容错策略对齐。
  - **工具失败不再静默**：此前这类工具调用失败会被误判为成功（卡片显示绿色），用户难以察觉。现已让此类失败正确标红可见。

### 优化

- **移除创建器草稿预览区**：AI 插件创建器右侧面板移除顶部的实时预览区（iframe 预览 + 刷新按钮 + 不可预览提示）。该预览在 iframe 沙箱内用 sdk shim 模拟运行，与发布后真实环境差异大、参考价值有限，反而占去大量纵向空间。移除后右侧面板直接展示「插件信息」编辑区与提交/保存栏，信息更聚焦；创建器在有草稿时的窗口宽度同步收窄（1540px → 1320px），布局更紧凑。
- **信息编辑与提交流程保留**：移除的仅是预览区，插件名字/描述/ID/版本/运行类型/入口/能力/可见性编辑、结构检查结果、提交到团队空间与保存草稿到本地等核心功能完整保留。

### 内部

- **UI 自动化测试**：搭建 Playwright 测试体系（29 个用例 + 截图基线），覆盖 ToolCallCard 三态、TodoPanel 进度/优先级、Markdown 渲染（代码块/表格/列表）、对话气泡左右对齐、思考块等界面元素。采用「集成测试 + 组件白板挂载」双层架构，通过系统 Chrome 运行，新增 `test:e2e` / `test:e2e:update` 脚本。

### 发布

- **版本统一到 0.0.11**：桌面端、Tauri 配置、安装器版本号同步更新。

## v0.0.10

本次更新聚焦 AI 插件创建器的联网搜索可靠性、任务清单跟踪、弱网体验与应用信息展示。

### 新功能

- **任务清单（Todo）**：复杂多步任务（≥3 步、多文件改动）由 AI 自动拆解为可勾选清单，进度随会话持久化，刷新或切回历史对话仍可见；强制「同一时间至多一项进行中」避免进度混乱。
- **日期时间工具（DateTime）**：AI 现在能获取真实的当前日期与时间（含星期、时区、ISO 日期），不再凭训练数据猜「今天」是哪天。用户说「今天/最近/本周」时能正确理解。
- **网页正文抓取（WebFetch）**：新增 WebFetch 工具——WebSearch 只返回搜索摘要，很多问题的答案在网页正文里（如新闻全文、文档原文）。WebFetch 经后端抓取网页正文（Jina Reader 正文抽取 + markdown 化，Jina 不可达时降级直接抓 HTML 去标签）。典型用法：WebSearch 找链接 → WebFetch 抓正文。
- **联网搜索兜底源**：后端搜索新增免密钥、免实例的 Bing 兜底源，公共 SearXNG 实例被墙时仍有结果返回。
- **断流重试**：调用失败或已取消的 AI 轮次底部新增「重试」按钮，复用原输入一键重发。
- **关于页面**：设置页新增「关于」入口，展示应用名与版本号，并提供版本通道（Beta / 正式版）说明。Beta 开关统一在「更新」tab，避免重复。左下角头像菜单可直达。

### 问题修复

- **修复联网搜索带参数必报错**：此前模型带 `limit` 参数调用 WebSearch 必报「Invalid JSON input for tool」，部分模型/中转会把数字序列化成字符串或发送 `null` / 空串 / 越界值，严格校验全部拒绝。现已改为宽松接收并在工具内归一化，任意入参都回落合法值，**永不报错**。
- **修复脚本插件解释器探测失败**：自制安装版下团队 / 本地 Python、Node 插件点开后误报「解释器缺失」并降级显示源码，实际运行时已随安装包分发。根因是运行时目录解析依赖了不可靠的 `resource_dir()`，已改为优先用「exe 同级 `runtimes/` 目录」（与安装器布局一致），探测恢复正常。
- **修复 WebSearch 搜索质量**：此前 Bing 兜底源用 `ensearch=0` 强制走 CN 审查索引，GitHub 与英文技术内容被严重降权；`site:` 操作符也常被忽略返回无关结果。已改为 `ensearch=1`（国际结果），新增 GitHub 原生搜索源（`site:github.com` 走 API 精确搜仓库），并加 `site:` 查询改写层（剥离前缀 + 按域名后过滤）。同时修复 Bing `/ck/a` 跳转链接解析（base64 解码，不再产出垃圾 URL）。
- **修复工具调用循环报错**：AI 反复调用 WebSearch 无法收敛达到轮次上限时，不再裸露英文报错 `Max turns (8) exceeded`，改为给出可操作的中文提示（可换关键词或点重试从进度续跑）。
- **修复重试重新开始**：此前调用失败后点「重试」会从头重跑（重新搜索、重新写代码），现已改为**断点续**——保留已完成的工作（已搜到的 WebSearch 结果、已写的代码），只重跑失败部分。
- **修复联网搜索静默失败**：此前所有搜索源被墙时后端静默返回空结果，前端显示「未搜到」；现在新增「全源不可达」诊断位，区分「真无结果」与「全源故障」，并以红色失败卡片明确暴露，便于定位。
- **弱网瞬时断连自动重试**：连接级网络抖动（DNS / 连接重置）在底层自动指数退避重试（0.5s / 1s / 2s），多数情况用户无感，无需手动重试。

### 优化

- **弱网提示**：流式生成超过 12 秒无新内容时，状态条切换为「网络较慢，仍在等待响应…」，引导停止后重试。

### 发布

- **版本统一到 0.0.10**：桌面端、Tauri 配置、安装器版本号同步更新。
- **双安装包**：本次起提供两种安装包，按需选用：
  - **官方 NSIS 安装包** `LingFang-0.0.10-Setup.exe`（57.2 MB，SHA-256：`2a3a6e98db28b5f7c4a33cd05d96ee0f71d2bbb348d646763b81fde70bb07bf8`）：Tauri 官方 NSIS 安装器，信誉好、可叠加代码签名，**推荐优先使用**。
  - **第三方 SFX 安装包** `LingFang-Setup-0.0.10.exe`（99.4 MB，SHA-256：`332837bfd3d7af936fb20768aa603eabf958bf82c3b84128292e4b21b226664e`）：自制自解压安装器，体积较大但免 NSIS 工具链，作为备选。
- **安装包安全缓解**：自制安装器此前被部分安全软件拦截（未签名 + 自解压行为像 dropper）。已给 installer.exe 嵌入合法 VERSIONINFO 版本元数据（公司名/产品名/版本），降低误判概率。彻底解决需代码签名证书（已留接口）。
- **Windows 安装包重打包**：使用自制安装器流程生成 `LingFang-Setup-0.0.10.exe`。

## v0.0.9

本次更新聚焦插件创建可靠性、计费展示准确性和 AI 输出区视觉统一，并重新生成 Windows 安装包。

### 新功能

- **模型计费层级展示**：钱包流水支持显示本次模型请求是“快速请求”还是“高级请求”，便于核对消耗来源。
- **AI 输出区升级**：工具调用展示参考 OpenCode 风格，支持紧凑步骤行、状态胶囊以及 `Input` / `Output` 折叠面板。
- **更新页 Beta 体验增强**：设置页更新区域加入版本通道、校验、安装说明和更清晰的下载进度展示。

### 问题修复

- **修复插件创建失败**：`CreatePlugin` 对 Python/Tkinter 等多行源码更稳健，支持 `capabilities: null` 和分行写入文件内容，降低 JSON 转义失败概率。
- **修复灵石小数噪声**：计费扣减统一四舍五入到两位小数，避免出现 `-0.0005636999999999999` 这类浮点残值。
- **修复流水弹窗背景不明显**：Dialog / Sheet 遮罩统一加强暗化和模糊效果。

### 发布

- **版本统一到 0.0.9**：桌面端、Tauri 配置、安装器、协作 API 和管理后台版本号同步更新。
- **Windows 安装包重打包**：使用自制安装器流程生成 `LingFang-Setup-0.0.9.exe`。

## v0.0.8

本次更新补齐平台插件创建链路、上下文压缩与系统提示词精简，让 AI 插件创建器更适合长对话和复杂项目转换。

### 新功能

- **上下文查看与压缩**：插件创建器支持查看当前上下文构成，并在长对话中压缩早期内容，保留近期需求和插件包关键信息。
- **草稿创建流程增强**：创建器可以更稳定地将 AI 生成结果保存到本地草稿，右侧草稿面板在会话恢复时保持同步。
- **安装包分发流程完善**：自制 Windows 安装器打包链路稳定产出可上传后台的安装包和 SHA-256 校验值。

### 优化

- **系统提示词精简**：减少冗余约束，把插件结构、运行时默认值和输出要求交由 skills 补充。
- **创建器工具链整理**：工具命名和展示统一为更接近代码助手的风格，降低用户理解成本。
- **构建产物核对**：打包后输出安装包体积与 SHA-256，方便版本发布后台核验。

### 发布

- **版本统一到 0.0.8**：桌面端、Tauri 配置、安装器版本号同步更新。
- **Windows 安装包**：使用自制安装器流程生成 `LingFang-Setup-0.0.8.exe`。

### ⬆️ 升级建议

建议所有用户升级至 v0.0.8，以获得上下文压缩能力与更稳定的草稿创建流程，长对话场景体验提升明显。

## v0.0.7

本次更新聚焦桌面端 UI 细节、创建器交互和安装器体验，为后续平台中转与插件创建能力打底。

### 新功能

- **Tab 切换动画效果**：提升交互流畅度，页面切换更自然。
- **创建器悬浮窗背景模糊效果**：视觉效果更现代，专注度更高。
- **文件/文件夹附加功能**：支持多文件选择和上下文提供，创建器可获得更丰富的输入信息。
- **AI 创建页面全面美化**：输出样式优化，阅读体验更好。

### 重构

- **“创建偏好”改名为“技能”**：术语更统一，概念更清晰。
- **刷新按钮统一到页面顶部**：交互更符合直觉。
- **文件选择器 UI 重构**：图标化设计，视觉更简洁。
- **安装器 UI 采用 Windows 11 风格圆角**：与系统视觉语言更一致。

### 体验优化

- **安装进度更清晰**：安装过程按阶段反馈，用户能看到当前执行状态。
- **安装器视觉效果提升**：整体设计更现代、更精致。
- **发布元数据补齐**：补充版本说明，为安装包和后台发布流程提供依据。

### 发布

- **版本统一到 0.0.7**：桌面端、Tauri 配置、安装器版本号同步更新。
- **Windows 安装包**：使用自制安装器流程生成 `LingFang-Setup-0.0.7.exe`。

### ⬆️ 升级建议

建议所有用户升级至 v0.0.7，以获得更流畅的交互体验与更精致的安装器视觉。

## v0.0.6

本次更新引入**计费系统与模型中转（Relay + 灵石 Credit）**，并把 AI 能力从「本地 ClaudeCode/Codex CLI 子进程」整体切换为「平台统一中转（SDK/HTTP）」。插件创建器随之重建为基于平台中转的版本。

### ✨ 新功能

- **模型中转层（Relay）**：平台后端新增 OpenAI 兼容 + Anthropic 兼容双协议中转（`/api/relay/v1/*`），支持流式 SSE 透传。所有 AI 调用统一经此咽喉，便于计费、日志与策略管控。
- **灵石（Credit）计费**：引入虚拟货币「灵石」，按**团队**账户计费，独立于人民币余额。每个模型/能力的单价、计费单位（按 token / 按次 / 按张）均由后台动态配置；预扣额度 + 实算冲销，单次硬上限保护用户。
- **多渠道管理**：后台可创建多个上游渠道，按全局/团队/角色绑定适用范围，支持优先级、权重与故障转移（one-api 范式）。
- **平台 API Key**：用户可在「设置 → 模型与计费」自助创建/吊销平台 API Key（`lf_` 前缀，明文仅显示一次），用于外部脚本/插件接入中转。
- **前台版本收敛**：前台移除自定义模型/key，仅保留「快速版 / 高级版」两个固定选项，底层模型与参数由后台统一配置。
- **调用日志**：后台提供多维度查询的调用日志（时间/用户/团队/模型/版本/能力/状态/灵石消耗等）。
- **AI 插件创建器（重建）**：侧栏「创建插件」入口，自然语言描述 → 走平台中转生成插件包 → 预览 → 上传到团队空间。

### 🔧 重构

- **移除 code_assistant CLI**：删除本地 ClaudeCode/Codex CLI 子进程生成那套（Rust `code_assistant` 模块 + 旧创建器前端），AI 能力全部走平台中转。通用子进程工具抽到独立 `process_util` 模块。
- **系统提示词强制规则**：中转层在所有 AI 调用注入「AI 能力必须且仅能使用灵坊平台服务」规则。
- 内置 `ai-wardrobe` 插件移除硬编码第三方 key，改走平台中转生图（按张计费）。

### ⚠️ 注意

- 管理员需在后台「计费与模型」配置**渠道**、**模型版本**（快速/高级对应底层模型）、**定价**并给团队调整灵石额度后，中转才能对外服务。
- 全部工作区包与应用版本统一至 0.0.6。

## v0.0.5

本次更新聚焦权限体系建设——两级权限节点、自定义角色、权限码细分与 web 端团队角色管理，让团队权限管控更精细；同时完善成员管理与团队管理 UI，并修复了权限迁移相关的若干关键问题。

### ✨ 新功能

- **两级权限体系**：平台级 + 团队级，权限按「模块 → 操作」两级组织（如「成员管理 → 邀请/移除/分配角色」），角色配置一目了然。
- **自定义角色**：团队管理员可为团队创建自定义角色并勾选权限码；平台管理员同样可管理平台级角色。内置角色（系统团队管理员 / 成员）权限锁定不可改。
- **权限码细分**：把多个敏感度混合的权限码拆细——插件编辑拆为「编辑元数据 / 编辑草稿 / 改价格」，用户管理拆为「改资料 / 重置密码」，团队角色拆为「创建 / 编辑 / 删除」，平台插件拆为「编辑 / 删除」，权限粒度更可控。
- **web 端团队角色管理**：平台管理员可在后台为任意团队创建 / 编辑 / 删除角色、勾选权限，并给成员分配该团队的自定义角色（不再限于「团队管理员 / 普通成员」二选一）。
- **插件授权**：团队管理员可为团队插件按用户或角色设置「允许 / 禁用」，deny 优先，精细化控制插件访问。
- **成员列表显示角色实际名字**：成员管理显示成员当前角色的真实名称（含自定义角色如「开发者」），不再只显示固定文案。

### 🛠 体验优化

- **平台 / 团队管理线路分离**：web 平台管理后台管平台级，桌面端团队管理管团队级，职责清晰不混淆。
- **团队管理 UI 完善**：插件授权对话框下拉加宽、选项完整可见；邀请码 tab 补状态中文化、过期时间设置、创建时间展示，公开加入开关统一为项目复选框样式。
- 全部工作区包版本统一至 0.0.5。

### 🐞 问题修复

- 修复 RBAC 迁移 SQL 的幂等性问题（VALUES 改 SELECT 支持重复执行、补足 updatedAt 列值）。
- 修复旧平台管理员在新权限守卫下被锁死的问题（所有 platformRole 写入点同步 platformRoleId）。
- 修复前后端权限逻辑断裂（session 现返回权限码，清理旧入口）。
- docker 启动流程加入 seed-rbac，确保权限码与角色权限在每次部署时正确填充。

### ⬆️ 升级建议

建议所有团队管理员升级至 v0.0.5，以获得两级权限与自定义角色能力。服务端更新后请务必先在测试环境跑一次 `seed-rbac` 并确认迁移断言为 0 残留，再正式对外。

## v0.0.4

本次更新带来内置 Python 与 Node 运行时，插件无需用户自备环境即可运行，并修复多项运行时与界面问题。

### ✨ 新功能

- **内置 Python 与 Node 运行时**：桌面端自带运行时，插件开箱即用，免去用户手动安装环境。
- **PluginRunner**：新增统一的插件运行管理组件，运行状态更可控。

### 🛠 体验优化

- 更新 Tauri 自动更新签名公钥，保障后续云端更新的验签。
- 优化运行时设置与插件启动流程。
- 忽略本地 AI 工具状态，避免误判干扰。

### 🐞 问题修复

- 修复内置 Python 创建虚拟环境失败的问题。
- 修复设置页顶部标签栏的显示问题。
- 修复自动更新资源地址未返回绝对 URL 的问题。

### ⬆️ 升级建议

建议所有用户升级至 v0.0.4，以获得内置运行时与更稳定的插件运行体验。

## v0.0.3

本次更新聚焦代码助手能力升级、插件全生命周期管理与桌面端使用体验，并修复了多项影响日常使用的问题。

### ✨ 新功能

- **代码助手全面升级**：重构为内置 SDK 运行时，流程更稳定、响应更连贯；配合全新的对话流式引擎，AI 回复实时分段输出，阅读体验更顺畅。
- **插件助手工具能力扩展**：助手可调用更丰富的本地工具来协助插件开发与调试，自动化处理更多场景。
- **插件管理增强**：
  - 支持直接修改已有插件并落盘保存。
  - 聊天中可通过 `@` 快速引用已安装插件。
  - 支持从桌面端删除插件（本地目录与云端记录同步清理）。
  - 插件运行崩溃时直接展示错误输出，并可一键让 AI 协助修复。
- **依赖安装优化**：pip 安装新增国内镜像源支持，并改进了安装失败时的错误提示，国内网络环境下安装更快、更省心。
- **版本发布管理**：管理后台版本发布模块完善，支持发布、改通道、调整时间、归档与恢复等操作。

### 🛠 体验优化

- 优化插件管理整体交互，包括插件内点击响应、运行状态实时刷新、列表布局对齐等细节。
- AI 对话标题自动生成更准确，回复内容排版更美观易读。
- 增强 Windows 平台进程清理能力，插件停止/退出更干净，避免残留进程。

### 🐞 问题修复

- 修复插件市场安装包与创建器选项的相关问题。
- 修复重启后未完成的草稿插件可能丢失的问题。
- 修复本地插件删除时显示成功但目录未真正清除的问题。
- 修复公开下载页下载链接异常、无法正常下载安装包的问题。
- 修复代码助手在自定义模型与多引擎切换场景下的若干路由与配置问题。

### ⬆️ 升级建议

建议所有用户升级至 v0.0.3，以获得更稳定的代码助手与更完善的插件管理体验。
