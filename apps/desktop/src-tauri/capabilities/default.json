{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "桌面壳默认能力。插件越权能力另由 capability 网关按 manifest + 授权放行。",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
