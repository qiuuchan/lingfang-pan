{
  "name": "@lingfang/plugin-preview",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "tsx watch src/start.ts",
    "start": "node dist/start.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:e2e": "playwright test -c playwright.config.ts"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "tsx": "^4.22.4",
    "typescript": "^5",
    "vitest": "^4.1.8"
  }
}
