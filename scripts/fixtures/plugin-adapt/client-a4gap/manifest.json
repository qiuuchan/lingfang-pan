{
  "name": "lingfang-smoke-client-a4gap",
  "description": "灵坊插件适配流水线 · 集成缺陷探针：把硬编码第三方 base URL 写成 client 插件里的顶层 const 声明。引擎 A4 归一化会把它改写成语法非法的 JS，而 client 运行时确证只验 HTML，兜不住。默认不跑，用 --probes 或 --only client-a4gap 触发。",
  "runtime_type": "client"
}
